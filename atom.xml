<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>苍耳的技术博客</title>
  <icon>https://www.gravatar.com/avatar/2f748ebf01f37cc394aa692ef4bca1c2</icon>
  <subtitle>无悔的人生</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://djs66256.github.io/"/>
  <updated>2021-12-22T14:03:08.862Z</updated>
  <id>http://djs66256.github.io/</id>
  
  <author>
    <name>苍耳</name>
    <email>djs66256@163.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>给面试者的一些建议</title>
    <link href="http://djs66256.github.io/2021/12/22/2021-12-22-%E7%BB%99%E9%9D%A2%E8%AF%95%E8%80%85%E7%9A%84%E4%B8%80%E4%BA%9B%E5%BB%BA%E8%AE%AE/"/>
    <id>http://djs66256.github.io/2021/12/22/2021-12-22-给面试者的一些建议/</id>
    <published>2021-12-22T14:02:29.000Z</published>
    <updated>2021-12-22T14:03:08.862Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>从一个面试者到面试官，然后再到考虑团队建设，很多看法都发生了很大的改变。但是在自己上百场的面试过程中，也看到了很多自己以前的影子。很多时候并不是自己不够优秀，而是在某些方面没有做好。所以这里来聊聊这些问题。</p><p>本文并不想传授什么面试技巧，或者刷提指南，个人认为知识永远是无法短时间内补齐的，只要是有经验的面试官，其实很容易拆穿这种小把戏。我更希望能给大家了解招聘和面试中的一些标准和要求，让大家把最好的一面表现出来。当然在这方面我也还没达到特别高的程度，只能聊聊招聘技术开发的角色的一些问题。</p><a id="more"></a><h2 id="面试官与团队角度"><a href="#面试官与团队角度" class="headerlink" title="面试官与团队角度"></a>面试官与团队角度</h2><p>很多人，包括我自己，以及刚开始做面试官的时候，都很难去思考我们的招聘标准以及对人的一个要求是什么。但这的确是招人最重要的一个环节。</p><h3 id="如何考察一个人"><a href="#如何考察一个人" class="headerlink" title="如何考察一个人"></a>如何考察一个人</h3><p>考察一个面试者，一般会从下面3个方面进行：</p><ol><li>知识</li><li>对知识的应用</li><li>软实力</li></ol><h4 id="知识"><a href="#知识" class="headerlink" title="知识"></a>知识</h4><p>毫无疑问，作为一个技术人员，知识是最基本的，知识的深度和体量直接决定了上层建筑，这也是最容易区分人和进行量化的部分。</p><p>相信大厂的职级系统应该大部分都是直接对标的知识这个维度来设计的。</p><h4 id="对知识的应用"><a href="#对知识的应用" class="headerlink" title="对知识的应用"></a>对知识的应用</h4><p>其实这一点并不太好衡量，这里包括业务经验，过往工作经验，同时作为编程能力的一部分，抽象、总结能力也是非常重要的一部分。所以这点往往会从过往的项目中去考察。</p><p>但也存在着一定的问题，一个是不同的业务方向会有不同的经验，另一个是不同的面试官对这个标准都不太一样，很难形成一致的标准。这也是为什么有些面试官会被吐槽，往往都是拿着自己业务的东西，代入到他人的项目中去问。</p><p>至于为什么有些大厂会考察算法题，我个人感觉还是有一定道理的。算法除了能看出个人的编码习惯，也能看出一定的抽象逻辑思维能力。单纯从能力角度去量化衡量太难了，而变成算法就容易多了。</p><h4 id="软实力"><a href="#软实力" class="headerlink" title="软实力"></a>软实力</h4><p>作为技术人员，软实力这部分是最不重要的。但是会作为你最终职位和定位的一个参考。这个会在下一点讨论。</p><p>所以，只要你技术方面足够厉害，作为技术开发这个职位来说，就不会有任何问题。从Google的招聘思路上，宁缺毋滥，也是类似的。我们可以花大价钱找一个不听话的技术达人（当然不是指破坏者），也不会找一个处事圆滑的老油条。</p><h3 id="从招人角度看"><a href="#从招人角度看" class="headerlink" title="从招人角度看"></a>从招人角度看</h3><p>我们招到的人不可能从一个模子里刻出来的，也不可能在各个方面都达到要求，而如何发挥每个人最大的作用这就属于管理范畴的内容了，团队人才培养与梯度建设不在这里讨论。所以从团队管理的角度看一个人的部分缺点是完全可以用团队来弥补的，这也是我为什么说软实力在技术开发的角色中并不是特别重要，只要你不是害群之马，都不存在问题。</p><p>所以，关键的是每个人都要有自己的特色，自己突出的能力，这才是团队比较喜欢的一点。阿里曾经的招聘要求是，必须招到比团队平均线以上的人。我个人的理解应该是，招到在某个方向，或者某个领域，能够超过目前团队能力的人，这样团队才能不停的变的更优秀。</p><p>说了这么多，你应该知道企业都想要找到什么样的人了吧？时刻问问自己，自己的优势是什么？不要相信木桶原理，不停的在自己不擅长的领域做无用功。当然基础知识这一点不算在内，因为这是基础的基础。</p><h2 id="面试者角度"><a href="#面试者角度" class="headerlink" title="面试者角度"></a>面试者角度</h2><p>在做面试官后，遇到过各式各样的面试者，这里说一说大家普遍性的几个问题吧。</p><h3 id="简历"><a href="#简历" class="headerlink" title="简历"></a>简历</h3><p>简历是我们给面试官的第一印象，但是从我的角度看来，我几乎没有遇到过一份简历是写的很好的。</p><p>从我个人的角度看，我并不想了解你的毕业，四六级成绩，考过什么证书，只会关注你的最近2年左右的工作内容，以及你对技术领域方面的能力。当然这里不是指这些信息没用，而是说对于技术面的面试官来说不是重点。</p><p>有些人可能觉得简历应该足够精简，控制在一页以内。其实我想说的是这个完全是误导人，如果你几年的工作经历能够凝练在十行以内，那么我想说的是完全看不出来你所做的事情。精简是对的，但是必须能够说明白你做的事情，你做这件事情的前后缘由，自己的思考和思路，以及最终产生的收益和结果，如果有后续的一些反思是最好的。你需要找到你最亮眼的工作内容，在其中体现出你对于技术的应用，对于问题的探索和思考，这才是一份有亮点的简历。同时，这也能引导面试官在这个方向上来提问题，而不是基于面试官的个人经验。</p><p>其中比较典型的问题是流水账，大部分简历都是写的是</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">XXX公司：</span><br><span class="line">1. 负责xxxx</span><br><span class="line">2. 负责xxx</span><br><span class="line">3. 性能优化</span><br></pre></td></tr></table></figure><p>当然不是说这样写存在问题，而是在这种描述中完全看不出你个人在这些工作中所做的贡献与价值，这些描述都过于泛泛而谈了，面试官也不知道改怎么询问你在这个业务中的贡献。</p><p>与其让面试官去挖掘你的亮点，为什么不直接展示出来呢？所以我的建议是不要被篇幅局限了，找到你自己觉得最优秀的几个方面，用最精炼的语言说明白你在这件事情中的贡献。当然这个要基于你真正做过的事情，不能凭空捏造，被发现了反而是件非常不好的事情。</p><p>至于怎么展示你的亮点，可以参考下Star法则。亮点贵在精，而不在多。</p><h3 id="准备些什么"><a href="#准备些什么" class="headerlink" title="准备些什么"></a>准备些什么</h3><p>很多人都会问，我要面试了，需要准备些什么，可以刷哪些题。而我想说的是，临时抱佛脚都是没用的，特别是在知识这一块。</p><p>知识只能通过平时的日积月累，以及平时遇到的困难中去总结，任何临阵磨枪其实在优秀的面试官面前都会原形毕露，只要随便换种方式问一问就能知道你是不是真正的掌握了。刷题能骗过的也就是在这一块也不懂的面试官。</p><p>那么我们需要准备什么呢？其实很简单，把自己之前做过的事情重新梳理一遍，把自己以前对其中的各种问题的思考都再总结一遍。确保自己能够在讲述自己做的事情的时候，是完整的，条理清晰的。如果有条件可以找别人听一听你的描述，在这个基础上可以进行改进下就好了。其实我们最忌讳问到面试者怎么做或怎么思考的时候，说这个东西其实也没做什么，这个东西是老板派的任务，这个时间有点久了，记不清了。</p><p>只要做好平时的积累，以及对自己做的事情的梳理，剩下的就交给机遇吧。</p><h3 id="面试中"><a href="#面试中" class="headerlink" title="面试中"></a>面试中</h3><p>面试中的问题只要你做好前几个，一般问题不大，但是必须要注意几个点。</p><h4 id="不要不懂装懂"><a href="#不要不懂装懂" class="headerlink" title="不要不懂装懂"></a>不要不懂装懂</h4><p>如果被问到知识盲区的时候，千万不要自以为是的回答，不知道就是不知道，没有人是完美的，面试官并不会因为一个盲点而否定一个人的能力，可以大方的承认自己在这块的能力不足。</p><p>如果你有自己的理解和推断，可以先承认自己的盲区，然后再说明自己的推断和思路。哪怕这种推断和实际情况不符，但是合理也不会有什么问题。</p><h4 id="描述要精炼"><a href="#描述要精炼" class="headerlink" title="描述要精炼"></a>描述要精炼</h4><p>有部分面试者会出现这类问题，觉得自己回答的东西越多，总有一个点答到了面试官的点。</p><p>可以说这种思路完全是错误的，首先不说你说的内容是否有面试官关心的点，你的长篇大论可能面试官根本没有耐心听完。因为面试官要从回答中提炼出重点，然后思考和组织你的知识体系，并且记录，本身就是一件非常耗精力的事情，可能只能专注你的回答前半分钟。所以切记不要有这种想法。</p><p>回答一定要精炼，有重点，最好能够做到结构化的去描述。</p><p>同时不要夸夸而谈，技术出身的人大部分都是非常务实的，避免天马行空而没有办法实际落地的东西。</p><p>技术人员一般对沟通能力不会做太大的要求，只要能精炼的描述清楚一件事情就好了。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>机遇总是给有准备的人的，所以只要在平时做好积累，总有一天会降临到你的头上的。</p><p>欢迎大家给我投简历 <a href="mailto:djs66256@163.com">djs66256@163.com</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;从一个面试者到面试官，然后再到考虑团队建设，很多看法都发生了很大的改变。但是在自己上百场的面试过程中，也看到了很多自己以前的影子。很多时候并不是自己不够优秀，而是在某些方面没有做好。所以这里来聊聊这些问题。&lt;/p&gt;
&lt;p&gt;本文并不想传授什么面试技巧，或者刷提指南，个人认为知识永远是无法短时间内补齐的，只要是有经验的面试官，其实很容易拆穿这种小把戏。我更希望能给大家了解招聘和面试中的一些标准和要求，让大家把最好的一面表现出来。当然在这方面我也还没达到特别高的程度，只能聊聊招聘技术开发的角色的一些问题。&lt;/p&gt;
    
    </summary>
    
    
      <category term="文章" scheme="http://djs66256.github.io/tags/%E6%96%87%E7%AB%A0/"/>
    
  </entry>
  
  <entry>
    <title>微前端笔记</title>
    <link href="http://djs66256.github.io/2021/06/06/2021-06-06-%E5%BE%AE%E5%89%8D%E7%AB%AF%E7%AC%94%E8%AE%B0/"/>
    <id>http://djs66256.github.io/2021/06/06/2021-06-06-微前端笔记/</id>
    <published>2021-06-06T12:26:43.000Z</published>
    <updated>2021-06-06T13:39:47.916Z</updated>
    
    <content type="html"><![CDATA[<p>目前很多大型业务随着业务的飞速膨胀，单体结构(monolithic)已经远不能符合当前开发模式了，所以后端有微服务，前端有微前端，客户端有组件化，其主要目标都是一致的，都是为了解决单体结构的问题，但是各端也有各自的一些特色，其中后端的拆分最为复杂，而客户端拆分最难以彻底，而微前端则介于这两者之间。</p><a id="more"></a><p><img src="/images/2021/micro_frontend/1.png" alt=""></p><p>一般来说，都是后端先进行的微服务拆分，而组织架构的调整最好是前后端保持一致，才能确保最低的沟通成本，而从康威定律来看，相应的架构也需要进行同样的调整。</p><p><img src="/images/2021/micro_frontend/2.png" alt=""></p><p>同时服务拆分也给我们带来了异构系统的可能性，不仅仅是后端服务的异构实现，甚至前端也可以采用不同的框架进行搭建，这给予了团队不同的技术选型空间。</p><p><img src="/images/2021/micro_frontend/3.png" alt=""></p><p>服务拆分的最大一个好处就是功能发版不再相互依赖了，减少了团队间的沟通成本与增加了迭代速度。</p><p><img src="/images/2021/micro_frontend/4.png" alt=""></p><p>而对于微前端来说，比较重要的几个方面就是：</p><ul><li>页面路由</li><li>跨组件通信</li><li>页面组合</li></ul><p><img src="/images/2021/micro_frontend/5.png" alt=""></p><p>路由的事情相对比较容易解决，对于多页应用来说，通过配置反向代理即可，只需要定义好各个团队之间的域即可，避免出现冲突等问题。</p><p>通信的问题对前端来说也比较容易解决，通过事件冒泡的形式向上传递事件，接收方可以通过在window上加入监听实现。</p><p><img src="/images/2021/micro_frontend/6.png" alt=""></p><p>其中组合的方式较为复杂，有多种形式进行，但是需要保证的是各个团队的职责足够的清晰。</p><p>页面合成可以通过：</p><ul><li>iframe</li><li>ajax</li><li>web component</li></ul><p>从渲染发生的时间可以分为：</p><ul><li>CSR</li><li>SSR</li><li>两者混合渲染</li></ul><p><img src="/images/2021/micro_frontend/7.png" alt=""></p><p><img src="/images/2021/micro_frontend/7.1.png" alt=""></p><p>可以看到两种渲染方式的明显区别。这种特点在加载时间上也带来了不一样的体验。</p><p><img src="/images/2021/micro_frontend/9.png" alt=""></p><p><img src="/images/2021/micro_frontend/10.png" alt=""></p><p>下面是技术选型的一个指导。</p><p><img src="/images/2021/micro_frontend/11.png" alt=""></p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>可以看出来微前端既有一部分微服务在后台拆分思路一致的地方，也有和客户端一样单体页面拆分问题。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;目前很多大型业务随着业务的飞速膨胀，单体结构(monolithic)已经远不能符合当前开发模式了，所以后端有微服务，前端有微前端，客户端有组件化，其主要目标都是一致的，都是为了解决单体结构的问题，但是各端也有各自的一些特色，其中后端的拆分最为复杂，而客户端拆分最难以彻底，而微前端则介于这两者之间。&lt;/p&gt;
    
    </summary>
    
    
      <category term="micro-frontend" scheme="http://djs66256.github.io/tags/micro-frontend/"/>
    
  </entry>
  
  <entry>
    <title>wakeup in XNU</title>
    <link href="http://djs66256.github.io/2021/04/03/2021-04-03-wakeup-in-XNU/"/>
    <id>http://djs66256.github.io/2021/04/03/2021-04-03-wakeup-in-XNU/</id>
    <published>2021-04-03T07:04:51.000Z</published>
    <updated>2021-04-03T07:45:41.680Z</updated>
    
    <content type="html"><![CDATA[<p>苹果在iOS13的时候，在内核中加入了一个新的性能衡量指标<code>wakeup</code>，同时由于这个指标而被系统杀死的应用数不胜数，其中也包括我们常用的微信淘宝等。而这个指标完全是由XNU内核统计的，所以我们很难通过日志等普通手段去准确的定位问题，所以这里通过另一种思路去解决这个问题。</p><a id="more"></a><h2 id="为什么要统计-wakeup"><a href="#为什么要统计-wakeup" class="headerlink" title="为什么要统计 wakeup"></a>为什么要统计 wakeup</h2><p>要定位这个问题，首先我们需要知道这个指标的目的是什么。</p><p>XNU中，对性能的指标有CPU、内存、IO，而<code>wakeup</code>属于CPU的性能指标，同时属于CPU指标的还有CPU使用率，下面是XNU中对其限制的定义。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Default parameters for CPU usage monitor.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Default setting is 50% over 3 minutes.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>         DEFAULT_CPUMON_PERCENTAGE 50</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>         DEFAULT_CPUMON_INTERVAL   (3 * 60)</span></span><br></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TASK_WAKEUPS_MONITOR_DEFAULT_LIMIT              150 <span class="comment">/* wakeups per second */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TASK_WAKEUPS_MONITOR_DEFAULT_INTERVAL   300 <span class="comment">/* in seconds. */</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Level (in terms of percentage of the limit) at which the wakeups monitor triggers telemetry.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * (ie when the task&#x27;s wakeups rate exceeds 70% of the limit, start taking user</span></span><br><span class="line"><span class="comment"> *  stacktraces, aka micro-stackshots)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TASK_WAKEUPS_MONITOR_DEFAULT_USTACKSHOTS_TRIGGER        70</span></span><br></pre></td></tr></table></figure><p>总结来说，当CPU使用率在3分钟内均值超过50%，就认为过度使用CPU，当<code>wakeup</code>在300秒内均值超过150次，则认为唤起次数过多，同时在阈值的70%水位内核会开启监控。</p><p>CPU使用率我们很容易理解，使用率越高，电池寿命越低，而且并不是线性增加的。那么<code>wakeup</code>又是如何影响电池寿命的呢？</p><p>首先我们需要看看ARM架构中对于CPU功耗问题的描述：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Many ARM systems are mobile devices and powered by batteries. In such systems, optimization of power use, and total energy use, is a key design constraint. Programmers often spend significant amounts of time trying to save battery life in such systems. </span><br></pre></td></tr></table></figure><p>由于ARM被大量使用与低功耗设备，而这些设备往往会由电池来作为驱动，所以ARM在硬件层面就对功耗这个问题进行了优化设计。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Energy use can be divided into two components: </span><br><span class="line"></span><br><span class="line">- Static </span><br><span class="line">Static power consumption, also often called leakage, occurs whenever the core logic or RAM blocks have power applied to them. In general terms, the leakage currents are proportional to the total silicon area, meaning that the bigger the chip, the higher the leakage. The proportion of power consumption from leakage gets significantly higher as you move to smaller fabrication geometries. </span><br><span class="line"></span><br><span class="line">- Dynamic </span><br><span class="line">Dynamic power consumption occurs because of transistor switching and is a function of the core clock speed and the numbers of transistors that change state per cycle. Clearly, higher clock speeds and more complex cores consume more power. </span><br></pre></td></tr></table></figure><p>功耗可以分为2种类型，即静态功耗与动态功耗。</p><p>静态功耗指的是只要CPU通上电，由于芯片无法保证绝对绝缘，所以会存在“漏电”的情况，而且越大的芯片这种问题越严重，这也是芯片厂家为什么拼命的研究更小尺寸芯片的原因。这部分功耗由于是硬件本身决定的，所以我们无法去控制，而这种类型功耗占比不大。</p><p>动态功耗指的是CPU运行期间，接通时钟后，执行指令所带来的额外开销，而这个开销会和时钟周期频率相关，频率越高，耗电量越大。这也就说明了苹果为什么会控制CPU使用率，而相关研究（Facebook也做过）也表明，CPU在20以下和20以上的能耗几乎是成倍的增加。</p><p>CPU使用率已经能够从一定程度上限制电池损耗问题了，那么<code>wakeup</code>又是什么指标呢？</p><h2 id="wakeup-是什么"><a href="#wakeup-是什么" class="headerlink" title="wakeup 是什么"></a>wakeup 是什么</h2><p>要了解<code>wakeup</code>是什么，首先要知道ARM低功耗模式的2个重要指令<code>WFI</code>和<code>WFE</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ARM assembly language includes instructions that can be used to place the core in a low-power state. The architecture defines these instructions as hints, meaning that the core is not required to take any specific action when it executes them. In the Cortex-A processor family, however, these instructions are implemented in a way that shuts down the clock to almost all parts of the core. This means that the power consumption of the core is significantly reduced so that only static leakage currents are drawn, and there is no dynamic power consumption. </span><br></pre></td></tr></table></figure><p>通过这2个指令进入低功耗模式后，时钟将会被关闭，这个CPU将不会再执行任何指令，这样这个CPU的动态能耗就没有了。这个能力的实现是由和CPU核心强绑定的空转线程<code>idle thread</code>实现的，有意思的是XNU中的实现较为复杂，而<code>Zircon</code>中则非常直接暴力：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">__NO_RETURN <span class="keyword">int</span> <span class="title">arch_idle_thread_routine</span><span class="params">(<span class="keyword">void</span>*)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="function">__asm__ <span class="title">volatile</span><span class="params">(“wfi”)</span></span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在XNU中，一个CPU核心的工作流程被概括为如下状态机：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> *           -------------------- SHUTDOWN</span></span><br><span class="line"><span class="comment"> *          /                     ^     ^</span></span><br><span class="line"><span class="comment"> *        _/                      |      \</span></span><br><span class="line"><span class="comment"> *  OFF_LINE ---&gt; START ---&gt; RUNNING ---&gt; IDLE ---&gt; DISPATCHING</span></span><br><span class="line"><span class="comment"> *         \_________________^   ^ ^______/           /</span></span><br><span class="line"><span class="comment"> *                                \__________________/</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><p>而<code>wakeup</code>则表示的是，从低功耗模式唤起进入运行模式的次数。</p><h2 id="wakeup-如何统计的"><a href="#wakeup-如何统计的" class="headerlink" title="wakeup 如何统计的"></a>wakeup 如何统计的</h2><h4 id="ARM异常系统"><a href="#ARM异常系统" class="headerlink" title="ARM异常系统"></a>ARM异常系统</h4><p>CPU时钟被关闭了，那么又要怎么唤起呢？这就涉及到CPU的异常系统。</p><p>在ARM中，异常和中断的概念比较模糊，他把所有会引起CPU执行状态变更的事件都称为异常，其中包括软中断，debug中断，硬件中断等。</p><p>从触发时机上可以区分为同步异常与异步异常。这里指的同步异步并不是应用程序的概念，这里同步指的是拥有明确的触发时机，比如系统调用，缺页中断等，都会发生在明确的时机，而异步中断，则完全无视指令的逻辑，会强行打断指令执行，比如FIQ和IRQ，这里比较典型的是定时器中断。</p><p>异常系统有很多能力，其中一个重要的能力就是内核态与用户态切换。ARM的执行权限分为4个等级，EL0，EL1，EL2，EL3。其中EL0代表用户态，而EL1代表内核态，当用户态想要切换至内核态的时候，必须通过异常系统进行切换，而且异常系统只能向同等或更高等级权限进行切换。</p><p><img src="/images/2021/wakeup/el.png" alt=""></p><p><img src="/images/2021/wakeup/routines.png" alt=""></p><p>那么这么多类型的异常，又是如何响应的呢？这里就涉及到一个异常处理表（exception table），在系统启动的时候，需要首先就去注册这个表，在XNU中，这个表如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">    .section __DATA_CONST,__const</span><br><span class="line">    .align 3</span><br><span class="line">    .globl EXT(exc_vectors_table)</span><br><span class="line">LEXT(exc_vectors_table)</span><br><span class="line">    &#x2F;* Table of exception handlers.</span><br><span class="line">         * These handlers sometimes contain deadloops. </span><br><span class="line">         * It&#39;s nice to have symbols for them when debugging. *&#x2F;</span><br><span class="line">    .quad el1_sp0_synchronous_vector_long</span><br><span class="line">    .quad el1_sp0_irq_vector_long</span><br><span class="line">    .quad el1_sp0_fiq_vector_long</span><br><span class="line">    .quad el1_sp0_serror_vector_long</span><br><span class="line">    .quad el1_sp1_synchronous_vector_long</span><br><span class="line">    .quad el1_sp1_irq_vector_long</span><br><span class="line">    .quad el1_sp1_fiq_vector_long</span><br><span class="line">    .quad el1_sp1_serror_vector_long</span><br><span class="line">    .quad el0_synchronous_vector_64_long</span><br><span class="line">    .quad el0_irq_vector_64_long</span><br><span class="line">    .quad el0_fiq_vector_64_long</span><br><span class="line">    .quad el0_serror_vector_64_long</span><br></pre></td></tr></table></figure><h4 id="wakeup-计数"><a href="#wakeup-计数" class="headerlink" title="wakeup 计数"></a>wakeup 计数</h4><p>那么我们回过头来看看<code>wakeup</code>计数的地方：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> *  thread_unblock:</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *  Unblock thread on wake up.</span></span><br><span class="line"><span class="comment"> *  Returns TRUE if the thread should now be placed on the runqueue.</span></span><br><span class="line"><span class="comment"> *  Thread must be locked.</span></span><br><span class="line"><span class="comment"> *  Called at splsched().</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">boolean_t</span></span><br><span class="line">thread_unblock(</span><br><span class="line">    <span class="keyword">thread_t</span>                thread,</span><br><span class="line">    <span class="keyword">wait_result_t</span>   wresult)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="comment">// . . .</span></span><br><span class="line">    <span class="keyword">boolean_t</span> aticontext, pidle;</span><br><span class="line">    ml_get_power_state(&amp;aticontext, &amp;pidle);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Obtain power-relevant interrupt and “platform-idle exit&quot; statistics.</span></span><br><span class="line"><span class="comment">     * We also account for “double hop” thread signaling via</span></span><br><span class="line"><span class="comment">     * the thread callout infrastructure.</span></span><br><span class="line"><span class="comment">     * DRK: consider removing the callout wakeup counters in the future</span></span><br><span class="line"><span class="comment">     * they’re present for verification at the moment.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (__improbable(aticontext <span class="comment">/* . . . */</span>)) &#123;</span><br><span class="line">  <span class="comment">// wakeup ++</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// . . .</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而这里的<code>aticontext</code>则是通过<code>ml_at_interrupt_context</code>获取的，其含义则是是否处于中断上下文中。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> *  Routine:        ml_at_interrupt_context</span></span><br><span class="line"><span class="comment"> *  Function:   Check if running at interrupt context</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">boolean_t</span></span><br><span class="line">ml_at_interrupt_context(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* Do not use a stack-based check here, as the top-level exception handler</span></span><br><span class="line"><span class="comment">     * is free to use some other stack besides the per-CPU interrupt stack.</span></span><br><span class="line"><span class="comment">     * Interrupts should always be disabled if we’re at interrupt context.</span></span><br><span class="line"><span class="comment">     * Check that first, as we may be in a preemptible non-interrupt context, in</span></span><br><span class="line"><span class="comment">     * which case we could be migrated to a different CPU between obtaining</span></span><br><span class="line"><span class="comment">     * the per-cpu data pointer and loading cpu_int_state.  We then might end</span></span><br><span class="line"><span class="comment">     * up checking the interrupt state of a different CPU, resulting in a false</span></span><br><span class="line"><span class="comment">     * positive.  But if interrupts are disabled, we also know we cannot be</span></span><br><span class="line"><span class="comment">     * preempted. */</span></span><br><span class="line">    <span class="keyword">return</span> !ml_get_interrupts_enabled() &amp;&amp; (getCpuDatap()-&gt;cpu_int_state != <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么<code>cpu_int_state</code>标记又是在什么时候设置上去的呢？只有在<code>locore.S</code>中，才会更新该标记：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">str        x0, [x23, CPU_INT_STATE]            &#x2F;&#x2F; Saved context in cpu_int_state</span><br></pre></td></tr></table></figure><br>同时发现如下几个方法会配置这个标记：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">el1_sp0_irq_vector_long</span><br><span class="line">el1_sp1_irq_vector_long</span><br><span class="line">el0_irq_vector_64_long</span><br><span class="line">el1_sp0_fiq_vector_long</span><br><span class="line">el0_fiq_vector_64_long</span><br></pre></td></tr></table></figure><p>结合上述的异常处理表的注册位置，与ARM官方文档的位置进行对比，可以发现：</p><p><img src="/images/2021/wakeup/ints.png" alt=""></p><p>这几个中断类型均为FIQ或者IRQ，也就是硬中断。由此我们可以判断，<code>wakeup</code>必然是由硬中断引起的，而像系统调用，线程切换，缺页中断这种并不会引起<code>wakeup</code>。</p><h4 id="进程统计"><a href="#进程统计" class="headerlink" title="进程统计"></a>进程统计</h4><p>由上可以看出，<code>wakeup</code>其实是对CPU核心唤起次数的统计，和应用层的线程与进程似乎毫不相干。但从程序执行的角度思考，如果一个程序一直在运行，就不会进入等待状态，而从等待状态唤醒，肯定是因为某些异常中断，比如网络，vsync等。</p><p>在CPU核心被唤醒后，在当前CPU核心执行的线程会进行<code>wakeup++</code>，而系统统计维度是应用维度，也就是进程维度，所以会累计该进程下面的所有线程的<code>wakeup</code>计数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">queue_iterate(&amp;task-&gt;threads, thread, <span class="keyword">thread_t</span>, task_threads) &#123;</span><br><span class="line">        info-&gt;task_timer_wakeups_bin_1 += thread-&gt;thread_timer_wakeups_bin_1;</span><br><span class="line">        info-&gt;task_timer_wakeups_bin_2 += thread-&gt;thread_timer_wakeups_bin_2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以在我们代码中，如果在2个不同线程启用用同样的定时器，<code>wakeup</code>是同一个线程起2个定时器的2倍（同样的定时器在底层其实是一颗树，注册同样的定时器实际只注册了一个）。</p><p>用户层获取该统计值则可以通过如下方式：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mach/task.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mach/mach.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">BOOL <span class="title">GetSystemWakeup</span><span class="params">(NSInteger *interrupt_wakeup, NSInteger *timer_wakeup)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">task_power_info</span> <span class="title">info</span> =</span> &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="keyword">mach_msg_type_number_t</span> count = TASK_POWER_INFO_COUNT;</span><br><span class="line">    <span class="keyword">kern_return_t</span> ret = task_info(current_task(), TASK_POWER_INFO, (<span class="keyword">task_info_t</span>)&amp;info, &amp;count);</span><br><span class="line">    <span class="keyword">if</span> (ret == KERN_SUCCESS) &#123;</span><br><span class="line">        <span class="keyword">if</span> (interrupt_wakeup) &#123;</span><br><span class="line">            *interrupt_wakeup = info.task_interrupt_wakeups;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (timer_wakeup) &#123;</span><br><span class="line">            *timer_wakeup = info.task_timer_wakeups_bin_1 + info.task_timer_wakeups_bin_2;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (interrupt_wakeup) &#123;</span><br><span class="line">            *interrupt_wakeup = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (timer_wakeup) &#123;</span><br><span class="line">            *timer_wakeup = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="wakeup-治理"><a href="#wakeup-治理" class="headerlink" title="wakeup 治理"></a>wakeup 治理</h2><p>从以上分析来看，我们只需要排查各种硬件相关事件即可。</p><p>从实际排查结果来看，目前只有定时器或者拥有定时能力的类型是最普遍的场景。</p><p>比如<code>NSTimer</code>，<code>CADisplayLink</code>，<code>dispatch_semaphore_wait</code>，<code>pthread_cond_timedwait</code>等。</p><p>关于定时器，我们尽量复用其能力，避免在不同线程去创建同样的定时能力，同时在回到后台的时候，关闭不需要的定时器，因为大部分定时器都是UI相关的，关闭定时器也是一种标准的做法。</p><p>关于wait类型的能力，从方案选择上避免轮询的方案，或者增加轮询间隔时间，比如可以通过try_wait，runloop或者EventKit等能力进行优化。</p><h2 id="监控与防劣化"><a href="#监控与防劣化" class="headerlink" title="监控与防劣化"></a>监控与防劣化</h2><p>一旦我们知道了问题原因，那么对问题的治理比较简单，而后续我们需要建立持续的管控等长效措施才可以。</p><p>在此我们可以简单的定义一些规则，并且嵌入线下监控能力中：</p><ul><li>定时器时间周期小于1s的，在进入后台需要进行暂停</li><li>wait类型延迟小于1s，并且持续使用10次以上的情况需要进行优化</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>wakeup</code>由于是XNU内核统计数据，所以在问题定位排查方面特别困难，所以从另一个角度去解决这个问题反而是一种更好的方式。</p><p>同时从XNU中对CPU功耗的控制粒度可以看出，苹果在极致的优化方面做的很好，在自身的软件生态中要求也比价高。电量问题在短时间内应该不会有技术上的突破，所以我们自身也需要多思考如何减少电池损耗。</p><h4 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h4><p><a href="/references/arm/DDI0487G_a_armv8_arm.pdf">armv8</a></p><p><a href="/references/arm/DEN0024A_v8_architecture_PG.pdf">armv8 program guide</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;苹果在iOS13的时候，在内核中加入了一个新的性能衡量指标&lt;code&gt;wakeup&lt;/code&gt;，同时由于这个指标而被系统杀死的应用数不胜数，其中也包括我们常用的微信淘宝等。而这个指标完全是由XNU内核统计的，所以我们很难通过日志等普通手段去准确的定位问题，所以这里通过另一种思路去解决这个问题。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="iOS" scheme="http://djs66256.github.io/tags/iOS/"/>
    
      <category term="wakeup" scheme="http://djs66256.github.io/tags/wakeup/"/>
    
      <category term="XNU" scheme="http://djs66256.github.io/tags/XNU/"/>
    
  </entry>
  
  <entry>
    <title>Flutter 图片控件适配之路</title>
    <link href="http://djs66256.github.io/2020/12/10/2020-12-10-Flutter%E5%9B%BE%E7%89%87%E7%BB%84%E4%BB%B6%E8%B8%A9%E5%9D%91/"/>
    <id>http://djs66256.github.io/2020/12/10/2020-12-10-Flutter图片组件踩坑/</id>
    <published>2020-12-10T15:33:36.000Z</published>
    <updated>2021-04-03T07:11:06.767Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>目前大部分应用都会使用大量的图片，图片成为当前应用带宽占比最大的一种资源。在我们接入 Flutter 的时候，发现 Flutter 的图片控件缓存完全由自己管理，同时还没有提供磁盘缓存（1.22版本），所以在性能以及体验上均比较差，所以必须对其进一步优化。</p><a id="more"></a><h2 id="图片缓存"><a href="#图片缓存" class="headerlink" title="图片缓存"></a>图片缓存</h2><p>在目前很多 CDN 实现上，所有资源都是拥有唯一 uri 的，所以很多的客户端实现，是忽略了 HTTP 协议中的 Caches 能力，而是直接将 uri 作为唯一标识符来判断图片资源是否唯一的。这样大大节省了向服务端确认 304 的时间与请求。</p><p>而在客户端，一般都会存在至少<code>内存</code>和<code>磁盘</code>这两级缓存，而我们在接入 Flutter 图片库的时候，就希望能够将客户端的缓存与 Flutter 中的缓存进行打通，从而减少内存和网络的消耗。</p><p>而目前复用缓存的方向大致有如下3种：</p><ol><li>复用视图，完全由客户端来提供 Flutter 的图片能力，就像 React Native 一样。</li><li>复用磁盘缓存，不复用内存缓存，这种方案实现相对简单，但会导致内存中存在两份图片数据。</li><li>复用内存缓存，由客户端从磁盘加载到内存，并由客户端来管理整个缓存的生命周期，比如和 SDWebImage 进行深度融合。该方案看似是最完美的复用，而且客户端有能力对整个应用的图片缓存大小进行精确的控制。</li></ol><p>那么下面我们来看看这几种方案的实现，哪些看似美好的方案，我们都踩了哪些坑。</p><h2 id="复用视图"><a href="#复用视图" class="headerlink" title="复用视图"></a>复用视图</h2><p>Flutter 提供了一种和客户端原生视图进行无缝拼接的方案，原始的动机其实是为了像地图、WebView 这种场景，Flutter 不可能再去实现一套如此复杂的控件。那么如果我们用这个来做客户端图片桥接方案会怎么样呢？</p><p>首先，我们要明白 PlatformView 是如何进行桥接的（以下讨论的都是iOS实现）。在 Widget 中插入一层客户端 View，此时并不是我们想的那样，将此 View 简单的<code>draw</code>到 Flutter Root Layer 上。因为 Flutter 的<code>draw call</code>并不是发生在主线程上的，而是发生在<code>raster</code>线程上的，如果我们想要将客户端的 View 绘制到 Flutter 上，则必须先光栅化为一张图片，然后再进行绘制，这中间的性能开销与延迟显而易见是不可接受的，同时每帧都需要这么做也是不现实的。</p><p>所以，Flutter 采用了一种拆分 Flutter Layer 的形式。在插入一个客户端 View 后，Flutter 会自动将自己拆为2层：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">|-----| Flutter Overlay View 2</span><br><span class="line">|-----| Native View</span><br><span class="line">|-----| Flutter Root View 1</span><br></pre></td></tr></table></figure><p>客户端 View 就像夹心饼干一样被2个 Flutter view 夹住，此时位于 Platform View 上层以及后续的兄弟 Widget 都会被绘制到上层的 View 上，其他的依旧绘制在底层。这样虽然解决了客户端视图的接入，但也会导致一个问题，当上层视图发生位置等变更的时候，需要重新创建对应的 Overlay View，为了减少这种开销，Flutter 采用了一种比较 trick 的做法，即 Overlay View 会铺满屏幕，而通过移动上面的 mask 来进行控制展示区域。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// The overlay view wrapper masks the overlay view.</span></span><br><span class="line"><span class="comment">// This is required to keep the backing surface size unchanged between frames.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Otherwise, changing the size of the overlay would require a new surface,</span></span><br><span class="line"><span class="comment">// which can be very expensive.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// This is the case of an animation in which the overlay size is changing in every frame.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// +------------------------+</span></span><br><span class="line"><span class="comment">// |   overlay_view         |</span></span><br><span class="line"><span class="comment">// |    +--------------+    |              +--------------+</span></span><br><span class="line"><span class="comment">// |    |    wrapper   |    |  == mask =&gt;  | overlay_view |</span></span><br><span class="line"><span class="comment">// |    +--------------+    |              +--------------+</span></span><br><span class="line"><span class="comment">// +------------------------+</span></span><br></pre></td></tr></table></figure><p>目前已经解决了客户端视图接入 Flutter 的能力，但可以看到，当插入一张客户端 View，Flutter 需要额外创建2个 View 进行分区域绘制。当一个页面存在多张图片的时候，此时额外产生的开销显然也是不可接受的，性能更是不可接受。</p><p>下面是 Flutter 官方在 Platform View 上描述的关于性能的考虑。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Platform views in Flutter come with performance trade-offs.</span><br><span class="line"></span><br><span class="line">For example, in a typical Flutter app, the Flutter UI is composed on a dedicated raster thread. This allows Flutter apps to be fast, as the main platform thread is rarely blocked.</span><br><span class="line"></span><br><span class="line">While a platform view is rendered with Hybrid composition, the Flutter UI is composed from the platform thread, which competes with other tasks like handling OS or plugin messages, etc.</span><br><span class="line"></span><br><span class="line">Prior to Android 10, Hybrid composition copies each Flutter frame out of the graphic memory into main memory, and then copies it back to a GPU texture. In Android 10 or above, the graphics memory is copied twice. As this copy happens per frame, the performance of the entire Flutter UI may be impacted.</span><br><span class="line"></span><br><span class="line">Virtual display, on the other hand, makes each pixel of the native view flow through additional intermediate graphic buffers, which cost graphic memory and drawing performance.</span><br></pre></td></tr></table></figure><h2 id="复用磁盘缓存"><a href="#复用磁盘缓存" class="headerlink" title="复用磁盘缓存"></a>复用磁盘缓存</h2><p>让我们都退一步，我们首先解决网络带宽的问题，那么一个简单的方案便是复用磁盘缓存。</p><p>复用磁盘缓存的方案相对可以做的非常简单，并且拥有极低的倾入性。我们只需要设计一套 channel 接口，来同步双方缓存的状态和缓存的地址。</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">getCacheInfo(&#123; </span><br><span class="line">    <span class="built_in">String</span> url,</span><br><span class="line">    <span class="built_in">double</span> width,</span><br><span class="line">    <span class="built_in">double</span> height,</span><br><span class="line">    <span class="built_in">double</span> scale,</span><br><span class="line">    BoxFit fit&#125;) </span><br><span class="line">-&gt; &#123;<span class="built_in">String</span> path, <span class="built_in">bool</span> exists&#125;</span><br></pre></td></tr></table></figure><p>那么在使用的时候，我们仅需要定制一套新的 ImageProvider，将网络、本地两种 Provider 统一起来即可。</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">_CompositeImageStreamCompleter(&#123;</span><br><span class="line">    <span class="built_in">String</span> url,</span><br><span class="line">    <span class="built_in">double</span> width,</span><br><span class="line">    <span class="built_in">double</span> height</span><br><span class="line">&#125;) &#123;</span><br><span class="line">    getCacheInfo(&#123;url: url, width: width, height:height&#125;)</span><br><span class="line">        .then((info) &#123;</span><br><span class="line">        <span class="keyword">if</span> (info != <span class="keyword">null</span> &amp;&amp; info.path != <span class="keyword">null</span> &amp;&amp; info.path.length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> imageProvider;</span><br><span class="line">        <span class="keyword">var</span> decode = <span class="keyword">this</span>.decode;</span><br><span class="line">        <span class="keyword">if</span> (info.exists) &#123;</span><br><span class="line">            <span class="keyword">final</span> imageFile = File(info.path);</span><br><span class="line">            imageProvider = FileImage(imageFile, scale: <span class="keyword">this</span>.scale);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            imageProvider = NetworkImage(info.fixUrl ?? <span class="keyword">this</span>.url,</span><br><span class="line">                scale: <span class="keyword">this</span>.scale, headers: <span class="keyword">this</span>.headers);</span><br><span class="line">            decode = (Uint8List bytes,</span><br><span class="line">                &#123;<span class="built_in">int</span> cacheWidth, <span class="built_in">int</span> cacheHeight, <span class="built_in">bool</span> allowUpscaling&#125;) &#123;</span><br><span class="line">            <span class="keyword">final</span> cacheFile = File(info.path);</span><br><span class="line">            <span class="comment">// 缓存到磁盘</span></span><br><span class="line">            cacheFile.writeAsBytes(bytes).then((value) =&gt; &#123; &#125;);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.decode(bytes,</span><br><span class="line">                cacheWidth: cacheWidth,</span><br><span class="line">                cacheHeight: cacheHeight,</span><br><span class="line">                allowUpscaling: allowUpscaling);</span><br><span class="line">            &#125;;</span><br><span class="line">        &#125;</span><br><span class="line">        _childCompleter = imageProvider.load(imageProvider, decode);</span><br><span class="line">        <span class="keyword">final</span> listener =</span><br><span class="line">            ImageStreamListener(_onImage, onChunk: _onChunk, onError: _onError);</span><br><span class="line">        _childCompleter.addListener(listener);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;).catchError((err, stack) &#123;</span><br><span class="line">        <span class="built_in">print</span>(err);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里需要注意的是，当不存在磁盘缓存的时候，这里采用了 Flutter 来下载图片，此时需要我们手动将其保存到磁盘上，以保证磁盘缓存的一致性。</p><h2 id="复用内存缓存"><a href="#复用内存缓存" class="headerlink" title="复用内存缓存"></a>复用内存缓存</h2><p>复用磁盘缓存是风险较低的一种改动，但是代价是无法复用内存缓存，不仅仅需要分别读取，同时会保存多份内存缓存，因为双方的内存缓存部分是完全独立存在的。</p><p>那么如果我们想进一步优化，则需要采用复用内存缓存的方案，目前同步内存缓存大致有如下几种方案：</p><ul><li>利用 channel 通信，将内存传输给 Flutter</li><li>利用新特性 ffi 通道，将内存直接传递给 Flutter</li><li>利用 Texture 控件，从纹理层面进行复用</li></ul><h3 id="Channel"><a href="#Channel" class="headerlink" title="Channel"></a>Channel</h3><p>Flutter 官方稳定的消息通信方案，兼容性和稳定性都非常高。当我们需要展示缓存图片的时候，只需要将图片数据通过 BinaryMessenger 形式传递到 Flutter 即可。</p><p>由于 Channel 本身就必须是异步过程，所以该方式通信会有一定开销。</p><p>同时由于 Channel 在客户端是在主线程进行处理，所以也需要注意避免在主线程直接做加载与解码等耗时操作。</p><p>而 Channel 在数据传递过程中，由于机制（从安全角度来看也必须这么做）原因，二进制数据必然会被拷贝一份，这样导致的结果是 Flutter 这边维护的内存缓存和客户端自身的缓存依然是两份，并没有完美的达到我们上述的复用效果。</p><h3 id="ffi"><a href="#ffi" class="headerlink" title="ffi"></a>ffi</h3><p>从消息通信开销以及消息的内存拷贝问题来看，ffi 的出现似乎能够完美解决 Channel 中所有的问题。</p><p>原理和实现过程与 Channel 完全一致，此时只需要替换为 ffi 通道即可。ffi 并没有像 Channel 那么长的通信过程，不需要进行消息序列化与解析，也不需要切换线程处理，就像一个 HTTP 请求和一个简单的 API 调用的区别一样。</p><p>这里我们需要注意的是 ffi 接口是同步执行的，也就是说客户端执行的时候是处于 flutter.ui 线程，我们必须注意线程安全问题。而对于 Flutter 来说，由于是在 UI 线程执行，所以该方法必须尽量快的返回，不能执行一些耗时比较长的操作。</p><p>但是我们采用 ffi 就真的能够解决上述问题吗？仔细研究发现，其实还是不能解决内存复用的根本性问题，下面可以看下 ffi 转换的过程。</p><p>当我们把客户端图片加载到内存的时候，是通过 Buffer 的形式传递给 Flutter 的，比如是这样一个结构：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Buffer</span> &#123;</span></span><br><span class="line">    int8    *ptr;</span><br><span class="line">    <span class="keyword">size_t</span>  length;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而 Flutter 中图片解码过程会在 engine 层的 io 线程中执行，此时会进行一次图片数据拷贝。</p><p>所以，从最终结果来看，并不比 Channel 有更高的缓存复用能力。</p><h3 id="Texture"><a href="#Texture" class="headerlink" title="Texture"></a>Texture</h3><p>另一种是共享 PixelBuffer，也就是解码后的图片数据，在 Flutter 这里可以采用 Texture 来实现复用。</p><p>具体实现方案阿里已经研究的非常透彻，这里就不再复述了，我们主要分析下其性能与复用能力。</p><p>Texture 复用采用的是 TextureId，这是一个 int 值，所以在两端通信上不存在数据量上的性能开销。其主要过程是：</p><ol><li>客户端将纹理注册到 Flutter，同时会返回一个 id 作为唯一标识符（i++）。这个过程发生在 Platform 线程，也就是客户端主线程，而真正注册到 TextureRegistry 中则是在 raster 线程中完成的。</li><li>在 flutter.ui 线程处理 paint 事件的时候，会将该 id 传递给 TextureLayer。</li><li>并在 raster 线程，从 TextureRegistry 中取出并生成 draw call。</li></ol><p>从整体流程来看，Flutter 在中间流转过程全程只使用了 TextureId，并不会操作内存与纹理，并不存在多份缓存的问题。所以这种方案比较完美的解决了上述两个问题。</p><h2 id="内存优化"><a href="#内存优化" class="headerlink" title="内存优化"></a>内存优化</h2><p>虽然从上述分析中，缓存利用率最高的是 Texture，但是从内存上来分析，则出现了一个意想不到的结果。</p><p><img src="/images/2020/flutter-image/origin+10M.png" alt=""></p><p>上图是使用 Flutter Image 控件，加载几张大图的一个内存图，总共增加了 10M 内存消耗。</p><p><img src="/images/2020/flutter-image/uncrop 55-18M.png" alt=""></p><p>上图是使用 Texture 方案，加载同样图片所产生的内存消耗，达到了 37M，相差巨大。</p><p>同时可以看到原生 Flutter 图片在初始阶段有一个比较大的波峰，同样纹理也有，但相对平缓一些。</p><p>产生这样大的区别主要还是要从 Flutter Image 控件的渲染流程中说起。</p><ol><li>ImageProvider 将图片加载到内存后，首先会进行解码，而这个事情实在 flutter.io 线程完成的。</li><li>图片数据解码之后，会造成一个非常大的内存消耗，因为此时的图片数据是以 pixel buffer 的形式存储的。而 Flutter 在这一过程会进行一个优化，此时解码的数据将不是 100% 大小的，而是会当前 widget size进行调整，计算出一个最优的大小，然后在这一大小上进行解码，所以原生的 Image 反而在内存占用这个方面会比客户端更优秀。</li><li>在图片移除后，Flutter 会立刻回收解码后的内存，即 Flutter 仅对图片的原始压缩数据进行存储，并不缓存 pixel buffer。而我们客户端（SDWebImage）则会缓存解码后的全部数据，这也是另一个 Flutter 内存表现比客户端要优的地方。</li></ol><p>那么 Flutter 这种策略在内存占用上完胜客户端，是否就必然是好的呢？</p><p>其实从渲染流程中看，Flutter 仅仅是用解码时间换取了内存空间。在实际 Demo 中，列表快速滑动时，Flutter Image 控件的图片展示会有明显的延迟，而采用 Texture 方案，肉眼几乎无法分辨。所以从整体的表现上来说，Texture 方案并不是没有优点。</p><h2 id="Size"><a href="#Size" class="headerlink" title="Size"></a>Size</h2><p>从上述中可以看出来，Texture 方案在内存的表现上比较差，那么我们如何去进一步优化呢？</p><p>对于很多场景，比如用户头像等，都是有一个固定大小的，那么我们可以将该大小作为参数，传给 CDN，在 CDN 上就进行裁剪成我们需要的大小，这样也会节省大量流量。</p><p>但是同样有很多场景，我们是无法得到其控件大小的，比如充满容器大小这种场景。我们如何自动在所有图片上加上 Size 参数呢？</p><p>从渲染过程中，<code>Layout</code>之后会触发<code>Paint</code>，而此时该控件的大小必然已经是完全确定的了，那么我们可以在这里做一个假的占位控件，在计算出大小后，再替换为真正的图片。</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> ImageSizeResolve = <span class="keyword">void</span> <span class="built_in">Function</span>(Size size);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ImageSizeProxyWidget</span> <span class="keyword">extends</span> <span class="title">SingleChildRenderObjectWidget</span> </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> ImageSizeProxyWidget(&#123;Key key, Widget child, <span class="keyword">this</span>.onResolve&#125;)</span><br><span class="line">      : <span class="keyword">super</span>(key: key, child: child);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">final</span> ImageSizeResolve onResolve;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  ImageSizeProxyElement createElement() =&gt; ImageSizeProxyElement(<span class="keyword">this</span>);</span><br><span class="line"></span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  ImageSizeRenderBox createRenderObject(BuildContext context) =&gt;</span><br><span class="line">      ImageSizeRenderBox(onResolve);</span><br><span class="line"></span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  <span class="keyword">void</span> updateRenderObject(</span><br><span class="line">      BuildContext context, <span class="keyword">covariant</span> ImageSizeRenderBox renderObject) &#123;</span><br><span class="line">    <span class="keyword">super</span>.updateRenderObject(context, renderObject);</span><br><span class="line">    renderObject.onResolve = onResolve;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ImageSizeProxyElement</span> <span class="keyword">extends</span> <span class="title">SingleChildRenderObjectElement</span> </span>&#123;</span><br><span class="line">  ImageSizeProxyElement(RenderObjectWidget widget) : <span class="keyword">super</span>(widget);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ImageSizeRenderBox</span> <span class="keyword">extends</span> <span class="title">RenderProxyBox</span> <span class="title">with</span> <span class="title">RenderProxyBoxMixin</span> </span>&#123;</span><br><span class="line">  ImageSizeRenderBox(ImageSizeResolve onResolve, [RenderBox child])</span><br><span class="line">      : onResolve = onResolve,</span><br><span class="line">        <span class="keyword">super</span>(child);</span><br><span class="line"></span><br><span class="line">  ImageSizeResolve onResolve;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  <span class="keyword">void</span> paint(PaintingContext context, ui.Offset offset) &#123;</span><br><span class="line">    <span class="keyword">if</span> (hasSize) &#123;</span><br><span class="line">      <span class="keyword">if</span> (onResolve != <span class="keyword">null</span>) onResolve(size);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">super</span>.paint(context, offset);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，我们就能强制所有图片都必须带上 Size 参数了。</p><p><img src="/images/2020/flutter-image/crop+2M.png" alt=""></p><p>经过这样的优化处理后，内存占用下降到了 2M 左右（由于我用的测试图都是高清图，所以效果看上去会比较明显）。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Flutter 的很多思路和策略和客户端有着明显的区别，从图片这一个能力来看，就可以从各个方面进行适配与优化，如果需要达到完美可用的一个状态，看来还是需要不断的投入与探索。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h2&gt;&lt;p&gt;目前大部分应用都会使用大量的图片，图片成为当前应用带宽占比最大的一种资源。在我们接入 Flutter 的时候，发现 Flutter 的图片控件缓存完全由自己管理，同时还没有提供磁盘缓存（1.22版本），所以在性能以及体验上均比较差，所以必须对其进一步优化。&lt;/p&gt;
    
    </summary>
    
      <category term="Flutter" scheme="http://djs66256.github.io/categories/Flutter/"/>
    
    
      <category term="Flutter" scheme="http://djs66256.github.io/tags/Flutter/"/>
    
      <category term="Image" scheme="http://djs66256.github.io/tags/Image/"/>
    
      <category term="Texture" scheme="http://djs66256.github.io/tags/Texture/"/>
    
  </entry>
  
  <entry>
    <title>谈谈 Flutter、客户端、React 组件的几个区别</title>
    <link href="http://djs66256.github.io/2020/12/01/2020-12-1-%E8%B0%88%E8%B0%88Flutter%E4%B8%8EReact%E7%9A%84%E5%87%A0%E4%B8%AA%E5%8C%BA%E5%88%AB/"/>
    <id>http://djs66256.github.io/2020/12/01/2020-12-1-谈谈Flutter与React的几个区别/</id>
    <published>2020-12-01T15:37:30.000Z</published>
    <updated>2021-01-01T07:19:11.882Z</updated>
    
    <content type="html"><![CDATA[<p>Flutter 是 Google 新的一种跨端方案，大家都非常的看好他的上限。但是实际和大家了解下来，却处于一种比较尴尬的地位。客户端开发觉得这个和原生开发相差很大，学习成本陡峭，而前端又觉得这是一个客户端的技术，同时 dart 语言也远没有 js 灵活好用。但技术总是万变不离其宗，很多思想其实都是殊途同归。这里我们就简单讨论一下 Flutter 与 React 之间的一些区别与联系，有 React 开发经验的同学会更容易理解。</p><a id="more"></a><h2 id="Widget-与-Component-与-View"><a href="#Widget-与-Component-与-View" class="headerlink" title="Widget 与 Component 与 View"></a>Widget 与 Component 与 View</h2><p>Flutter 的 Widget 是页面的最基础组成部分，这个很类似与 React 中的 Component 和客户端中 View 的概念，但却又有本质上的区别。</p><p>首先不同的点是 mutable，Widget 是不可修改的，一旦创建就无法进行修改，因为一旦修改了其内容，就无法正确的对其进行 diff。这一点其实和 Component 很相似，虽然 Component 可以直接修改，但本质上还是需要通过状态机进行更新。而 View 则是完全开放其修改的能力的。</p><p>其次，概念本身会有区别，大致可以这么认为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RenderWidget + RenderElement + RenderObject &#x3D;&#x3D; Component + &lt;div&gt; &#x3D;&#x3D; View</span><br></pre></td></tr></table></figure><p>Widget 不一定会承载视图，即可能承载的是纯粹的业务逻辑或者消息通信能力，不像 Component，即使是个桥接能力的组件，也会生成一个 <code>div</code> 标签。粒度比 Component 更细，这可能也是前端同学觉得不够灵活的一个原因吧。</p><p>当 Widget 承载视图的时候，也不是直接承载的，而是通过 RenderElement 进行管理状态，RenderObject 管理布局与绘制。从客户端的角度看，可能这样的对应关系更为贴切：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">RenderElement -&gt; View</span><br><span class="line">RenderObject  &gt;&#x3D; Layer</span><br></pre></td></tr></table></figure><p>从绘制角度看，一个 Widget 未必就代表一个图层，而对客户端来说，一个 View 则必然代表一个或多个图层。Flutter 会对其进行优化，将多个 Widget 合并到一个图层上进行绘制，也有可能一个 Widget 会拆分为多个图层。虽然业务开发过程中不会接触到这个概念，但是必须了解其不同点。其中 RepaintBoundary 就是强制进行拆分图层的一个组件。</p><h2 id="Stateful-与-Stateless"><a href="#Stateful-与-Stateless" class="headerlink" title="Stateful 与 Stateless"></a>Stateful 与 Stateless</h2><p>Flutter 和 React 进行界面更新，都是通过状态机进行的，个人认为这种思路非常的优秀，而客户端则很少基于这种思路进行开发。从性能来看状态机流程所需要处理的流程会多的多，而目前来说，基本不存在这种瓶颈，但在开发过程中却需要一定的技巧，减少状态变更导致的视图树重建开销。</p><p>在 React 中，并没有严格的 Stateful 和 Stateless 的区分，只有 class component 和 function component 的区别，Flutter 将这两个概念进行了严格的区分，不允许混用，同时 Flutter 将很多不同类型的能力都进行了特化，目的就是为了进一步的优化性能。</p><p>在 Flutter 中 Stateless 仅仅是对于 Widget 层的逻辑而言，并不是代表了这个组件是完全不会有状态更新的，这个在下面的依赖绑定中可以看到。</p><h2 id="Dependency"><a href="#Dependency" class="headerlink" title="Dependency"></a>Dependency</h2><p>依赖绑定是 Flutter 中的一个优化特性，React 也可以做到同样的能力，但似乎并不是官方提供的能力。</p><p>依赖绑定让我们拥有了一种控制局部更新的能力，最典型的就是系统的 Theme 组件。如果没有这种能力，那么我们必须在每个使用该能力的地方，都注册监听来更新当前状态机，或者直接重建整棵树。前者会大大增加代码复杂度，而后者则会大大增加性能开销。</p><p>个人觉得依赖绑定特别适合应用于数据类型状态机与组件之间的关系绑定。比如当前用户信息状态，不同页面信息状态同步等，这种范围相对较大，同时又需要在多个地方进行数据同步。甚至我们可以实现自定义规则的依赖绑定，这些都可以根据我们的需要进行。</p><p>举个例子，当前用户头像中 VIP 状态同步，可以思考下直接使用 instance 和使用 of 之间的区别：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Widget build(Context context) &#123;</span><br><span class="line">    <span class="keyword">return</span> AvatarImage(&#123;</span><br><span class="line">        url: CurrentUser.instance.avatarUrl,</span><br><span class="line">        childBuilder: (ctx) &#123;</span><br><span class="line">            <span class="keyword">final</span> currentUser = CurrentUser.of(ctx)</span><br><span class="line">            <span class="keyword">return</span> currentUser.isVIP ? VIP() : <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>依赖绑定的发挥空间很大，很多功能都可以考虑下这种能力。</p><h2 id="Layout"><a href="#Layout" class="headerlink" title="Layout"></a>Layout</h2><p>Flutter 中的布局方案和前端基本保持一致，都是 Flex 布局。但是在 Flutter 中，布局模式被切分为各个子项进行特化，并且是与渲染组件是分离的，而不像网页那样，单个组件可以添加各种属性。这种改变让我感觉特别不适应，更不要说前端开发了，但这对于第一次接触的人来说，学习成本似乎会低一些。</p><p>前端写法类似于：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Foo(</span><br><span class="line">    style: &#123;</span><br><span class="line">        padding: xxx,</span><br><span class="line">        width: xxx,</span><br><span class="line">        height: xxx,</span><br><span class="line">    &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>而在 Flutter 则需要改为如下结构，似乎变得更加啰嗦了。</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Padding (</span><br><span class="line">    SizedBox (</span><br><span class="line">        Foo()</span><br><span class="line">    )</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>布局拆分特化同时也减少了代码复杂度，以及自己新增布局能力的灵活性。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>可以看到，这几种技术的基本元素依然可以关联起来，只是各自有各自的一些独特能力，相信了解 React 或者 SwiftUI 这种框架的人应该能够很快适应这种开发。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Flutter 是 Google 新的一种跨端方案，大家都非常的看好他的上限。但是实际和大家了解下来，却处于一种比较尴尬的地位。客户端开发觉得这个和原生开发相差很大，学习成本陡峭，而前端又觉得这是一个客户端的技术，同时 dart 语言也远没有 js 灵活好用。但技术总是万变不离其宗，很多思想其实都是殊途同归。这里我们就简单讨论一下 Flutter 与 React 之间的一些区别与联系，有 React 开发经验的同学会更容易理解。&lt;/p&gt;
    
    </summary>
    
      <category term="文章" scheme="http://djs66256.github.io/categories/%E6%96%87%E7%AB%A0/"/>
    
    
      <category term="Flutter" scheme="http://djs66256.github.io/tags/Flutter/"/>
    
      <category term="React" scheme="http://djs66256.github.io/tags/React/"/>
    
  </entry>
  
  <entry>
    <title>RN优化实践之拆包与预热</title>
    <link href="http://djs66256.github.io/2020/10/17/2020-10-17-RN%E4%BC%98%E5%8C%96%E5%AE%9E%E8%B7%B5%E4%B9%8B%E6%8B%86%E5%8C%85%E4%B8%8E%E9%A2%84%E7%83%AD/"/>
    <id>http://djs66256.github.io/2020/10/17/2020-10-17-RN优化实践之拆包与预热/</id>
    <published>2020-10-17T09:24:08.000Z</published>
    <updated>2020-10-17T09:32:42.747Z</updated>
    
    <content type="html"><![CDATA[<p>随着RN技术在业务中广泛的应用，一些比较重要的功能也开始采用RN的方案来进行了，这就给RN页面的打开速度提出了更高的要求，因为打开速度是影响用户跳出率的重要原因之一。</p><a id="more"></a><h2 id="拆包"><a href="#拆包" class="headerlink" title="拆包"></a>拆包</h2><p>对于RN打开速度优化，业界比较通用的方案也就是预热+拆分基础包，减少容器初始化时间和基础库加载时间。</p><p>对RN进行拆包可以依赖于官方提供的工具进行，但是官方提供的能力是JS内部的一个拆分加载，如果我们需要做容器预热，则无法使用官方的加载方案，而需要我们从客户端原本的逻辑中进行，进行多步加载。</p><p>我们需要对RN的逻辑进行改造，就需要对RN初始化逻辑有所了解。</p><p><img src="/images/2020/react-native-multipart/before.png" alt=""></p><p>上图是一个大致的过程，这里我们对比较关键的几个步骤进行简单的说明。</p><ol><li>RNBridge在实例化之后，会首先准备好JS运行线程和原生模块。</li><li>然后会创建一个JSExcutor，这个执行器决定了JS执行环境是客户端还是远程调试（安卓可以是自己定制的执行器，比如v8）。</li><li>加载源码（bundle），这个根据来源不同可能是从本地加载，也可能通过url从远端加载。</li><li>由于初始化JS执行器和代码是并行触发的，这里需要一个栅栏同步两者结果，之后开始将代码放入执行器执行（JS代码运行）。</li><li>在此之后，客户端会监听垂直同步信号（该信号的作用是在页面发生变更的时候，需要重新刷新页面）。</li><li>此时RootView收到JS加载完成的通知，开始触发RunApp逻辑，该逻辑就是启动前端的app注册表中对应的应用。</li></ol><p>整个流程比较长，但是分工还是相当明确的，此次拆包改造的地方也非常明确。</p><p><img src="/images/2020/react-native-multipart/after.png" alt=""></p><p>上图中绿色框内就是我们此次改造的点，这里为了逻辑简单与实际需求，将加载代码设计了串行加载，如果有需要，加载过程也可以进行并发。</p><p>这里我们对加载能力进行一次抽象，加载一段代码定义为一个<code>SourceLoader</code>，那么一个拆包bridge就相当于有一个加载器列表，对应于bridge上的属性就非常简单。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSArray</span>&lt;<span class="keyword">id</span>&lt;RCTBridgeSourceLoaderProtocol&gt;&gt; *preloadSourceLoaders;  <span class="comment">// 预加载的加载器</span></span><br><span class="line">- (<span class="keyword">void</span>)preloadSourceWithCompletion:(<span class="keyword">void</span>(^)(<span class="built_in">NSError</span> *error))completion; <span class="comment">// 触发预加载加载器</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSArray</span>&lt;<span class="keyword">id</span>&lt;RCTBridgeSourceLoaderProtocol&gt;&gt; *sourceLoaders;  <span class="comment">// 非预加载加载器</span></span><br><span class="line">- (<span class="keyword">void</span>)loadSourceWithCompletion:(<span class="keyword">void</span>(^)(<span class="built_in">NSError</span> *error))completion;  <span class="comment">// 触发非预加载器</span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)loadAllSourcesWithCompletion:(<span class="keyword">void</span>(^)(<span class="built_in">NSError</span> *error))completion;  <span class="comment">// 先加载预加载代码，再加载非预加载代码</span></span><br></pre></td></tr></table></figure><p>这里有一个需要注意的点是，我们需要启动一个垂直同步信号监听，为了性能考虑，需要在预热容器加载到真正视图的时候才能开启，所以这里对加载器增加一个标记，只有加载到该加载器之后才能开启监听。</p><p>经过这样的改造，我们的RN就已经支持了多包分布加载了。我们就可以把一些基础功能的js代码打包进app内部，也减少一些包大小。</p><h2 id="容器预热"><a href="#容器预热" class="headerlink" title="容器预热"></a>容器预热</h2><p>以上的分包加载并不能对加载速度有太大的影响，而真正的优化点是容器的预热，可以将很多准备工作先做了，在业务加载的时候只会触发加载业务代码与渲染页面。</p><p>受限于手机性能的局限，以及一些苹果官方的策略，我们不太可能无限制的去使用该能力，所以这里按3个方面来看预热。</p><p><strong>预热触发时机</strong></p><p>目前预热触发的时机主要有下面3个点</p><ul><li>冷启动</li><li>热启动</li><li>容器复用之后</li></ul><p>在这些时机触发之后，再延迟一定时间（几秒），进行创建预热实例。延迟一会的原因是这些时机大概率都是在做一些CPU密集型任务，如果此时再加入创建预热容器这种非必须的任务，反而可能影响主业务的一些性能。</p><p><strong>预热容器销毁</strong></p><p>目前销毁的时机主要有下面2个</p><ul><li>内存警告</li><li>进入后台</li></ul><p>进入后台销毁的目的主要是为了降低后台运行的内存，虽然影响不是很大，但是目前苹果对后台app策略还是比较严格的，减少一点是一点。</p><p><strong>预热的场景化</strong></p><p>由于RN这种业务在我们的业务中并不是主流业务，可能大部分用户都不会使用到RN，而我们对全量用户进行无差别的开启预热功能，也不是一种最优的方式。目前我们还没有能力对用户场景进行机器学习这样的智能化分析，那么这次就对一些场景做一下简单的归类：</p><ul><li>3天内没有使用过RN，则认为该用户</li><li>3天内没有由于内存警告而销毁的记录</li><li>当前应用启动周期内：<ul><li>预加载失败3次，则该周期内不再启用预加载</li></ul></li></ul><p>容器预热的关键点是在不影响用户其他体验的时候，尽可能的提高预热的命中率，目前做的一些策略都比较简单，后续如果要优化，就需要深入业务场景中。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>此次RN的优化分为拆包和预热两部分，各自能力独立，并分别进行AB控制，最大可能保证稳定性。后续优化将会深入业务场景去做一些优化策略。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;随着RN技术在业务中广泛的应用，一些比较重要的功能也开始采用RN的方案来进行了，这就给RN页面的打开速度提出了更高的要求，因为打开速度是影响用户跳出率的重要原因之一。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="react-native" scheme="http://djs66256.github.io/tags/react-native/"/>
    
  </entry>
  
  <entry>
    <title>跨端能力平台化建设</title>
    <link href="http://djs66256.github.io/2020/10/05/2020-10-05-%E8%B7%A8%E7%AB%AF%E8%83%BD%E5%8A%9B%E5%B9%B3%E5%8F%B0%E5%8C%96%E5%BB%BA%E8%AE%BE/"/>
    <id>http://djs66256.github.io/2020/10/05/2020-10-05-跨端能力平台化建设/</id>
    <published>2020-10-05T14:12:59.000Z</published>
    <updated>2020-10-13T18:37:50.390Z</updated>
    
    <content type="html"><![CDATA[<p>跨端能力一直是近些年来的大热门，我们对跨端能力也一直抱有非常高的期望，我从2016年开始就一直关注着跨端能力的发展，经历了其中各个发展阶段，而目前我们团队也将建设整个跨端平台化能力，这里就来讨论下跨端的一些东西，以及我们的平台化会如何建设整个生态。</p><a id="more"></a><h2 id="我们真的需要跨端能力吗"><a href="#我们真的需要跨端能力吗" class="headerlink" title="我们真的需要跨端能力吗"></a>我们真的需要跨端能力吗</h2><p>跨端的发展基本经历了这么几个阶段，<code>h5</code>-&gt;<code>RN/weex</code>-&gt;<code>小程序</code>-&gt;<code>flutter</code>，而最近flutter成了最火爆的弄潮儿，但是我们可以看到，每种跨端方案在提出之初，都被吹捧为将会颠覆整个客户端开发，而现实却是原生开发依然占据着无法撼动的地位。</p><p>我在早期也非常的看好跨端方案，因为这是唯一一种能够同时大量降低业务开发工作量的方案，而且跨端的很多技术方案也是领先原生开发一个世代的，无论从学习成本，开发效率以及一些前后端融合的趋势都有着非常优秀的设计，以至于我一直认为跨端将是原生开发的未来，原生开发模式的终结者。然而后续的现实却深深的打了我的脸，从facebook放弃h5以及RN，阿里放弃weex，说明当时的跨端方案几乎是失败的。为什么我会认为当时的方案几乎是失败的，我们又将如何对待跨端这项技术呢，接下来我们可以简单的分析一下。</p><h4 id="早期的跨端方案是失败的"><a href="#早期的跨端方案是失败的" class="headerlink" title="早期的跨端方案是失败的"></a>早期的跨端方案是失败的</h4><p>早期的跨端方案基本都是打着消灭原生开发的口号而进行的，从Facebook对h5的各种优化来看，认为当前的网页早已经能够承担所有能力了，而且随着手机摩尔定律的发展，不用几年，手机就会和电脑一样，能够很好的承载网页了。而现实却是Facebook最终还是没有等来那个美好的未来。</p><p>随着前端技术的快速发展，衍生出了react和vue这样的技术，这项技术给前端的开发模式带来了巨大的变革，从此前端不需要再依赖jQuery这样去操作具体节点了，所有DOM节点都被抽象化，这给也给一批跨端先驱者们带来了曙光。既然DOM节点可以被抽象化，同样的原生View也可以被抽象化，并通过映射关系，组建原生视图。可以说，这种思路是一个跨时代的想法，第一次前端与原生应用进行了一次激烈的碰撞。这种碰撞也催生了众多的新的思路与设计理念（比如swiftUI，个人也将部分理念贯彻到了原生开发之中，来解决原生开发的一些问题），在跨端方面也催生了RN和weex这两种方案，这两种方案都以<code>write once, run anywhere</code>这样的主旨努力，想要统一前端、iOS、安卓这几个平台。</p><p>但这些技术的最终结果却是很悲惨，Facebook放弃了h5和RN，目前RN也只是开源爱好者在用爱发电了，而阿里也逐步淘汰了weex，可能国内某些厂在内部还在维护一些魔改版。两大创时代的技术创造者最后却放弃了，这让我陷入深深的怀疑，跨端的方向是否是正确的。</p><p>大家可能会说，现在还是有大量的公司在使用这种方案，并不代表这项技术是失败的。但我个人认为它的失败在于它并没有完成最早所承诺的能力与效果。</p><p>实行这种方案对人员素质要求非常高，需要开发仅对前端、移动端都有所涉猎，在出现一些兼容问题的时候能够自主解决该问题。而现实是这种人才真的太少了，特别是在国内的这种环境，要找到并留住这种人才是非常困难的，而如果人员素质没有达到如此高的程度，那么会大大增加多端的沟通成本，甚至比原生开发需要更多的人力成本（以个人经验来看，一旦涉及到跨端能力接入，那么至少需要1iOS+1安卓+1前端人力，包括接口设计、调试等沟通时间，未必会比原生开发减少太多的人力）。</p><p>如果缺乏一个能够掌控全局的人，那么每次需要一个原生能力的时候，便会新增一个接口，而这些接口能力并没有经过良好设计，从而导致这些能力的复用性极差，以至于后期这类接口能力的爆炸，无法维护。这类问题虽然看似比较少见，但在一个业务快速膨胀的团队，仅需要1年时间就可以让沟通成本急剧增加。（不要说文档，如果文档能够解决这类问题，也就不存在前后端接口爆炸的问题了，而且文档也是一种大大增加沟通成本的东西，并且我不相信目前国内有哪个团队在文档这块做到及格的。）</p><p>双端有着这种不一致的体验与设计语言，<code>write once, run anywhere</code>这个理念本身就是一个无法达到的乌托邦，并且react-native自身也认识到这个问题，并没有把<code>run anywhere</code>作为自己的目标，而是区分对待两端的特殊性，而在上层进行整合这种特殊性，所以依然无法逃避在不同平台实现不同逻辑。而weex则更激进一些，甚至将原生View给映射为div这种前端标签，想要一次性整合3端，然而这种巨大的不一致性所导致的结果是彻底的失败，最终还是仅仅在移动端进行了部分整合。</p><p>当然这些跨端方案还有另一个问题就是通信成本特别高，特别是一些实时性、流畅性要求高的场景，往往是达不到要求的。</p><p>综上所述，早期的跨端方案在多个维度都没有兑现自己的想法，所以我认为是失败的，但是这些方案却给我们带了一个意料之外的惊喜，那就是动态化，这个之后再说。</p><h4 id="特定领域跨端化实践"><a href="#特定领域跨端化实践" class="headerlink" title="特定领域跨端化实践"></a>特定领域跨端化实践</h4><p>在经历了早期跨端实践的失败后，大家都开始思考跨端的未来，如果还是想要统一各端的开发，那么很有可能将会经历一样的失败。此时微信找到了一个极好的应用场景，那便是小程序。</p><p>小程序这种场景有着这么几个特点：</p><ul><li>对动态性要求特别高，因为苹果不允许执行下发动态库，所以必须采用动态化特性</li><li>对性能要求一般，但对功能要求较高，需要能够满足大量现有能力</li><li>要求较好的隔离性，由于子应用是可以由3方提供服务，那么主应用与子应用之间必须是单向管理的关系，需要严格限制子应用的能力特性，以防被3方软件滥用</li><li>要求较高的鲁棒性，主应用就相当于操作系统，能够保持每次升级与优化的兼容性与一致性</li></ul><p>那么从以上几个特点来看，设计一套简化版的DSL是必须的，同时需要设计自己的通用能力与权限系统，以及OAuth2认证等。而目前最为符合，同时也是最小开发量的就是h5技术，所以目前常见的小程序基本还是以h5容器为主。</p><p>小程序的出现，不仅仅是跨端领域的一次新的尝试，同时也是业务领域的一个创举。小程序的出现，将一个普通的应用，演化成一个完整的平台系统，不仅仅给其他公司提供了一个发展的平台，同时也帮助自己巩固自身的平台地位。将一个巨应用，逐步转化为微应用集合，减少各类成本的同时，也极大的丰富了这种功能服务。可以说小程序就是大平台应用的一次战略性布局，当然现在已经展示出了它的价值。</p><p>这次，小程序给跨端指明了一个方向，那就是在特定领域创造价值。</p><h4 id="统一移动端思潮的亡者归来"><a href="#统一移动端思潮的亡者归来" class="headerlink" title="统一移动端思潮的亡者归来"></a>统一移动端思潮的亡者归来</h4><p>flutter的出现，大家又掀起一次跨端思潮的崛起，但这次的崛起，似乎减少了对双端的统一要求（安卓的material与iOS的cupertino），大家似乎已经从狂热的思潮中冷静下来，认识到了各端不一致性的必然性，以及用户对各自平台的接受程度，这次的革新，似乎已经不是<code>write once, run anywhere</code>这样的呐喊，而更多的是对所有“大前端”的开发模式的一次统一。（有很多人认为<code>write once, run anywhere</code>就是flutter的目的，并且是跨端的终极模式，个人认为并不是，这个口号官方从来没有喊过，而且flutter的野心比单纯的实现跨端方案要大的多）</p><p>flutter的目的并不是单纯的实现一个跨端方案，所以就决定了所采用的技术就不会依赖于各个平台的UI层，它从最基础的GPU绘制开始，完全使用自己的能力，这是一套非常完整的UI框架。由于flutter完全抛弃了各平台的UI层，那么便不存在各个平台的差异性了（这其实也是个坑），理论上可以做到不需要做任何兼容性实现。但现实却是，平台的差异性目前已经深入人心，粗暴的提供一种效果和习惯并不能收获用户最好回应。但是如果你是想要拿着flutter去实现安卓的效果，或者实现iOS的效果，那么你可以打消这个念头了，他能做到的最多是“模拟”，而非“实现”。</p><p>那么flutter的应用场景就比较固定了，我们依然只能在一些重业务与展示的场景去使用，而在个性非常突出的场景，以及涉及到大量原生能力的地方，就显得不那么适合了。</p><p>为什么我会认为flutter想要打造一个完善的统一的UI开发平台才是其真正的目的。由于目前移动端的开发人员依然比较割裂，而未来的趋势必然是整个前端的大融合，甚至是与部分后端的融合，让业务方去关注整个业务而不是业务的一个点，这样的融合就必须首先统一各端的开发模式以增加效率和人员的流通性，可以说flutter的出现对于移动端未来发展也有深刻的影响。</p><p>所以在此看来flutter依旧不能完全实现跨端统一这一乌托邦，但是其引入的很多前端思路将会给目前的客户端开发模式带来足够的冲击。由于flutter这个项目还在快速发展中，所以我会进行持续关注。</p><h4 id="跨端能力总结"><a href="#跨端能力总结" class="headerlink" title="跨端能力总结"></a>跨端能力总结</h4><p>在进入跨端平台化建设之前，我们花了大量的篇幅来回顾各个时间段的跨端发展历程，我这里更多的采用了批判性的角度去看待这些技术，目的是为了找到各个技术的局限性与应用场景。各种技术都有自己的优势与局限性，我们无法将其单纯的判定为成功与失败。</p><p>跨端的思路也给我们带来了一些业务上的创新力与未来开发模式的一次变革机会。我们可以看看如何做所有跨端能力平台化整合。</p><h2 id="平台化"><a href="#平台化" class="headerlink" title="平台化"></a>平台化</h2><p>目前业务的发展导致当前项目中引入了多种跨端方案，而在如此多的方案中，我们又该如何选择，如何去有效的利用各个能力，这是我们希望完成跨端平台化的初衷。</p><p>跨端方案想要解决的问题无非是两大类：</p><ol><li>动态性（快速迭代）</li><li>开发效率（成本）</li></ol><p>由于客户端应用更新的特性，导致我们的新功能、新特性永远无法第一时间到达用户手上，从安卓碎片化程度来看，就知道想让用户保持更新的习惯是多么困难的了。那么在一些场景，比如活动、微应用等，会非常依赖于实时性调整，就会有强烈的动态能力要求。</p><p>而效率问题一直是我们所探讨的，虽然有很多种解决效率问题的方案，但跨端给我们提供的一种思路似乎就是一种万能且直接有效的方案，能够大量缩减客户端的人力，毕竟前端人员比客户端人员会好招的多。</p><p>但是跨端方案毕竟不是万能的，在诸多方案上还是拥有大量限制，而理想化的一些优势也需要在特定场景才会有明显的发挥。我们想要组建的平台化，也将会从各个方面去帮助业务与开发人员去正确的选择方案，并且提供一系列完整的能力去保障业务的完好运行。</p><h4 id="平台化整体能力"><a href="#平台化整体能力" class="headerlink" title="平台化整体能力"></a>平台化整体能力</h4><p>大部分时候，我们关心的都是开发阶段的一些问题与解决，开发虽然是跨端方案影响和作用最大的一个环节，但在整个过程中，开发仅占用一部分环节。既然我们希望做一个平台化的能力，那么我们就必须考虑到整个过程的能力。</p><p><img src="/images/2020/hybrid/loop.png" alt=""></p><p>首先我们需要明确的是，我们的目标是希望所有参与人员，都去关注整个业务，而不是自己这一环节，之前也说过这是未来业务开发模式的一种革新方向。我们不希望产品提了需求就不管了，也不希望开发测试仅仅跟踪到上线这个环节。目前难以做到这一点的主要原因是因为整个流程是割裂的，不同环节都隶属于不同的团队，业务分工足够细，且需要大量人员参与。这种问题在越大的团队会越明显，那么需要平台化去整合的需求也会越强烈。</p><p>同时，在整个过程中，人员的因素太过于复杂，需要协调好各个人员与角色，显然不可以仅仅通过沟通与默契来解决，我们必须在整个过程中加入足够多的自动化过程，来推动整个流程的前进，而这自动化的过程，就需要平台化的能力。</p><p>跨端方案有很多选择，其之间有很多的相同点，也有很多的区别，而我们在整个过程中，如果因为这其中的不同，从而分化出不同的开发流程，反而会给所有人带来很大的困惑，增加各自的人力成本。</p><p>所以可以看到，平台化来整合所有能力也是非常重要的。</p><h4 id="业务维度细化"><a href="#业务维度细化" class="headerlink" title="业务维度细化"></a>业务维度细化</h4><p>如果需要实现以上所说的全流程整合，那么每个业务功能则必会细化为一个个微服务，或者说是微应用。每个业务模块也是按照一个应用的流程来管理。</p><p>由于这些应用（业务）所承载的服务其实是比较小的内容，所以人员可以变得灵活调配，与普通开发人员调动不同，调配的不仅仅是“人力”，而是微型“团队”，以此也可以大幅减少沟通成本。</p><p>业务细化肯定会带来大量的微应用，就像淘宝这样的巨应用，存在着无数的微应用。我们需要管理所有的微应用，则不论使用跨端技术，都需要提供一套统一的管理流程。</p><p>在我们拥有这一个完整的流程后，可以来看看每个阶段需要哪些东西。</p><h4 id="技术选型"><a href="#技术选型" class="headerlink" title="技术选型"></a>技术选型</h4><p>在业务方案的选择中，我们可以提供一个简单的筛选条件，但是并不是一个强制的约束。</p><p><img src="/images/2020/hybrid/choose.png" alt=""></p><p>在具体的业务场景中，我们所需要考虑会比这些条件多的多。比如有些强依赖原生能力的业务需求，如果我们使用跨端方案去实施，最终结果可能完全的违背了跨端的优势，从而导致多端开发的成本直线上升，可能短时间内看似美好，但从长远来看，将会陷入维护的泥潭。</p><p>所以这在很多场合是一个比较经验主义的过程，在我们选型的时候，不要先入为主的去决定使用哪种技术，而应该仔细分析业务场景，使用什么技术可以满足需求的同时，尽可能的减少成本。</p><h4 id="框架"><a href="#框架" class="headerlink" title="框架"></a>框架</h4><p>在引入跨端方案的时候，如果我们毫无节制与约束的使用，最终肯定会变得非常糟糕，那么在此基础上必须有一定的规范来组织与约束大家的使用。</p><p>那么下面我们从组织结构、抽象层、沙盒化三个角度进行分析。</p><p><strong>组织结构</strong></p><p><img src="/images/2020/hybrid/hybrid_structure.png" alt=""></p><p>我们的结构大致分为以上几个层次，这里解释一下中间2层代表的是什么。</p><ul><li>基础能力桥接层，是与业务无关的能力，是组建所有应用都需要使用到的能力，可以认为这是一套像POSIX标准接口的定义。所有app内的这一层都是相同的，那么在我们想要移植一个跨端应用到另一个app的时候，就会变的非常简单。事实上我们也的确有一些这样的需求。另一个好处是，统一的标准能够让开发人员更好的进行跨应用间的开发，就像为什么支付宝会故意去抄袭微信小程序的api规范一样。</li><li>业务相关桥接层，每个应用必然是会有各自业务的特殊性，导致一些无法进行标准化与通用的能力，那么我们就需要这一层来做一些脏活了。</li></ul><p><strong>抽象层</strong></p><p>为了满足上述组织结构中的标准接口定义，那么我们必须要引入一个抽象层，来屏蔽各个跨端技术与app之间的不同。</p><p><img src="/images/2020/hybrid/rpc_structure.png" alt=""></p><p>我们的抽象层大致是这么设计的，实现方案这里就不讨论了，后续会增加一篇该方案的文章[Native RPC]。抽象层给使用者与开发者都屏蔽了多端的特性，所表现出来的是完全的一致性。</p><p>我们会有一个专门管理接口定义的平台，从该平台上可以自动生成接口调用代码与demo工程，这样可以大大减少接入、测试、回归等成本。</p><p><strong>沙盒化</strong></p><p>业务细化后我们必须面对的是这些业务对主应用的影响，以及相互之间的影响。这种影响客户端以及平台层面上是无法把控的，所以需要对各个微应用之间进行沙盒化隔离。</p><p>沙盒化后带来的另一个需求则是权限管理，由于目前业务都是面向内部的，所以权限方面要求不是特别严格，这里暂不将权限控制作为重要的一环。</p><p><strong>业务层多平台统一</strong></p><p>上面所涉及的都是跨端与原生之间的一个统一，那么在各个跨端之间的也可以进行一次统一，比如比较有名的有Taro, rax等。</p><p>但这种统一仅仅是为了降低前端开发的学习成本与一些降级策略，强行将这些东西糅合到一起，反而可能会导致开发成本变高，不兼容性与性能下降。在我们有比较固定的人力做单平台业务开发的时候，完全没有必要再引入一层复杂的适配，带来额外的复杂度。</p><p>这种方案的适用场景比较局限，适合大量且业务复杂度不高的业务，可以快速搭建与上线，适合多平台3方应用的接入，比如各个平台的小程序。</p><p>所以这里并不考虑把这种方案作为我们其中一个目标，就像阿里也并没有吧rax作为内部的主流技术。</p><h4 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h4><p>由于我们的跨端能力大部分是使用在业务场景中，所以大部分场景做自动化测试的成本都非常高，所以测试环节基本将会是黑盒测试。而得益于部分跨端技术的动态性，问题的修复成本与原生开发相比会小很多。</p><h4 id="线上监控"><a href="#线上监控" class="headerlink" title="线上监控"></a>线上监控</h4><p>我们选择跨端应用的时候，会比较担心一些跨端应用的负面效果，比如性能，错误率等，所以必须建立比较完善的线上监控系统。</p><p>监控系统应该具备我们普遍会关心的一些数据，比如加载时长、错误率、性能指标等，同时提供横向对比的能力，提供业务方一个指导性的意见，同时增强线上预警的能力。</p><p>现在这么多不同的跨端技术方案，面临的问题是监控系统的分散，从而无法形成有效的闭环，所以建立一个统一的完整的系统，串联整个迭代链路。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>随着跨端技术的发展与应用，也给我们的很多基础设施提供了巨大的挑战，如何将跨端的优势真正的发挥出来，来提高我们的效率与灵活性，这是我们必须面对与解决的问题，而在这之中，如何完善与整合多端能力则是重中之重。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;跨端能力一直是近些年来的大热门，我们对跨端能力也一直抱有非常高的期望，我从2016年开始就一直关注着跨端能力的发展，经历了其中各个发展阶段，而目前我们团队也将建设整个跨端平台化能力，这里就来讨论下跨端的一些东西，以及我们的平台化会如何建设整个生态。&lt;/p&gt;
    
    </summary>
    
      <category term="文章" scheme="http://djs66256.github.io/categories/%E6%96%87%E7%AB%A0/"/>
    
    
      <category term="跨端" scheme="http://djs66256.github.io/tags/%E8%B7%A8%E7%AB%AF/"/>
    
      <category term="h5" scheme="http://djs66256.github.io/tags/h5/"/>
    
      <category term="小程序" scheme="http://djs66256.github.io/tags/%E5%B0%8F%E7%A8%8B%E5%BA%8F/"/>
    
      <category term="react native" scheme="http://djs66256.github.io/tags/react-native/"/>
    
      <category term="flutter" scheme="http://djs66256.github.io/tags/flutter/"/>
    
  </entry>
  
  <entry>
    <title>谈谈苹果禁用HTML5应用</title>
    <link href="http://djs66256.github.io/2020/02/08/2020-02-08-%E8%B0%88%E8%B0%88%E8%8B%B9%E6%9E%9C%E7%A6%81%E7%94%A8HTML5%E5%BA%94%E7%94%A8/"/>
    <id>http://djs66256.github.io/2020/02/08/2020-02-08-谈谈苹果禁用HTML5应用/</id>
    <published>2020-02-08T04:06:53.000Z</published>
    <updated>2020-10-10T14:21:08.994Z</updated>
    
    <content type="html"><![CDATA[<p>苹果在2019年更新了审核条款，其中可能对我们影响最大的就是HTML技术的应用了。</p><p><a href="https://developer.apple.com/news/?id=09062019b">App Updates for HTML5 Apps</a></p><a id="more"></a><p>总所周知，苹果一向以来都不喜欢动态特性，从最早的其他语言限制，以及到jspatch的禁用。这里涉及到的就有两个问题了。</p><ul><li>一是苹果自身所说的保证其生态以及用户体验一致性，这样的确能够淘汰一大批低质量和壳应用，清洗了整个生态环境。</li><li>二则是苹果自身的利益。苹果不予许一些应用绕过他的审核，来做一些事情。</li></ul><p>国内开发者对于苹果对动态化的态度非常的不满，目前国内对于动态性的热情依然还是非常高涨，这得益于国内软件开发现状。但在我看来，这事情有好有坏。</p><ul><li>对于我们客户端开发来说，这个饭碗不会那么容易丢了。</li><li>目前市面上的应用参差不齐，还有很多违法的，这的确能够减少很多这样的现象。</li><li>对于大部分应用来说，有没有动态化其实并没有太大的影响。</li></ul><p>反对的人则主要觉得：</p><ul><li>无法热修复（这个人觉得真不能怪苹果，自己的质量不过关，给应用埋下一个后门真的不太好，特别是一些涉及用户隐私和金钱的）</li><li>无法实现3端统一，无法一套代码多端使用，减少人力成本。（个人认为想要做3端统一，则必须要牺牲一些平台特性，反而可能造成不符合当前平台的行为。而所谓的write once，目前证明大部分都是夸张的。阿里已经抛弃weex，Facebook自己也没有用RN。至于较少人力成本，这个还真不一定，毕竟想要找到能够了解多端特性的人才还是非常困难的。）</li><li>快速响应，快速上线（由于苹果的审核机制，这个是国内最大的痛点。这可能与国内的环境有关，没有长远、详细的计划，经常改变策略）</li></ul><p>那么我们真的无法使用HTML5技术了吗？下面我们来解读一下苹果条款。</p><p>App Store Review Guidelines:</p><pre><code>4.7 HTML5 Games, Bots, etc.Apps may contain or run code that is not embedded in the binary (e.g. HTML5-based games, bots, etc.), as long as code distribution isn’t the main purpose of the app, the code is not offered in a store or store-like interface, and provided that the software (1) is free or purchased using in-app purchase; (2) only uses capabilities available in a standard WebKit view (e.g. it must open and run natively in Safari without modifications or additional software); your app must use WebKit and JavaScript Core to run third-party software and should not attempt to extend or expose native platform APIs to third-party software; (3) is offered by developers that have joined the Apple Developer Program and signed the Apple Developer Program License Agreement; (4) does not provide access to real money gaming, lotteries, or charitable donations; (5) adheres to the terms of these App Review Guidelines (e.g. does not include objectionable content); and (6) does not support digital commerce. Upon request, you must provide an index of software and metadata available in your app. It must include Apple Developer Program Team IDs for the providers of the software along with a URL which App Review can use to confirm that the software complies with the requirements above.</code></pre><p>苹果并没有禁止HTML5，而是指出要合理的利用HTML技术。从苹果最终还是上了PWA这件事上来说，苹果并没有阻止HTML5这样的技术。苹果只是希望控制应用的权限，不要做一些挂羊头买狗肉的事情。那么我们来看看目前几种比较热门的技术吧。</p><h2 id="RN、WEEX"><a href="#RN、WEEX" class="headerlink" title="RN、WEEX"></a>RN、WEEX</h2><p>苹果已经警告过这两个方案了，后续虽然苹果并没有对这两个技术下手，但个人觉得从这次苹果的态度来说，还是非常反对这种技术的，因为这个技术太容易做到挂羊头卖狗肉的事情了。虽然这个和热修复来说，只是公开了有限的能力，但其本质上来说和HTML5应用还是一样的。</p><p>那么我们一点都不能用了吗？以目前苹果的态度来说，也没有完全禁止，只是禁止使用这些技术做核心功能，而一些次要功能应该暂时还是安全的。如果是纯RN应用，那么个人建议还是尽早想想退一步的方案了。优化好的网页也不比原生的差。</p><h2 id="小程序"><a href="#小程序" class="headerlink" title="小程序"></a>小程序</h2><p>小程序这个其实已经动到了苹果的利益了，如果你不是大厂，个人不太建议去试探苹果。</p><p>而这次更新，苹果更是明确说了HTML5应用更适合在浏览器上使用，而不是一个独立的app。所以个人并不太看好这个方案。</p><h2 id="flutter"><a href="#flutter" class="headerlink" title="flutter"></a>flutter</h2><p>以目前来看，最符合苹果的方案只有flutter了。</p><p>因为在iOS系统中，flutter是以二进制打包进去的，并没有违反苹果的条款。</p><p>听说咸鱼团队已经开始做flutter的字节码虚拟机了。恩。。。这个个人还是持保留态度，也建议小厂不要没事和苹果干。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>最后，我们需不需要动态特性依然还是需要看自身业务需求，大量展示型的，没有复杂交互的页面使用这类技术的确拥有更多优势，但是动态性的投入产出比可能并不一定能有那些技术宣传的那么好。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;苹果在2019年更新了审核条款，其中可能对我们影响最大的就是HTML技术的应用了。&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://developer.apple.com/news/?id=09062019b&quot;&gt;App Updates for HTML5 Apps&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="文章" scheme="http://djs66256.github.io/categories/%E6%96%87%E7%AB%A0/"/>
    
    
  </entry>
  
  <entry>
    <title>开发标准化杂谈</title>
    <link href="http://djs66256.github.io/2020/01/31/2020-01-30-%E5%BC%80%E5%8F%91%E6%A0%87%E5%87%86%E5%8C%96%E6%9D%82%E8%B0%88/"/>
    <id>http://djs66256.github.io/2020/01/31/2020-01-30-开发标准化杂谈/</id>
    <published>2020-01-31T14:12:41.000Z</published>
    <updated>2020-10-10T14:21:05.875Z</updated>
    
    <content type="html"><![CDATA[<p>在iOS开发这一块，国内的标准化做的都不太好，和国外的一些项目相比，差距非常大，所以这里就来谈谈标准化的重要性吧。</p><a id="more"></a><h2 id="标准化"><a href="#标准化" class="headerlink" title="标准化"></a>标准化</h2><p>需要标准化的场景非常多，比如：</p><ul><li>文档</li><li>代码规范</li><li>接口</li><li>方案设计</li></ul><p>就拿文档来说，目前应该几乎没有对注释的覆盖率有要求的，特别是客户端这种以写业务为主的场景，没有注释那是自然的。</p><h2 id="Why"><a href="#Why" class="headerlink" title="Why"></a>Why</h2><p>为什么个人非常看重标准化这个问题。</p><p>因为在这些年的工作经验中，发现有很大一部分bug是由于标准化做的不足而引起的，而这些问题往往是那种非常简单的错误。</p><p>另外一个是在做一些重构或者整合的时候，没有做标准化的业务几乎很难去重构，需要花费大量精力去“适配”这种不标准。</p><p>比如：</p><p>用户id的定义，可能是<code>id</code>，<code>userId</code>，<code>resourceId</code>，这些不同的名字往往会让人非常困扰。更为困扰的是拥有相同名字，却有不同的含义的内容。</p><p>这些都非常影响软件的质量。</p><h2 id="How"><a href="#How" class="headerlink" title="How"></a>How</h2><p>其实这类问题并不是我们才有的问题，这些问题基本上都有现成的比较权威的方案。</p><p>比如代码规范，Google整理的各种语言的规范，阿里的java白皮书这种都可以参考。</p><p>比如接口中分页的设计，以前总要在<code>pageSize</code>，<code>lastId</code>，<code>lastTime</code>等众多方案中挑选一种，而没有采用一些更优秀的设计（可以参考GraphQL的分页方案）。</p><p>标准化的解决方案，大部分问题我们都可以参考现有的解决方案。</p><h2 id="困难"><a href="#困难" class="headerlink" title="困难"></a>困难</h2><p>虽然标准化这个事情看起来非常简单，但是要实施起来却非常困难。</p><p>标准化不仅仅是单端的问题，甚至可能从需求方开始就要改善。这样所涉及的人过于庞大，更不要说像阿里这种大厂了。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在iOS开发这一块，国内的标准化做的都不太好，和国外的一些项目相比，差距非常大，所以这里就来谈谈标准化的重要性吧。&lt;/p&gt;
    
    </summary>
    
      <category term="文章" scheme="http://djs66256.github.io/categories/%E6%96%87%E7%AB%A0/"/>
    
    
  </entry>
  
  <entry>
    <title>页面组件化设计汇总</title>
    <link href="http://djs66256.github.io/2019/11/30/2019-11-30-%E9%A1%B5%E9%9D%A2%E7%BB%84%E4%BB%B6%E5%8C%96%E8%AE%BE%E8%AE%A1%E6%B1%87%E6%80%BB/"/>
    <id>http://djs66256.github.io/2019/11/30/2019-11-30-页面组件化设计汇总/</id>
    <published>2019-11-29T17:58:55.000Z</published>
    <updated>2020-02-04T07:41:58.000Z</updated>
    
    <content type="html"><![CDATA[<p>现在的应用都是越来越大，越来越复杂，更加需要模块的精细化切分，以及支持多人共同合作开发。</p><p>目前大量应用单纯一个页面的业务量级已经非常庞大，这里就来再次从页面级别讨论一下页面组件化。</p><a id="more"></a><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>我们可以将任何一个页面都视作一个列表，每类元素都可以看做列表中的一个cell。之前，在美学新项目启动的时候，按照页面组件化的思路做了一个组件化拆分方案（DDComponent），在当时的业务场景来说，已经基本足够，但是在不停的使用过程中，也逐渐发现了一些问题，比如</p><ol><li>数据源不匹配。这是一个非常常见的问题，异步、没有正确reloadData都是引起这类问题的原因，目前我还没有看见过有哪个开源方案能够彻底解决这个问题的。</li><li>reloadData效率问题。当一个列表足够长的时候，每次reloadData都将会非常消耗性能。</li><li>size计算。在布局和构建列表的时候，最让人麻烦的就是size计算，这会让我们的布局工作量加倍，而系统提供的方法实际效果并不好，虽然有FD的自动计算方案，但依然存在很多问题。</li><li>通信问题。组件间应该是相互独立的，如何解决相互间的通信问题也是一个重点。</li></ol><p>这次重新设计的这套数据驱动系统主要考虑了如下几个目标：</p><ul><li>开发效率。在目前的业务开发过程中，效率永远是第一的，决定业务快速迭代的基础。</li><li>并行开发。在这个多人合作的场景中，如何减少沟通成本也是非常关键的，而从架构层面解决这个问题是最有效的。</li><li>复用。对于一些信息展示类为主的应用，到处存在着复用问题，而以前的复用层面仅仅是view、model，而无法把这个模块的MVC进行复用。</li><li>标准化。提供一套完全标准化的页面组件方案。</li><li>扩展性。能够应对平时的大部分场景以及不同类型布局系统。</li><li>执行效率。从架构的层面考虑性能问题，并为性能优化提供一系列方案，并且可以进行逐步优化。</li></ul><h2 id="数据源不匹配问题"><a href="#数据源不匹配问题" class="headerlink" title="数据源不匹配问题"></a>数据源不匹配问题</h2><p>数据源问题是我们开发列表中遇到的最大的一个问题，也是非常难以解决的问题。</p><h4 id="场景"><a href="#场景" class="headerlink" title="场景"></a>场景</h4><p>当一个列表比较简单的时候，我们很少会有这样的问题。但是随着业务发展，列表内容越来越复杂，那么就非常容易出现这类问题，主要可能表现为以下几个场景：</p><ul><li>cell 为空</li><li>数组越界</li><li>非法调用（类型不匹配）</li></ul><h4 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h4><p>在我们处理数据的时候，如果在if-else的条件里面遗漏了某种数据，可能会导致该数据返回空cell。这种情况在迭代和重构的时候，由于对现有业务没有透彻的理解，就很容易发生。</p><p>而数组越界、非法调用，往往是异步和时序问题。在我们更新数据源之后，如果没有及时、同步、正确的更新列表，就会发生这样的问题。这类问题一般是偶现的问题，难以排查，也难以修改。而且如果该页面是拆分后的结构，那么每个独立模块内可能是正常的，但是多个模块间相互影响可能就是有问题的。</p><h4 id="如何解决这个问题？"><a href="#如何解决这个问题？" class="headerlink" title="如何解决这个问题？"></a>如何解决这个问题？</h4><p>我们很难去保证每个人的开发素质，也很难去保证多人并行开发过程中的沟通和一致性。所以我们需要从架构层面去解决这个问题。</p><p>解决这个问题的关键在于数据源，我们列表中所展示的数据，和我们正在处理的数据并不能保证一致性，那么最好的解决办法就是保存两份数据源，一份是开发自己维护的，一份是用于页面展示的。</p><p>目前列表所有接口返回的都是位置信息，而不是我们展示的数据，我们需要得到数据就需要从数组中的位置去取，这也无法保证展示的数据源index和真实的数据源index的一致性。</p><p>那么这里需要解决的问题总结为：</p><ul><li>展示数据源和当前数据源的分离</li><li>抹除位置信息，直接通过数据来表示</li></ul><h4 id="数据源分离实现"><a href="#数据源分离实现" class="headerlink" title="数据源分离实现"></a>数据源分离实现</h4><p>其实做数据源分离的思路很简单，系统的很多设计思路也是类似的。在我们提交数据源变化的时候，我们直接创建一个只读的数据源快照，用于展示使用。当我们再次刷新的时候，同样需要生成一个新的快照用于刷新列表。大致的流程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">--&gt; update data ---&gt; original data source --&gt; update data --&gt; original data source --&gt; ...</span><br><span class="line">                          |                                         |</span><br><span class="line">                       reload                                     reload</span><br><span class="line">                          |                                         |</span><br><span class="line">                          v                                         v</span><br><span class="line">                      snapshot ---&gt; display data source           snapshot ---&gt; display data source</span><br><span class="line">                                        |                                           |</span><br><span class="line">                                        v                                           v</span><br><span class="line">                                    UICollectionView                            UICollecitonView</span><br></pre></td></tr></table></figure><h4 id="绑定数据，而不是index"><a href="#绑定数据，而不是index" class="headerlink" title="绑定数据，而不是index"></a>绑定数据，而不是index</h4><p>我们开发过程中，一直以来都是以index为核心进行布局数据，而没有从数据进行布局列表，这是导致这类问题最根本的思路上的问题，如果我们所有接口都是直接得到相应的数据，我们不需要操作index，那么我们就绝对不会出现越界等问题了。</p><p>那么我们可以把接口设计成这样：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">NSArray</span> *)viewModelsForComponent:(NELayoutEntityComponent *)component;</span><br><span class="line">- (Class)component:(NELayoutEntityComponent *)component cellClassForViewModel:(<span class="keyword">id</span>)viewModel;</span><br><span class="line">- (<span class="built_in">CGSize</span>)component:(NELayoutEntityComponent *)component sizeForViewModel:(<span class="keyword">id</span>)viewModel;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)reloadViewModel:(<span class="keyword">id</span>)viewModel;</span><br><span class="line">- (<span class="keyword">void</span>)deselectViewModel:(Type)viewModel animated:(<span class="built_in">BOOL</span>)animated;</span><br></pre></td></tr></table></figure><p>所有行为都必须通过数据来执行，让真实的数据作为模块的核心。那么这里数据就必须具备唯一性这个条件。</p><p>可能你要说，并不是每一个模块都是有这么明确的数据类型来表示，那么这时候可以让该组件自己作为数据来表示。因为这里每一个组件的数据可以认为是每个组件的全局唯一id，只要保证其唯一性即可。</p><h2 id="reload-效率问题"><a href="#reload-效率问题" class="headerlink" title="reload 效率问题"></a>reload 效率问题</h2><p>列表刷新的效率问题主要集中在两个地方：</p><ul><li>刷新导致重新计算布局</li><li>刷新导致大量更新视图数据</li></ul><p>要解决这两个问题，可以增加size缓存、减少计算量以及视图更新的频次。为了减少计算量，这里引入增量系统。</p><p>对应于上面所述，我们将数据作为列表的核心，所以我们可以对数据源进行增量计算，对比得到变化的部分进行更新。而这也是很多系统所采用的方式，比如IGList。</p><p>这里为了一些异步能力考虑，并没有采用IGList这些成熟的开源框架，而是重新设计了更新流程。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">main thread:  new data source -+            +-&gt; force update -&gt; reload collection view</span><br><span class="line">                               v            ^</span><br><span class="line">thread1:                       +-&gt; size ----+</span><br><span class="line">                               |            |</span><br><span class="line">thread2:                       +-&gt; differ --+</span><br><span class="line">                                            v</span><br><span class="line">                                          cancel ---&gt; back to &#39;new data source&#39; state</span><br></pre></td></tr></table></figure><p>在触发更新并且重新生成快照之后，会进入两个异步线程，分别进行计算数据源增量和异步size计算（在需要极限优化性能的时候可以采用，在下个章节中讨论）。计算完增量后回到主线程，然后进行列表的更新。在这个过程中间如果触发了新reload，那么直接取消这一次的数据刷新操作，忽略该次结果。</p><p>这样，我们尽可能的减少列表的刷新和数据源的频繁计算，同时也在异步进行数据源增量计算（目前来看这个时间占总刷新时间的比例并不高）。</p><h4 id="效率追踪"><a href="#效率追踪" class="headerlink" title="效率追踪"></a>效率追踪</h4><p>在以前我们写一个大页面的时候，如果出现性能问题，我们需要去查找问题的原因往往靠经验，也就是猜测，然后一个个排查。这样大大降低了我们排查问题的效率和准确性。</p><p>而且以前的问题都是后知的，也就是发生严重影响的时候，才会反过来修改。</p><p>而我们进行组件拆分后，每个类型的组件都是独立的，我们可以在任意我们所需要的地方，比如：</p><ul><li>size计算</li><li>diff耗时</li><li>cell渲染</li></ul><p>进行插桩、统计与分析。那么我们可以在投入生产之前就能得到一手数据，甚至在这些地方加入限制与报警，从而进行精确的优化。</p><h4 id="数据源differ协议"><a href="#数据源differ协议" class="headerlink" title="数据源differ协议"></a>数据源differ协议</h4><p>数据需要能够比对增量，就必须满足一定的规则，这里采用的和IGList一样的协议。但是这样会带来一些问题：</p><ul><li>对每一个数据需要重写diffable协议，增加了开发工作量。</li><li>每一个数据都需要做copy，才能进行对比前后变化，这对工作量和性能都会有一定的影响。</li><li>并不能保证团队所有人都能够正确的理解和设计diffable内容，不良的设计可能直接导致增量系统的未知行为（比如不能保证数据的唯一性）。</li><li>在迭代过程中，数据diffable并不是一成不变的，可能会根据需求来改变其等价的条件。</li><li>在同一个应用中，可能在不同场景中的等价条件是互斥的，这样我们就无法复用该数据模型。</li></ul><p>基于这些问题，我决定完全放弃基于diffable协议去做更新操作，而是基于数据去强制刷新。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)reloadDataWithForce:(<span class="built_in">BOOL</span>)force animated:(<span class="built_in">BOOL</span>)animated;</span><br><span class="line">- (<span class="keyword">void</span>)reloadViewModel:(<span class="keyword">id</span>)viewModel withForce:(<span class="built_in">BOOL</span>)force animated:(<span class="built_in">BOOL</span>)animated;</span><br></pre></td></tr></table></figure><p>在很多场景下，我们仅需要更新某个cell的数据，就可以直接采用该能力，而默认我们则采用数据指针这一个唯一值作为参照条件。这样可以让我们减少需要小心维护数据协议的大量工作量。</p><h2 id="size问题"><a href="#size问题" class="headerlink" title="size问题"></a>size问题</h2><p>size计算一直是iOS开发过程中一个非常机械且重复的工作，而且特别容易出bug。</p><ul><li>如果一个cell的布局非常复杂，那么这个计算过程也会显得难以阅读。</li><li>如果经过很长时间的迭代，也会让计算的逻辑变得复杂无比。</li><li>如果cell中有多行文字，那么计算文字既耗时，又会让逻辑变得混乱，同时一旦修改一个字体等属性，很容易遗漏了size计算中的属性。</li><li>如果cell中的元素比较动态，会根据不同数据做不同展示。</li><li>size计算如果由每个人去写，会产生各种各样的写法，甚至隐含了很多的问题（目前为止见过太多这样的问题了）。</li></ul><h4 id="自动计算"><a href="#自动计算" class="headerlink" title="自动计算"></a>自动计算</h4><p>其实cell最终展示的时候，我们是可以确认size的，比如安卓的listView，以及tableView的自动适应size。</p><p>既然我们可以根据cell的数据排版可以知道size，那么我们其实是可以自动计算出size的。这样不仅减少了一定的工作量，而且也避免了很多bug。</p><p>到这里，肯定很多人跳出来说，你这样做性能肯定不好。当然这么做性能可能会变差，但是以目前设备性能和未来的增长速度，大部分场景下性能是过剩的，那么我们为什么不去好好利用这一优势。就像有人说的，未来移动端肯定会被前端所统治一样。</p><p>要做到可以自动计算size，那么布局系统就不能使用手写frame的方式了。目前苹果提供给我们的AutoLayout布局就能很好的完成这个任务。</p><p>另外第三方的flexbox也是非常好的一种布局方式，关于flexbox布局之前我也介绍过，但是官方的YogaKit并不好用，所以我重新设计了一套与JSX、SwiftUI类似思想的方案，同时做了一些优化。这个之后可以专门介绍一下。</p><h4 id="缓存"><a href="#缓存" class="headerlink" title="缓存"></a>缓存</h4><p>size计算另一个非常重要的点就是缓存。我们的列表往往是增量加载的，所以每次的数据和展示并不会发生变化，所以也就没有必要每次都计算一次size。</p><p>目前有一些第三方库可以做到这一点，但是很多都是基于indexPath的，而且需要自己手动进行缓存与查找。而在这里，我们的架构有着天然的优势，那就是我们是完全基于数据的，所以这里可以做到完全自动的缓存能力。只需要开启该能力即可：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">self</span>.cacheEnable = <span class="literal">YES</span>;</span><br></pre></td></tr></table></figure><p>当然我们也会有手动清除缓存的接口和能力。</p><h4 id="size性能优化"><a href="#size性能优化" class="headerlink" title="size性能优化"></a>size性能优化</h4><p>当然，我们也不排除一些页面存在性能问题，比如一个页面加载了非常大量的数据，或者页面布局过于复杂，或者数据解析比较耗时等等，都可能导致我们的自动计算耗时增加。</p><p>一种方式是对解析完的数据和布局进行缓存，这样能够非常好的解决刷新与复用的问题。</p><p>如果随着业务迭代，我们发现了性能出现瓶颈，那么我们可以精确的对一类数据进行优化。如下表所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">          自动计算  自动计算+缓存  手动计算  手动计算+缓存  异步计算  异步计算+缓存  </span><br><span class="line">开发效率      &gt;          &gt;         &gt;           &gt;          &gt;         &gt;</span><br><span class="line">执行效率      &lt;          &lt;         &lt;           &lt;          &lt;         &lt;</span><br></pre></td></tr></table></figure><p>但我们一般不推荐一开始就考虑性能问题，我们还是需要以质量和开发效率为主。</p><h4 id="性能优化补充"><a href="#性能优化补充" class="headerlink" title="性能优化补充"></a>性能优化补充</h4><p>一般我们反对一开始就考虑性能优化，因为可能这里根本不会存在性能问题，没有必要浪费大量的时间在这，而且会把简单的东西变复杂了。</p><p>但是从架构上，我们还是需要考虑到需要优化的这种场景与需求，在架构层面我们就需要能够支持一步步的优化与数据验证，这样才能够看到量化的结果和形成闭环。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">开发 --&gt; 迭代 --&gt; 性能报告 ------&gt;  迭代  --&gt; 性能报告</span><br><span class="line">                    \             |</span><br><span class="line">                    分析问题 --&gt; 优化性能</span><br></pre></td></tr></table></figure><h2 id="通信问题"><a href="#通信问题" class="headerlink" title="通信问题"></a>通信问题</h2><p>一个页面是有关联性的，当我们把一个页面拆分为多个组件的时候，就必然存在组件间通信的问题。</p><p>我这里的解决方案比较简单，做一个局部通知系统的机制，使用消息转发，只要组件实现了协议A，那么该组件就会自动接收到协议A的消息，从而达到了通信的目的。而这个通知系统只作用于当前vc和当前组件树中的组件，不会逃逸到其他地方。</p><p>这里为什么要使用通知这种方案呢？</p><ul><li>消息传递的层次性。我们要通过delegate从组件A上传递消息到组件B上，那么整个链路可能是：<code>组件A</code>-&gt;<code>section A</code>-&gt;<code>viewController</code>-&gt;<code>section B</code>-&gt;<code>组件B</code>，整个链路非常长，而且难以管理。</li><li>消息并不一定是一对一的，存在一对多的情况。比如viewController的appearence事件，可能任意组件都需要知道这个事件。</li></ul><h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul><li>开发效率高</li><li>灵活且适用性高</li><li>解决了链路长的问题</li><li>在不同组件组合的场景下，具有更高的兼容性与解耦性</li><li>避免了消息逃逸到其他页面</li></ul><h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><ul><li>和常见的回调、通知机制不同，需要一定的了解学习</li><li>需要在组件树中的组件才会收到消息，会在某些时候产生一定的误解</li></ul><h4 id="其他方案"><a href="#其他方案" class="headerlink" title="其他方案"></a>其他方案</h4><p>这里并不限制和约束其他的通信方案，比如delegate，notification等。而该方案从总体来说，解耦的优势还是更为突出。</p><h2 id="曝光问题"><a href="#曝光问题" class="headerlink" title="曝光问题"></a>曝光问题</h2><p>对于cell的display事件来说，在某些情况下可能是不准确的：</p><ul><li>reloadData触发复用的时候</li><li>组件移除组件树的时候，或者组件发生替换的时候</li></ul><p>这和CollectionView与拆分组件的机制有关，所以这里设计了一套全新的曝光方案，使用数据，而不是cell作为曝光源，来做到更为精确的曝光。</p><p>下面简单的说明下思路。</p><h4 id="曝光检查"><a href="#曝光检查" class="headerlink" title="曝光检查"></a>曝光检查</h4><p>此次采用的是主动检测方式，在列表发生滚动的时候，会触发一次曝光检查。检查的目的是为了找到：</p><ul><li>哪些数据由<code>不可见</code>-&gt;<code>可见</code></li><li>哪些数据由<code>可见</code>-&gt;<code>不可见</code></li></ul><p>这个可以利用CollectionView的visibleIndexPaths来查找，如果需要更为精确的数据，还可以自己再根据inset再校验一遍。</p><h4 id="检查时间点"><a href="#检查时间点" class="headerlink" title="检查时间点"></a>检查时间点</h4><p>那么那些场景可能会需要检查？</p><ul><li>列表滚动</li><li>reloadData</li><li>viewControllerAppearance</li></ul><p>以及其他根据业务需求可以增加的检查。</p><h4 id="效率"><a href="#效率" class="headerlink" title="效率"></a>效率</h4><p>目前还未发现这种方案的效率问题，一般来说可见的组件也不会有很多，不会产生什么验证的效率问题。</p><h2 id="并行开发与组件原子性"><a href="#并行开发与组件原子性" class="headerlink" title="并行开发与组件原子性"></a>并行开发与组件原子性</h2><p>每个组件我们希望承担的功能尽可能的单一，只负责一类数据与一类视觉展示，这样我们就能保证组件的最小粒度，以及其原子性。</p><p>这样，我们就可以很容易的将一个大型的复杂页面进行拆分，分配给多人开发，而不会产生很大的冲突和依赖。</p><p>同时这也非常有利于我们快速的开发一个功能，以适应市场变化。</p><h2 id="复用性"><a href="#复用性" class="headerlink" title="复用性"></a>复用性</h2><p>虽然组件化的一个很重要的特点是复用性，但是在实际情况下，可复用的组件依然占少数，而想要设计出一个能够比较好复用的组件是相当困难的，对人员的能力要求，以及对组件化的理解都很高。</p><p>所以这里我不想特意去要求高复用性，而更希望将组件做的更小，更简单，粒度更细。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>这是从个人经验上，从最早的DDComponent拆分思想，以及后来出现的IGList的diff思路，经过自身的大量业务实践，最终总结所得，同时兼顾简单、易学的一套方案。</p><p>其实这里可以做的更激进一些，以满足一些比如动态化、DSL的能力，但考虑到受众应该是所有层次的开发，所以不便做的太过复杂。</p><p>由于项目原因，暂不便公开源码。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;现在的应用都是越来越大，越来越复杂，更加需要模块的精细化切分，以及支持多人共同合作开发。&lt;/p&gt;
&lt;p&gt;目前大量应用单纯一个页面的业务量级已经非常庞大，这里就来再次从页面级别讨论一下页面组件化。&lt;/p&gt;
    
    </summary>
    
    
      <category term="组件化" scheme="http://djs66256.github.io/tags/%E7%BB%84%E4%BB%B6%E5%8C%96/"/>
    
      <category term="UICollectionView" scheme="http://djs66256.github.io/tags/UICollectionView/"/>
    
  </entry>
  
  <entry>
    <title>从响应链收集手动埋点</title>
    <link href="http://djs66256.github.io/2019/06/01/2019-06-01-%E4%BB%8E%E5%93%8D%E5%BA%94%E9%93%BE%E6%94%B6%E9%9B%86%E6%89%8B%E5%8A%A8%E5%9F%8B%E7%82%B9/"/>
    <id>http://djs66256.github.io/2019/06/01/2019-06-01-从响应链收集手动埋点/</id>
    <published>2019-06-01T09:49:06.000Z</published>
    <updated>2019-11-30T19:47:51.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>手动埋点一直以来都是一个比较麻烦的行为，埋点不属于业务逻辑，而我们必须插入业务逻辑中，有可能就导致了我们不得不修改设计以满足埋点的需求。</p><p>同时数据方很多时候会要求带上额外的数据，而这些额外的数据甚至和当前模块毫无关系，也就是破坏了我们的独立性和解耦原则。比如在事件点击中加入当前页面的pageId，当前cell的index等等。</p><a id="more"></a><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>目前我们手动埋点的埋点信息可能包含的内容比较多，但也可以进行归类。</p><p>不同类型的埋点信息都是在不同的层上收集的，比如我们需要pageId，那么这个必然是在当前VC上才有，如果我们要收集上个页面的pageId，那么只有navigationController上会有该信息，而如果我们需要收集cell的位置信息，那么我们需要在dataSource上进行收集。</p><p>这给我们的埋点收集一个思路，我们手动埋点的时候不要马上收集完所有数据上报，而是收集当前层存在的信息，并把这个埋点任务抛向上层。每一层都会往埋点任务里面写入自己层的信息，直到结束，这样我们就可以收集一个非常完整的埋点信息。</p><p>当然这样做的缺点是埋点里面的信息比较多，比较冗余，但作为埋点信息来说，信息冗余并不是太大的问题。</p><p>要建立这样一个结构，如果是一些新业务或者组织架构设计较好的业务来说，会比较简单，但是对于一个已经成型的，有很多复杂的业务却比较麻烦。这里需要去寻找一种方法，使改动量尽可能的小，并且组织灵活。</p><h2 id="数据流"><a href="#数据流" class="headerlink" title="数据流"></a>数据流</h2><h4 id="数据方向"><a href="#数据方向" class="headerlink" title="数据方向"></a>数据方向</h4><p>首先我们来看看一般应用的页面结构。</p><p><img src="/images/2019/trace/view_structure.png" alt=""></p><pre><code>绿色：Application蓝色：View橘色：ViewController</code></pre><p>我们更新页面的数据方向是这样的：</p><p><img src="/images/2019/trace/data_flow.png" alt=""></p><p>但是我们埋点触发，需要收集的数据却是这样的：</p><p><img src="/images/2019/trace/trace_flow.png" alt=""></p><p>我们更新数据和收集数据的方向是完全相反的，这也就是导致我们手动埋点总是会感觉很别扭的原因。</p><p>而我们一般做埋点的方式，是将这个事件通过回调等方式一层层传递到ViewController上，然后进行统一埋点。</p><p>但是对于一个复杂的VC，我们往往会继续拆分，成为多个独立组件的组合（可以看我之前的DDComponent），这样不仅能避免VC的无限膨胀，也能解耦业务。但是遇到埋点需求时，我们却不得不打破这个组织方式，因为有很多信息必须要在VC上才能获取到。</p><h4 id="数据流-1"><a href="#数据流-1" class="headerlink" title="数据流"></a>数据流</h4><p>我们在上面了解到的埋点现状，可以抽象为一条数据流，在这个数据流上有多个节点，不同的节点可以收集必要的埋点信息。</p><p><img src="/images/2019/trace/trace_node.png" alt=""></p><p>而每一个节点，可以抽象为一个Stream。</p><p>这里我们可以看到每一个节点同时也是响应链的一部分，那么我们是不是就可以依靠响应链来连接整个数据流呢？这样我们就不需要大量改动代码，从而达到建立这套系统了。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">UIResponder</span> (<span class="title">TraceStream</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">readonly</span>, <span class="keyword">null_resettable</span>) TraceStream *trace;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>这里我们还需要为这条数据流创建一个终点，也就是最终输出端，可以选择Application上的节点为输出节点。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">UIApplication</span> (<span class="title">TracePipeline</span>)</span></span><br><span class="line"></span><br><span class="line">- (TraceStream *)trace &#123;</span><br><span class="line">    <span class="built_in">NSAssert</span>([<span class="built_in">NSThread</span> isMainThread], <span class="string">@&quot;Must call on main thread.&quot;</span>);</span><br><span class="line">    TraceStream *stream = objc_getAssociatedObject(<span class="keyword">self</span>, <span class="keyword">@selector</span>(trace));</span><br><span class="line">    <span class="keyword">if</span> (stream == <span class="literal">nil</span>) &#123;</span><br><span class="line">        TraceStream *stream = [[LoggerTraceStream alloc] init];</span><br><span class="line">        objc_setAssociatedObject(<span class="keyword">self</span>, <span class="keyword">@selector</span>(nm_trace), stream, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">        <span class="keyword">return</span> stream;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> stream;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><h4 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h4><p>这里我们举一个列表中点赞埋点的列子：</p><p>在cell中的某个view中，触发了点赞：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)onPriase:(<span class="keyword">id</span>)sender &#123;</span><br><span class="line">    [<span class="keyword">self</span>.trace addTraceInfoWithBuilder:^(TraceInfo * _Nonnull traceInfo) &#123;</span><br><span class="line">        traceInfo</span><br><span class="line">        .setAction(<span class="string">@&quot;praise&quot;</span>)</span><br><span class="line">        .setId(<span class="keyword">self</span>.video.videoId)</span><br><span class="line">        .setType(<span class="keyword">self</span>.praised ? <span class="string">@&quot;1&quot;</span> : <span class="string">@&quot;0&quot;</span>);</span><br><span class="line">    &#125;];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是我们还需要知道这个cell是位于列表中的位置，那么我们可以cell中加入该信息：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="built_in">UITableViewCell</span> *)tableView:(<span class="built_in">UITableView</span> *)tableView cellForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123;</span><br><span class="line">    <span class="built_in">UITableViewCell</span> *cell = ...;</span><br><span class="line">    </span><br><span class="line">    [cell.trace.filter setFilterBlock:^<span class="built_in">BOOL</span>(TraceInfo * _Nonnull traceInfo) &#123;</span><br><span class="line">        traceInfo.set(<span class="string">@&quot;index&quot;</span>, @(indexPath.row).stringValue);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">    &#125; forKey:<span class="string">@&quot;index&quot;</span>];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> cell;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后我们还需要知道是在哪个页面中触发的，那么我们需要在vc中加入：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123;</span><br><span class="line">    [<span class="keyword">super</span> viewDidLoad];</span><br><span class="line">    [<span class="keyword">self</span>.trace.filter setFilterBlock:^<span class="built_in">BOOL</span>(TraceInfo * _Nonnull traceInfo) &#123;</span><br><span class="line">        traceInfo.setPage(<span class="string">@&quot;test&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">    &#125; forKey:<span class="string">@&quot;addPage&quot;</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么最后我们收集到的埋点信息就会包括了<code>praise</code>, <code>index</code>, <code>page</code>信息。</p><h4 id="自定义"><a href="#自定义" class="headerlink" title="自定义"></a>自定义</h4><p>在开发过程中，我们可能还会有很多业务分割的部分，比如ViewModel，这些内容并不一定在响应链中，那么我们需要能够将这个数据流通过该组件来收集数据。</p><p>这里我们可以在默认的数据流中插入一个自定义节点来解决。本身这个数据流和响应链其实并没有什么关系，我们只是利用了响应链的结构而已。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">viewModel.view.trace.outStream = viewModel.trace;</span><br><span class="line">viewModel.trace.outStream = controller.trace;</span><br></pre></td></tr></table></figure><p><img src="/images/2019/trace/custom_trace.png" alt=""></p><p>这样我们就可以自定义数据节点了。</p><h4 id="过滤器"><a href="#过滤器" class="headerlink" title="过滤器"></a>过滤器</h4><p>由于各种原因，我们的埋点并不能完全的通用，比如有时候，同样是点赞，有时候是<code>praise</code>，有时候是<code>like</code>。有时候我们又不希望触发某些埋点。</p><p>我们需要在某些确定的节点要求能够过滤或者转换某些埋点，那么我们需要在每个节点上添加过滤器：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">[<span class="keyword">self</span>.trace.filter setFilterBlock:^<span class="built_in">BOOL</span>(NMTraceInfo * _Nonnull traceInfo) &#123;</span><br><span class="line">    <span class="keyword">if</span> ([traceInfo.action isEqual:<span class="string">@&quot;praise&quot;</span>]) &#123;</span><br><span class="line">        traceInfo.setAction(<span class="string">@&quot;like&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125; forKey:<span class="string">@&quot;addPage&quot;</span>];</span><br></pre></td></tr></table></figure><p><img src="/images/2019/trace/trace_filter.png" alt=""></p><p>虽然这么做并不是特别好，但在一些无法抗拒的情况下还是能够给我们一个修改的机会。</p><h4 id="逻辑型埋点"><a href="#逻辑型埋点" class="headerlink" title="逻辑型埋点"></a>逻辑型埋点</h4><p>另一种让我们所有人都头疼的就是逻辑型埋点。</p><p>一般来说我们的埋点都是事件埋点，也就是触发一个方法，我们埋一次。但是有些需求要统计时长，比如用户点播放，和点暂停之前的时间间隔。按照原有方法埋点，那么我们可能需要在VC里增加一个成员，专门为了记录这个时间间隔。而这些成员和业务毫无关系，只服务于埋点信息，导致了业务的耦合增加。</p><p>对于我们程序来说，事件的点是比较简单的，而这种逻辑型埋点则非常复杂。如果我们把逻辑型埋点都转化为事件埋点，那么就会减少很多复杂度。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">__block <span class="built_in">NSDate</span> *playTime = <span class="literal">nil</span>;</span><br><span class="line">[<span class="keyword">self</span>.trace.filter setFilterBlock:^<span class="built_in">BOOL</span>(NMTraceInfo * _Nonnull traceInfo) &#123;</span><br><span class="line">    <span class="keyword">if</span> ([traceInfo.action isEqual:<span class="string">@&quot;play&quot;</span>]) &#123;</span><br><span class="line">        playTime = [<span class="built_in">NSDate</span> date];</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> ([traceInfo.action isEqual:<span class="string">@&quot;pause&quot;</span>]) &#123;</span><br><span class="line">        <span class="comment">// 得到两者之间的时间间隔</span></span><br><span class="line">        [<span class="keyword">self</span>.trace addTraceInfoWithBuilder:^(TraceInfo * _Nonnull traceInfo) &#123;</span><br><span class="line">            traceInfo</span><br><span class="line">            .setAction(<span class="string">@&quot;play-pause&quot;</span>)</span><br><span class="line">            .setTime([playTime timeIntervalSinceNow]);</span><br><span class="line">        &#125;];</span><br><span class="line">        playTime = <span class="literal">nil</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NO</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">YES</span>;</span><br><span class="line">&#125; forKey:<span class="string">@&quot;play-pause&quot;</span>];</span><br></pre></td></tr></table></figure><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这种方案虽然能够解决很大一部分的埋点痛点，但也依然存在一些缺陷。</p><ul><li>开发者必须要了解这种方案的原理，才能知道在什么节点收集埋点，以及如何插入自定义节点。</li><li>埋点信息会比较冗余，会带上一些无用信息。</li><li>埋点信息必须保证大部分是统一的，不然每个页面全部采用不同的key和埋点方式，也会让这个系统失去复用的能力。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h2&gt;&lt;p&gt;手动埋点一直以来都是一个比较麻烦的行为，埋点不属于业务逻辑，而我们必须插入业务逻辑中，有可能就导致了我们不得不修改设计以满足埋点的需求。&lt;/p&gt;
&lt;p&gt;同时数据方很多时候会要求带上额外的数据，而这些额外的数据甚至和当前模块毫无关系，也就是破坏了我们的独立性和解耦原则。比如在事件点击中加入当前页面的pageId，当前cell的index等等。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="iOS" scheme="http://djs66256.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>流程状态机架构</title>
    <link href="http://djs66256.github.io/2019/05/30/2019-05-30-%E6%B5%81%E7%A8%8B%E7%8A%B6%E6%80%81%E6%9C%BA%E6%9E%B6%E6%9E%84/"/>
    <id>http://djs66256.github.io/2019/05/30/2019-05-30-流程状态机架构/</id>
    <published>2019-05-30T15:24:20.000Z</published>
    <updated>2019-11-30T19:47:43.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>在现实的项目过程中，由于人员的更替，以及业务的发展，总是会导致一个问题，那就是流程细节的丢失，项目时间越久，这个问题越是严重。所以往往会出现产品在不知道这个功能的流程的情况下设计方案，开发在开发过程中才发现流程上的设计缺陷，这不仅仅导致了项目时间的推迟，更可能导致整个业务流程上出现漏洞而危害整个产品，如果是云音乐这种类型产生问题可能表现不大，但像考拉这种涉及到钱的项目严重的可能导致信任危机。</p><a id="more"></a><p>要解决这个问题传统的做法就是详细业务文档，这需要：</p><ul><li>文档要求详细，并且在统一的地方维护</li><li>人员交接的时候，文档需要完整交接</li><li>多方（产品、开发、测试等）的信息同步</li></ul><p>遗憾的是，这么多年的工作中，我没有发现一个项目能够做到这一点的，遇到问题只能人肉阅读代码。在目前国内企业的环境来看，要写一份详细完整的文档，也是几乎不现实的。</p><p>无论在哪种问题下，产品不了解流程，或是视觉不知道中间的弹窗，或是多方出现理解分歧，最终都是由开发阅读老代码来判断以前的流程是怎么样的，所以代码中的逻辑一般是最准确和详细的。在需求变更后，最先更新的也往往是代码（上帝也不能保证产品会把变革更新到文档）。</p><p>那么我们如果能从我们的代码逻辑中直接提取这个流程，并且通过流程图的形式展示出来，也就省去了很多一部分为别人服务的时间。而且拥有流程图之后我们也可以直观的检查我们的代码逻辑是是不是存在bug。</p><p>markdown有一个流程图表的功能，很多markdown编辑器也已经支持了，这给了我一个开发一个能够解决流程问题的思路。</p><pre><code>mermaidgraph TD</code></pre><h2 id="简述"><a href="#简述" class="headerlink" title="简述"></a>简述</h2><p>我们需要这个新的框架能够足够通用，能够适用于大部分场景。</p><ul><li><p>每个流程需要足够的独立，不能依赖太多的外部信息，能够复用和自由组合。</p></li><li><p>可以输出可读性强的流程信息，比如markdown等。</p></li><li><p>有一定的自检能力，保证整个流程的完整性和正确性。</p></li></ul><p>这里我们用一个最简单的点赞流程来解决：</p><p><img src="/images/2019/statement/praise.png" alt=""></p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><h4 id="1-结构"><a href="#1-结构" class="headerlink" title="1. 结构"></a>1. 结构</h4><h6 id="1-1-状态-State"><a href="#1-1-状态-State" class="headerlink" title="1.1 状态 State"></a>1.1 状态 State</h6><p>我们在看流程图的时候，把一整个流程拆分为一个个节点的时候，可以看到每个节点（状态）都可以抽象为拥有一个输入，多个输出的结构：</p><p><img src="/images/2019/statement/state.png" alt=""></p><p>每个具体状态必须只做一件事情，每个条件分支也必须为一个独立的状态。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (isLogin) &#123;</span><br><span class="line">    <span class="comment">// do login</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// do unlogin</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>需要抽象为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">StateIsLogin --&gt; |isLogin| login</span><br><span class="line">StateIsLogin --&gt; |!isLogin| unlogin</span><br></pre></td></tr></table></figure><p>多个状态可以组合成为一个复杂的状态，这个复杂的状态其行为依然和最简单的状态一致。当我们最终组合而成的点赞功能，也应该是一个状态机，拥有一个输入和多个输出。</p><h6 id="1-2-规则-Rule"><a href="#1-2-规则-Rule" class="headerlink" title="1.2 规则 Rule"></a>1.2 规则 Rule</h6><p>不同的状态之间，是通过某种规则进行连接。</p><p>最简单的规则就是执行下一步，最普遍的规则就是匹配上一个状态输出的结果。</p><p>为什么这里要加入个规则的概念，而不是直接建立状态直接的依赖关系？主要原因有：</p><ul><li>设计上更简单、明确，通用性更强，我可以自定义规则来实现一些多参数的匹配等</li><li>条理更清晰，可以方便的转化、输出文档</li><li>可以集中化管理，可以方便的跟踪当前状态的变更</li></ul><h6 id="1-3-组合"><a href="#1-3-组合" class="headerlink" title="1.3 组合"></a>1.3 组合</h6><p>这里演示为了代码的可读性，所以采用了c++，实际使用的时候语言并不是阻碍。</p><p>所有的状态创建统一通过工厂创建：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">auto</span> b = Builder(ctx);</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> checkNetwork = b.check(^BOOL(NSDictionary *params) &#123;</span><br><span class="line">    <span class="keyword">return</span> ![self isOffline];</span><br><span class="line">&#125;).debugName(@<span class="string">&quot;网络连接&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> checkLogin = b.check(^BOOL(NSDictionary * _Nullable params) &#123;</span><br><span class="line">    <span class="keyword">return</span> [self isLogin];</span><br><span class="line">&#125;).debugName(@<span class="string">&quot;用户登录&quot;</span>);</span><br><span class="line"></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>那么上述一个简单的点赞流程可以写作：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">b.start()</span><br><span class="line">    &gt;&gt; checkNetwork;</span><br><span class="line"></span><br><span class="line">checkNetwork</span><br><span class="line">    &gt;&gt; Result::Yes &gt;&gt; checkLogin</span><br><span class="line">    &gt;&gt; Result::No &gt;&gt; b.end();</span><br><span class="line"></span><br><span class="line">checkLogin</span><br><span class="line">    &gt;&gt; Result::Yes &gt;&gt; praiseRequest</span><br><span class="line">    &gt;&gt; Result::No &gt;&gt; b.end();</span><br><span class="line"></span><br><span class="line">praiseRequest</span><br><span class="line">    &gt;&gt; Result::Success &gt;&gt; successToast</span><br><span class="line">    &gt;&gt; Result::Failure &gt;&gt; failureToast;</span><br><span class="line"></span><br><span class="line">successToast</span><br><span class="line">    &gt;&gt; b.end();</span><br><span class="line"></span><br><span class="line">failureToast</span><br><span class="line">    &gt;&gt; b.end();</span><br><span class="line">    </span><br><span class="line"><span class="keyword">auto</span> praiseMachine = b.compositeMachine();</span><br></pre></td></tr></table></figure><p>那么我们最终得到的状态机可以在需要的时候启动：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[praiseMachine startWithParams:@&#123;<span class="string">@&quot;test&quot;</span>: <span class="string">@&quot;Test Value&quot;</span>&#125;];</span><br></pre></td></tr></table></figure><p>这里就不再描述具体结构了，详细可以看我的<a href="https://github.com/djs66256/DDStateMachine">demo</a></p><h6 id="1-4-纯粹性-原子性"><a href="#1-4-纯粹性-原子性" class="headerlink" title="1.4 纯粹性/原子性"></a>1.4 纯粹性/原子性</h6><p>一个状态在被使用过后，可能会产生很多垃圾，会污染了这个状态的内部。反之我们可以定义在执行前后不会发生变化的状态为纯状态。</p><p>如果我们的子状态机都是纯状态，那么我们组合而成的状态机也必然是纯状态。</p><p>纯状态的一个好处就是可以被反复使用，而不需要重新创建，减少创建的开销，以及提前创建流程状态机。</p><h4 id="2-输出文档"><a href="#2-输出文档" class="headerlink" title="2. 输出文档"></a>2. 输出文档</h4><p>我们把结构改成状态机虽然能够在代码流程上更为清晰，但也会增加学习成本和代码量，单纯这么做的意义并不大，所以我们需要将这个流程以更可见的形式输出。</p><p>某些markdown编辑器集成了流程图的语法，我们刚好可以借助这个功能。下面我们将我们的结构格式化后打印出来：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">DDStateMachineMarkdownWriter *writer = [DDStateMachineMarkdownWriter new];</span><br><span class="line">[praiseMachine debugWriteMarkdownText:writer];</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@&quot;%@&quot;</span>, writer.markdownText);</span><br></pre></td></tr></table></figure><p>打印结果是</p><pre><code><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">graph TD</span><br><span class="line"></span><br><span class="line">DDBlockStateMachine1(网络连接) --&gt; |YES| DDBlockStateMachine2(用户登录)</span><br><span class="line">DDBlockStateMachine1(网络连接) --&gt; |NO| DDContinueStateMachine1(End)</span><br><span class="line">DDBlockStateMachine3(点赞请求) --&gt; |success| DDBlockStateMachine4(提示)</span><br><span class="line">DDBlockStateMachine3(点赞请求) --&gt; |failure| DDBlockStateMachine5(提示)</span><br><span class="line">DDContinueStateMachine2(Start) --&gt; DDBlockStateMachine1(网络连接)</span><br><span class="line">DDBlockStateMachine5(提示) --&gt; DDContinueStateMachine1(End)</span><br><span class="line">DDBlockStateMachine2(用户登录) --&gt; |NO| DDContinueStateMachine1(End)</span><br><span class="line">DDBlockStateMachine2(用户登录) --&gt; |YES| DDBlockStateMachine3(点赞请求)</span><br><span class="line">DDBlockStateMachine4(提示) --&gt; DDContinueStateMachine1(End)</span><br><span class="line"></span><br></pre></td></tr></table></figure></code></pre><p>复制到markdown编辑器中，可以转化为流程图：</p><p><img src="/images/2019/statement/state_graph.png" alt=""></p><p>当然这只是一种导出方式，我们可以自行设计更好的导出方式。</p><p>我们可以通过比对流程图，来看我们修改的逻辑流程是否发生了正确的改动。</p><p>甚至我们可以在自动化测试脚本中加入流程的验证，或者控制流程的走向。</p><p>在线上我们也可以用于记录用户操作，是在什么环节出现了错误导致本次流程的失败或者撤销。</p><h4 id="3-状态跟踪"><a href="#3-状态跟踪" class="headerlink" title="3. 状态跟踪"></a>3. 状态跟踪</h4><p>当我们把流程封装成一个状态后，就相当于一个黑盒系统了，我们无法在这个流程中插入一些额外的非流程内的东西了，比如统计埋点等。</p><p>这里我们需要给外部系统提供一个状态跟踪的能力，当触发这个状态变更的时候给予外部一个信号或者执行一段代码，具体能力可以依据需要实现。这也是上述为什么要设计Rule这一结构的原因之一。</p><p>以上的流程代码可改为：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">checkLogin</span><br><span class="line">    &gt;&gt; Result::Yes &gt;&gt; Trace(@<span class="string">&quot;login&quot;</span>) &gt;&gt; praiseRequest</span><br><span class="line">    &gt;&gt; Result::No &gt;&gt; b.end();</span><br></pre></td></tr></table></figure><p>那么我们就可以去监听@”login”这个信号，在请求成功后就会自动触发。比如在这里增加一个埋点信息。（关于埋点，我们后续可以再讨论一篇）</p><p>比如打印信息：<code>Machine (用户登录) will goto (点赞请求) with log (login)</code></p><h4 id="4-完整性检查"><a href="#4-完整性检查" class="headerlink" title="4. 完整性检查"></a>4. 完整性检查</h4><p>另一个开发中比较容易出现的问题就是对异常和特殊情况的判断，这个问题在传统的开发中并没有什么特别有效的方法去避免，只能靠个人能力和测试的完备。</p><p>一个简单的例子就是，我们会在请求之后判断成功而更新UI，但会比较容易忽略失败的场景，而对于某些场景我们还是需要去处理失败的。</p><p>而在这种新的结构下，我们就比较容易做这个检查了。</p><p>我们需要在强制检查的状态中指定可能会返回的结果：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">machine.validResults = @[Result::Yes, Result::No];</span><br></pre></td></tr></table></figure><p>然后去检查可能的结果是否都已被Rule处理了：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">NSError</span> *error = <span class="literal">nil</span>;</span><br><span class="line">[praiseMachine checkRuleCompleteWithError:&amp;error];</span><br><span class="line"><span class="keyword">if</span> (error) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;Error: %@&quot;</span>, error.localizedDescription);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果发现缺失，则会输出哪一个状态的哪一条规则缺失：<code>Error: &lt;DDBlockStateMachine&gt; 用户登录 do not obey result(YES);</code>。</p><p>这样我们就可以强制开发者必须保证逻辑流程的完整性，以防止我们在不经意间引入一些问题。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>以上几个能力是我觉得在现实的场景中比较重要的，现实项目中，特别是大型项目，人员流动频繁，有些时候整个业务都可能会进入一个不可控的场景，这时候我们不能保证外部给我们信息是完整的，也不能保证内部人员足够的熟悉项目的细节，那么只能从架构上进行约束，将很多流程规范化，结构化，进行强校验才是解决这类问题的方法。</p><p>采用新的架构也必然会导致整个学习成本的上升，同时也要求整体人员的水平能够从更高的角度去看待问题，能够去理解这么做的原因和原理。</p><p>约束并不是目的，而是在约束下我们能更好的保证质量。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h2&gt;&lt;p&gt;在现实的项目过程中，由于人员的更替，以及业务的发展，总是会导致一个问题，那就是流程细节的丢失，项目时间越久，这个问题越是严重。所以往往会出现产品在不知道这个功能的流程的情况下设计方案，开发在开发过程中才发现流程上的设计缺陷，这不仅仅导致了项目时间的推迟，更可能导致整个业务流程上出现漏洞而危害整个产品，如果是云音乐这种类型产生问题可能表现不大，但像考拉这种涉及到钱的项目严重的可能导致信任危机。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="iOS" scheme="http://djs66256.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>Corountines 看似美好却危险重重</title>
    <link href="http://djs66256.github.io/2019/04/07/2019-04-07-Corountines-%E7%9C%8B%E4%BC%BC%E7%BE%8E%E5%A5%BD%E5%8D%B4%E5%8D%B1%E9%99%A9%E9%87%8D%E9%87%8D/"/>
    <id>http://djs66256.github.io/2019/04/07/2019-04-07-Corountines-看似美好却危险重重/</id>
    <published>2019-04-06T17:11:27.000Z</published>
    <updated>2019-04-09T18:51:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>协程Coroutines这个概念在5、6年前就已经比较热门了，而且也涌现了一波以该目标的第三方库，但是这个概念并没有被广泛的应用，这里来看看Coroutines给我们带来的便利和危险吧。</p><a id="more"></a><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>关于Coroutines、Generator的介绍有大量优秀文章和wiki的介绍，这里不做赘述。</p><p>协程出现的时间非常早，在一批语言（C#，Go，ES7）从语言本身开始支持这种特性而再一次受到关注。同时也有一大批C/C++类库的出现，来实现协程的特性，比如PCL, fiber, coroutine，以及支持Coroutine的网络库asio。甚至有人提议C++标准组织增加关键字来支持【<a href="https://isocpp.org/files/papers/N3858.pdf">N3858</a>】【<a href="http://www.open-std.org/jtc1/sc22/wg21/docs/papers/2014/n3985.pdf">N3985</a>】。</p><h2 id="化异步为同步"><a href="#化异步为同步" class="headerlink" title="化异步为同步"></a>化异步为同步</h2><p>Coroutine主要想解决的问题是异步回调问题，这个问题自从多线程开始就是另大家头疼的问题，我们使用过很多方法，都无法很好的解决这个问题。比如Callback和Promise，都存在着一些瑕疵。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Callback</span></span><br><span class="line">fs.readFile(<span class="string">&#x27;a.txt&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">error, data</span>) </span>&#123;</span><br><span class="line">  fs.readFile(<span class="string">&#x27;b.txt&#x27;</span>, <span class="function"><span class="keyword">function</span> (<span class="params">error, data</span>) </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Promise</span></span><br><span class="line"><span class="built_in">Promise</span>.all([</span><br><span class="line">  fs.readFile(<span class="string">&#x27;a.txt&#x27;</span>), </span><br><span class="line">  fs.readFile(<span class="string">&#x27;b.txt&#x27;</span>)])</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure><p>对我们来说同步执行的逻辑是最简单的，也是最清晰的，我们将很多同步功能放到异步执行往往是为了解决阻塞问题，所以我们是完全可以将异步的功能写成同步的。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">await</span> fs.readFile(<span class="string">&#x27;a.txt&#x27;</span>)</span><br><span class="line"><span class="keyword">await</span> fs.readFile(<span class="string">&#x27;b.txt&#x27;</span>)</span><br></pre></td></tr></table></figure><p>这里我们可以用关键字<code>await</code>来表示这个方法是一个异步过程。有很多语言天生自带这一类特性，但是C系列语言并不具备这个关键字，下面来看看如何实现这个功能。</p><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>这里盗C++论文一张图来说明一下我们需要实现协程的流程：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="title">foo</span>(<span class="params"></span>)</span> &#123;</span><br><span class="line">  <span class="keyword">await</span> bar()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/images/2019/coroutines/coroutine-c.png" alt=""></p><p>我们需要在<code>await</code>的时候，将代码执行能力转交给<code>bar</code>，然后在<code>bar</code>异步回调的时候，将执行权限交还给<code>foo</code>，这需要一种上下文切换的能力，从CPU层面来说上下文就是寄存器，那么我们要实现上下文切换的功能也就非常简单了。</p><p>个人比较熟悉arm架构，所以这里简单说明下coobjc的<code>setcontext</code>和<code>getcontext</code>。对汇编不熟悉的人可以跳到<a href="#C语言实现上下文切换">C语言实现上下文切换</a>。</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">_coroutine_getcontext:</span></span><br><span class="line">    stp    x18,x19, [x0, <span class="number">#0x090</span>]</span><br><span class="line">    stp    x20,x21, [x0, <span class="number">#0x0A0</span>]</span><br><span class="line">    stp    x22,x23, [x0, <span class="number">#0x0B0</span>]</span><br><span class="line">    stp    x24,x25, [x0, <span class="number">#0x0C0</span>]</span><br><span class="line">    stp    x26,x27, [x0, <span class="number">#0x0D0</span>]</span><br><span class="line">    <span class="keyword">str</span>    x28, [x0, <span class="number">#0x0E0</span>]<span class="comment">;</span></span><br><span class="line">    stp    x29, x30, [x0, <span class="number">#0x0E8</span>]<span class="comment">;  // fp, lr</span></span><br><span class="line">    <span class="keyword">mov</span>    x9,      <span class="built_in">sp</span></span><br><span class="line">    <span class="keyword">str</span>    x9,      [x0, <span class="number">#0x0F8</span>]</span><br><span class="line">    <span class="keyword">str</span>    x30,     [x0, <span class="number">#0x100</span>]    <span class="comment">// store return address as pc</span></span><br><span class="line">    stp    <span class="built_in">d8</span>, <span class="built_in">d9</span>,  [x0, <span class="number">#0x150</span>]</span><br><span class="line">    stp    <span class="built_in">d10</span>,<span class="built_in">d11</span>, [x0, <span class="number">#0x160</span>]</span><br><span class="line">    stp    <span class="built_in">d12</span>,<span class="built_in">d13</span>, [x0, <span class="number">#0x170</span>]</span><br><span class="line">    stp    <span class="built_in">d14</span>,<span class="built_in">d15</span>, [x0, <span class="number">#0x180</span>]</span><br><span class="line">    <span class="keyword">mov</span>    x0, <span class="number">#0</span>                   </span><br><span class="line">    ret</span><br></pre></td></tr></table></figure><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">_coroutine_setcontext:</span></span><br><span class="line">    ldp    x18,x19, [x0, <span class="number">#0x090</span>]</span><br><span class="line">    ldp    x20,x21, [x0, <span class="number">#0x0A0</span>]</span><br><span class="line">    ldp    x22,x23, [x0, <span class="number">#0x0B0</span>]</span><br><span class="line">    ldp    x24,x25, [x0, <span class="number">#0x0C0</span>]</span><br><span class="line">    ldp    x26,x27, [x0, <span class="number">#0x0D0</span>]</span><br><span class="line">    ldp    x28,x29, [x0, <span class="number">#0x0E0</span>]</span><br><span class="line">    <span class="keyword">ldr</span>    x30,     [x0, <span class="number">#0x100</span>]  <span class="comment">// restore pc into lr</span></span><br><span class="line">    <span class="keyword">ldr</span>    x1,      [x0, <span class="number">#0x0F8</span>]</span><br><span class="line">    <span class="keyword">mov</span>    <span class="built_in">sp</span>,x1                  <span class="comment">// restore sp</span></span><br><span class="line">    ldp    <span class="built_in">d8</span>, <span class="built_in">d9</span>,  [x0, <span class="number">#0x150</span>]</span><br><span class="line">    ldp    <span class="built_in">d10</span>,<span class="built_in">d11</span>, [x0, <span class="number">#0x160</span>]</span><br><span class="line">    ldp    <span class="built_in">d12</span>,<span class="built_in">d13</span>, [x0, <span class="number">#0x170</span>]</span><br><span class="line">    ldp    <span class="built_in">d14</span>,<span class="built_in">d15</span>, [x0, <span class="number">#0x180</span>]</span><br><span class="line">    ldp    x0, x1,  [x0, <span class="number">#0x000</span>]  <span class="comment">// restore x0,x1</span></span><br><span class="line">    ret    x30</span><br></pre></td></tr></table></figure><p>arm64中寄存器分为两类：普通寄存器和向量寄存器（也可以称为浮点寄存器）。根据C++ abi文档，各种寄存器的功能如下：</p><table><thead><tr><th>registers</th><th>role</th></tr></thead><tbody><tr><td>SP</td><td>stack pointer</td></tr><tr><td>r30</td><td>LR</td></tr><tr><td>r29</td><td>FP</td></tr><tr><td>r0-r7</td><td>参数寄存器</td></tr><tr><td>r8</td><td>IR</td></tr><tr><td>r9-r15</td><td>临时寄存器</td></tr><tr><td>r16/r17</td><td>PLT(目前我也不了解)</td></tr><tr><td>r18</td><td>平台寄存器/临时寄存器</td></tr><tr><td>r19-r28</td><td>Callee寄存器，必要时由被调用者保存</td></tr><tr><td>v0-v7</td><td>参数寄存器</td></tr><tr><td>v8-v15</td><td>Callee寄存器，必要时由被调用者保存</td></tr><tr><td>v16-v31</td><td>其他</td></tr></tbody></table><p>由此，我们可以知道必须缓存的寄存器是哪些了。</p><h4 id="C语言实现上下文切换"><a href="#C语言实现上下文切换" class="headerlink" title="C语言实现上下文切换"></a>C语言实现上下文切换</h4><p>当然系统其实也给我们提供了相似的功能，原理其实都是一样的，下面介绍一下几种系统提供的方式，详细信息可以参考相关文档：</p><h6 id="setjmp-longjmp"><a href="#setjmp-longjmp" class="headerlink" title="setjmp/longjmp"></a>setjmp/longjmp</h6><p>这种方式非常的容易理解，唯一的缺点就是返回值类型是int，所以在32位系统上我们可以返回上下文指针，而64位系统就不能这么做了。</p><h6 id="ucontext"><a href="#ucontext" class="headerlink" title="ucontext"></a>ucontext</h6><p>目前苹果已经不支持了。</p><h6 id="signal"><a href="#signal" class="headerlink" title="signal"></a>signal</h6><p>信号也是通过类似方式实现的。</p><h2 id="危险"><a href="#危险" class="headerlink" title="危险"></a>危险</h2><p>具体实现细节还是需要自己去研究，这里说明一下其中带来的几个问题。</p><h4 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h4><p>在我们的功能特性中，在执行async函数时，是需要直接返回的。由于C语言调用栈的结构，在函数返回时，是需要销毁栈的，所以我们必然不能在当前栈上运行我们的async函数，不然等异步返回的时候，函数栈帧已经被回滚（unwind）。所以在我们第一次（当前上下文的首次）启动一个async函数的时候，必须要为该函数申请一个不会被立刻销毁的栈，也就是malloc一块内存作为栈使用。目前像coobjc默认是64k的大小，对于性能要求比较关键的地方就需要好好考虑了。当然，按目前来说，一定的性能损耗给我们开发带来便捷还是可以接受的。</p><p>但是，上下文的切换可能让我们debug陷入一个无尽的深渊。大家在开发多线程的时候，都感觉比较难以调试，因为线程之间是独立的。而我们一旦切换了上下文，同时重新创建调用栈，也就意味着我们丢失了起点，有点像一个独立的线程，是无法找到调用地点的，除非我们在创建新栈的时候，将旧的栈全部拷贝，但这明显是不现实的，比较现实的可能是拷贝若干附近的栈帧数据。总之，c系语言的协程，debug将是一个比较困难的场景。</p><p>而coobjc在会库内部产生很多的栈帧，也增加了调试的复杂度。如果能够在切换上下文的时候将栈回滚，则会减少很多干扰调试的调用信息。</p><h4 id="stackful"><a href="#stackful" class="headerlink" title="stackful"></a>stackful</h4><p>目前来看C系的协程基本上都是stackful的，也就是可以在其他函数内部触发：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">some_function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">yield</span> <span class="string">&quot;OK&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">async_call</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// stackless 则必须在该函数栈帧内触发yield，比如javascript</span></span><br><span class="line">  some_function();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">do</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">await</span> async_call();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在语言本身并没有相关的特性的时候，我们很难检查目前方法是否支持协程，特别是整个链路比较长的时候，更加难以知道。一些规范也只是较弱的约定，依然无法保证在一个复杂的应用中是否会出现一些遗漏的问题。</p><p>如果我们只允许其中的<code>async/await</code>与<code>promise</code>特性，则会将影响面控制在一定范围内，应该会好一些。实际上我们依然无法规避这种问题。</p><h4 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h4><p>个人认为这是我们应用协程最大的挑战，这里的问题往往已经颠覆了我们的平常认知，这里可以看一个例子：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// on Thread 1</span></span><br><span class="line"><span class="keyword">await</span> do_something1()</span><br><span class="line"><span class="comment">// on Thread 2</span></span><br><span class="line"><span class="keyword">await</span> do_something2()</span><br><span class="line"><span class="comment">// on Thread 3</span></span><br></pre></td></tr></table></figure><p>我们无法预知在await函数之后会在哪个线程！这给多线程开发带来了非常大的挑战，因为这里几乎必然会发生竞争问题！</p><p>虽然在一个过程中，我们的调用是有顺序的，是不会产生什么问题，但是我们的场景往往不会只触发一次那么简单。我们需要在每个await调用上思考多线程问题，这个反而给我们的开发带来了更大的麻烦。</p><p>当然我们可以约定，回调必须发生在主线程等，但是约定始终是约定，无法作为强制性的禁止，完全依赖个人的开发能力。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>可以看到，协程作为C系语言的扩展，依然存在巨大的缺陷，C++标准组委会到目前为止，也依然没有将其作为一个特性。协程的目的是为了化异步为同步，简化我们的开发和理解，但在真实的多线程环境中，反而对开发者的要求更高了，已经脱离了初衷。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>Glibc</p><p>armasm_user_guide</p><p>IHI0055C_beta_aapcs64</p><p><a href="https://www.boost.org/doc/libs/1_57_0/libs/coroutine/doc/html/index.html">https://www.boost.org/doc/libs/1_57_0/libs/coroutine/doc/html/index.html</a></p><p><a href="https://en.wikipedia.org/wiki/Setjmp.h">https://en.wikipedia.org/wiki/Setjmp.h</a></p><p><a href="https://en.wikipedia.org/wiki/Setcontext">https://en.wikipedia.org/wiki/Setcontext</a></p><p><a href="https://en.wikipedia.org/wiki/Iterator">https://en.wikipedia.org/wiki/Iterator</a></p><p><a href="https://en.wikipedia.org/wiki/Continuation">https://en.wikipedia.org/wiki/Continuation</a></p><p><a href="https://en.wikipedia.org/wiki/Coroutine">https://en.wikipedia.org/wiki/Coroutine</a></p><p><a href="http://libdill.org/tutorial-basics.html">C Generator</a></p><p><a href="https://www.codeproject.com/KB/cpp/cpp_generators.aspx">C++ Generator</a></p><p><a href="https://www.boost.org/doc/libs/1_67_0/libs/fiber/doc/html/index.html">Boost fiber</a></p><p><a href="https://www.boost.org/doc/libs/1_54_0/doc/html/boost_asio/overview/core/spawn.html">C++ stackful coroutines</a></p><p><a href="https://www.boost.org/doc/libs/1_69_0/libs/coroutine/doc/html/index.html">C++ Coroutine</a></p><p><a href="https://github.com/jamboree/co2">A C++ await/yield emulation library for stackless coroutine</a></p><p><a href="https://blog.varunramesh.net/posts/stackless-vs-stackful-coroutines/">Stackless vs. Stackful Coroutines</a></p><p><a href="http://www.open-std.org/jtc1/sc22/wg21/docs/papers/2014/n3985.pdf">n3985</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;协程Coroutines这个概念在5、6年前就已经比较热门了，而且也涌现了一波以该目标的第三方库，但是这个概念并没有被广泛的应用，这里来看看Coroutines给我们带来的便利和危险吧。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="coobjc" scheme="http://djs66256.github.io/tags/coobjc/"/>
    
      <category term="coroutines" scheme="http://djs66256.github.io/tags/coroutines/"/>
    
  </entry>
  
  <entry>
    <title>C++虚函数表一探</title>
    <link href="http://djs66256.github.io/2019/02/03/2019-02-03-C-%E8%99%9A%E5%87%BD%E6%95%B0%E8%A1%A8%E4%B8%80%E6%8E%A2/"/>
    <id>http://djs66256.github.io/2019/02/03/2019-02-03-C-虚函数表一探/</id>
    <published>2019-02-03T11:39:14.000Z</published>
    <updated>2019-02-03T12:58:22.000Z</updated>
    
    <content type="html"><![CDATA[<p>虽然iOS中对C++的要求并不是必须的，但是很多基础都是用C++写的。我们都说C++难学，除了众多的特性以及为了兼容底层语言的一些妥协，导致特性极其复杂，虽然经过C++11之后的标准，已经更加完善和易用了，但我们还是需要去了解一些C++的实现原理及特性，才能避免掉入坑中。</p><a id="more"></a><p>这里将不会一一说明C++的基础知识，我相信有很多人应该比我说的更好。</p><h2 id="虚函数"><a href="#虚函数" class="headerlink" title="虚函数"></a>虚函数</h2><p>C++的虚函数实现是通过虚函数表，这是大家都知道的。那么虚函数表是一个什么样的结构呢？</p><p>这里我们用类C语言的描述来说明一下。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">public</span> A &#123;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">g</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>以上用C结构表示大概是这样的：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">vtable_A</span> &#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(A *<span class="keyword">this</span>)</span> </span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">A</span> &#123;</span></span><br><span class="line">  vtable_A *__vtable;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">vtable_B</span> &#123;</span></span><br><span class="line">  vtable_A _super;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">g</span><span class="params">(B *<span class="keyword">this</span>)</span> </span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">B</span> &#123;</span></span><br><span class="line">  vtable_B *__vtable;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>编译器会在类中自动生成一个指向虚函数表的指针，并在构造类的时候帮我们把对应的虚函数指针填上。那么如果我们重写了某个基类的虚函数的时候，我们的虚函数表相对应的虚函数会改写为子类的函数指针，如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">vtable_A:  | A::f() |</span><br><span class="line">如果没有重写f()，B的虚函数表应该如下：</span><br><span class="line">vtable_B:  | A::f() | B::g() |</span><br><span class="line">如果重写了f()，B的虚函数表则会变为：</span><br><span class="line">vtable_B:  | B::f() | B::g() |</span><br></pre></td></tr></table></figure><p>所以我们从表中查找出来的将会是B::f。这也解释了为什么C++类，只含有虚函数的class，size也是需要有一个指针的大小。</p><p>当有多个基类的时候，这时候为了分辨多个来源的时候，将会采用多个虚函数表结构：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> &#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> &#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">g</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> &#123;</span></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这时候的C的虚函数表将会是这种感觉：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">vtable_A:  | C::f() |</span><br><span class="line">vtable_B:  | C::f() | B::g() |</span><br><span class="line">class C &#123;</span><br><span class="line">  vtable_A *__vtable_a;</span><br><span class="line">  vtable_B *__vtable_b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而更复杂的情况也是这目前的扩展，这里就不赘述了。</p><h2 id="函数参数"><a href="#函数参数" class="headerlink" title="函数参数"></a>函数参数</h2><p>我们都知道，C++能和C无缝链接，其实就是对C语言的一个扩展，其大部分机制，比如参数传递都是和C语言一模一样的。而C语言是不支持this这种语法的，那么C++是如何传递参数的呢？</p><p>虽然我们看来<code>A::f()</code>就是该函数定义，但实际上我们是有一个this的隐式参数，也就是说<code>A::f(A *this)</code>才是该函数的真正定义。</p><p>结合上节，那么我们就可以动态的取出vtable内的函数指针，进行调用，实现类似“动态语言”的某些特性。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*Fun)</span><span class="params">(<span class="keyword">void</span>*)</span></span>;</span><br><span class="line">C c;</span><br><span class="line"><span class="keyword">uintptr_t</span> cptr = <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">uintptr_t</span>&gt; (&amp;c);</span><br><span class="line"><span class="keyword">uintptr_t</span> *vtable = <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">uintptr_t</span> *&gt; (cptr);</span><br><span class="line">Fun *vtc = <span class="keyword">reinterpret_cast</span>&lt;Fun *&gt;(*(vtable + <span class="number">0</span>));</span><br><span class="line">Fun f = *(vtc);</span><br><span class="line">f(&amp;c);</span><br></pre></td></tr></table></figure><h2 id="多基类的this指针"><a href="#多基类的this指针" class="headerlink" title="多基类的this指针"></a>多基类的this指针</h2><p>上节我们知道this指针将会是一个隐式参数，那么出现多个基类的时候，又是如何处理的呢？</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; __PRETTY_FUNCTION__ &lt;&lt; <span class="string">&quot; this: &quot;</span> &lt;&lt; <span class="keyword">this</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">g</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; __PRETTY_FUNCTION__ &lt;&lt; <span class="string">&quot; this: &quot;</span> &lt;&lt; <span class="keyword">this</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">     <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">         <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; __PRETTY_FUNCTION__ &lt;&lt; <span class="string">&quot; this: &quot;</span> &lt;&lt; <span class="keyword">this</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">     &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">k</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;f();</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; __PRETTY_FUNCTION__ &lt;&lt; <span class="string">&quot; this: &quot;</span> &lt;&lt; <span class="keyword">this</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> :</span> <span class="keyword">public</span> A, <span class="keyword">public</span> B &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; __PRETTY_FUNCTION__ &lt;&lt; <span class="string">&quot; this: &quot;</span> &lt;&lt; <span class="keyword">this</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>如果我们调用</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">C c;</span><br><span class="line">c.k()</span><br></pre></td></tr></table></figure><p>此时调用的是子类的C::f()还是自己的B::f()呢？</p><p>按照我们对面向对象语言的理解和要求，最终调用的肯定是C::f()。但是按照vtable的布局要求，B::f()中this所指向的是<code>&amp;c+8</code>的地址，那么基类又是怎么知道最终需要调用C::f()并且找到子类的地址<code>&amp;c</code>呢？</p><p>以上代码的打印为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">class C</span><br><span class="line">virtual void C::f() this: 0x7ffeefbff560</span><br><span class="line">virtual void B::k() this: 0x7ffeefbff568</span><br></pre></td></tr></table></figure><p>可以看到this指针的确是真实的vtable指针，概括为C语言大概是这样的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">C</span> <span class="title">c</span>;</span></span><br><span class="line">B::k( &amp;c + <span class="number">8</span> <span class="comment">/* sizeof(B::f) */</span> );</span><br><span class="line"></span><br><span class="line"><span class="comment">// 而k()中的调用可以解释为</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">B::k</span><span class="params">(B *<span class="keyword">this</span>)</span> </span>&#123;</span><br><span class="line">  func_ptr p = vtable_get(<span class="keyword">this</span>, B::f);</span><br><span class="line">  p(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>要知道编译器具体为我们做了什么魔法操作，那么我们就需要看看其汇编结果了。</p><p>在<code>this-&gt;f()</code>单步跟踪，我们会发现他会进入一个奇怪的函数。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">0x100001620 &lt;+0&gt;:  pushq  %rbp</span><br><span class="line">0x100001621 &lt;+1&gt;:  movq   %rsp, %rbp</span><br><span class="line">0x100001624 &lt;+4&gt;:  movq   %rdi, -0x8(%rbp)</span><br><span class="line">0x100001628 &lt;+8&gt;:  movq   -0x8(%rbp), %rdi</span><br><span class="line">0x10000162c &lt;+12&gt;: addq   $-0x8, %rdi</span><br><span class="line">0x100001630 &lt;+16&gt;: popq   %rbp</span><br><span class="line">0x100001631 &lt;+17&gt;: jmp    0x100001540               ; C::f at main.cpp:103</span><br></pre></td></tr></table></figure><p>说明我们虚函数表里面的<code>C::f()</code>并不是我们所写的那个方法，而是编译器帮我们封装了一层，专门进行了<code>this-8</code>这样的还原指针地址的操作。</p><p>可以说这一顿操作真的是很厉害，我们平时用的时候都不曾注意这些，也不曾深入了解为什么C++中有些方法必须是虚函数，或者有些时候虚函数存在设计缺陷。</p><p>不过在debug的过程中，xcode中this指针永远是显示的子类的指针，不知道做了如何的处理。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>C++依然是一门比较复杂的语言，虽然我个人还是比较喜欢C++的特性，拥有非常大的灵活性，但是不得不说要用好还是非常难的。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;虽然iOS中对C++的要求并不是必须的，但是很多基础都是用C++写的。我们都说C++难学，除了众多的特性以及为了兼容底层语言的一些妥协，导致特性极其复杂，虽然经过C++11之后的标准，已经更加完善和易用了，但我们还是需要去了解一些C++的实现原理及特性，才能避免掉入坑中。&lt;/p&gt;
    
    </summary>
    
      <category term="C++" scheme="http://djs66256.github.io/categories/C/"/>
    
    
      <category term="vtable" scheme="http://djs66256.github.io/tags/vtable/"/>
    
  </entry>
  
  <entry>
    <title>CPU分支预测</title>
    <link href="http://djs66256.github.io/2019/01/29/2019-01-29-CPU%E5%88%86%E6%94%AF%E9%A2%84%E6%B5%8B/"/>
    <id>http://djs66256.github.io/2019/01/29/2019-01-29-CPU分支预测/</id>
    <published>2019-01-28T18:27:04.000Z</published>
    <updated>2019-01-29T17:41:30.000Z</updated>
    
    <content type="html"><![CDATA[<p>CPU在我们不知道的情况下做了很多的优化，下面我们就来看看。</p><a id="more"></a><h2 id="Case"><a href="#Case" class="headerlink" title="Case"></a>Case</h2><p>如下，有一个n长度的数组，内容为0~256的随机数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> <span class="built_in">array</span>[i];</span><br><span class="line"><span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">array</span>[i] &lt; <span class="number">128</span>) &#123;</span><br><span class="line">        sum += <span class="built_in">array</span>[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果该数组是一个排好顺序的数组，那么同样条件下执行该逻辑。</p><p>我们会发现排序的会比未排序的执行效率高很多，甚至是其几倍的关系，这是为什么呢？</p><p>这就涉及到CPU在执行条件语句的时候所做的优化了，也就是分支预测。</p><h2 id="Why"><a href="#Why" class="headerlink" title="Why?"></a>Why?</h2><p>分支预测是指在执行条件分支前，就去猜测其条件结果。其目的是为了提升CPU流水线的执行效率。在现代CPU中，分支预测扮演着一个非常重要的，提升效率的角色。</p><p>如果没有分支预测，那么CPU需要等待条件执行完后才能知道下一步的跳转地址。为了消除之间的等待时间，CPU会去预测其条件结果，是更倾向于true，还是更倾向于false。在预测错误的情况下，需要忽略之后的指令，并且重新加载正确的指令。</p><p>重新加载指令所造成的时间浪费取决于流水线架构CPU的层级。比如下图最简单的4级流水线。现代CPU往往拥有非常大的流水线级别，很容易造成10~20时钟周期的浪费。所以就更需要更智能的分支预测了。</p><p><img src="/images/2018/branch_prediction/pipline.png" alt=""></p><h4 id="流水线架构"><a href="#流水线架构" class="headerlink" title="流水线架构"></a>流水线架构</h4><p>流水线架构CPU在执行指令的时候一般有如下几个过程：</p><ul><li>取指 Fetch</li><li>译指 Decode</li><li>执行 Execute</li><li>回写 Write-back</li></ul><h4 id="Static-branch-prediction-静态分支预测"><a href="#Static-branch-prediction-静态分支预测" class="headerlink" title="Static branch prediction / 静态分支预测"></a>Static branch prediction / 静态分支预测</h4><p>静态分支预测是一个非常简单的策略，不依赖执行环境和执行过程中的状态。比如某些CPU，总是会预测一个条件是false的，有些则会对向前条件和向后条件做不同的预测，甚至有些架构允许插入预测代码。但总的来说，静态预测永远是编译器就已经做好了。</p><h4 id="Dynamic-branch-prediction-动态分支预测"><a href="#Dynamic-branch-prediction-动态分支预测" class="headerlink" title="Dynamic branch prediction / 动态分支预测"></a>Dynamic branch prediction / 动态分支预测</h4><p>动态分支预测则是在运行期间来决定我们的预测结果。接下来我们要分析的均是动态分支预测。</p><h2 id="前提"><a href="#前提" class="headerlink" title="前提"></a>前提</h2><p>在做分支预测前，我们所确定的是，所有分支都是有偏好的，即可能80%都会是true，或是false。</p><h5 id="向前条件-Forward-conditional-branches"><a href="#向前条件-Forward-conditional-branches" class="headerlink" title="向前条件 / Forward conditional branches"></a>向前条件 / Forward conditional branches</h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (condition) <span class="keyword">continue</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="向后条件-Backward-conditional-branches"><a href="#向后条件-Backward-conditional-branches" class="headerlink" title="向后条件 / Backward conditional branches"></a>向后条件 / Backward conditional branches</h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (condition) &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="无条件跳转-Unconditional-branches"><a href="#无条件跳转-Unconditional-branches" class="headerlink" title="无条件跳转 / Unconditional branches"></a>无条件跳转 / Unconditional branches</h5><p><img src="/images/2018/branch_prediction/misprediction_rate.png" alt=""></p><h2 id="动态分支预测-Dynamic-Conditional-Branch-Prediction"><a href="#动态分支预测-Dynamic-Conditional-Branch-Prediction" class="headerlink" title="动态分支预测 / Dynamic Conditional Branch Prediction"></a>动态分支预测 / Dynamic Conditional Branch Prediction</h2><p>动态分支预测相比于静态分支预测，最大的区别就是在运行的时候进行预测。一般对于结果的预测都是依赖于历史结果，也就是该条件前几次的执行结果进行统计。</p><p>这里描述几个简单的方案：</p><h4 id="分支历史表-Branch-History-Table-BHT"><a href="#分支历史表-Branch-History-Table-BHT" class="headerlink" title="分支历史表 / Branch History Table (BHT)"></a>分支历史表 / Branch History Table (BHT)</h4><p><img src="/images/2018/branch_prediction/BHT.png" alt=""></p><p>BHT中记录历史结果大小，可以为</p><h6 id="1-Bit-Prediction-Quality"><a href="#1-Bit-Prediction-Quality" class="headerlink" title="1-Bit Prediction Quality"></a>1-Bit Prediction Quality</h6><p>1 bit只能记录 taken 和 not taken 两种结果，在很多场合下会降低预测准确率，特别是在循环中。</p><h6 id="2-Bit-Branch-Prediction"><a href="#2-Bit-Branch-Prediction" class="headerlink" title="2-Bit Branch-Prediction"></a>2-Bit Branch-Prediction</h6><p>2 bit就能记录4中状态：</p><ul><li>Strongly not taken</li><li>Weakly not taken</li><li>Weakly taken</li><li>Strongly taken</li></ul><p>这就需要2次不同的结果才能反转当前预测。当然可以选择更多 bit，但实验表明 2 bit 和 4096 个存储单元已经能够覆盖 82% - 99% 的场景了。</p><h6 id="Two-Level-Predictors-and-the-GShare-Algorithm"><a href="#Two-Level-Predictors-and-the-GShare-Algorithm" class="headerlink" title="Two-Level Predictors and the GShare Algorithm"></a>Two-Level Predictors and the GShare Algorithm</h6><p>另外还有一些更为复杂的方案，比如2层的预测算法，这里就不做描述了。</p><h4 id="Branch-Target-Buffer-BTB"><a href="#Branch-Target-Buffer-BTB" class="headerlink" title="Branch Target Buffer (BTB)"></a>Branch Target Buffer (BTB)</h4><p>另一种是BTB，这个表内存的是地址映射，也就是能够让我们在取指的时候就知道这是一个条件指令，那么下一个取指就可以根据该表得到预测的分支地址了。</p><p>一般情况下，我们需要在译码阶段才能知道该指令是否是条件指令，才能决定下一个指令是否需要分支预测，利用BTB可以继续提前一步。</p><p><img src="/images/2018/branch_prediction/BTB.png" alt=""></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;CPU在我们不知道的情况下做了很多的优化，下面我们就来看看。&lt;/p&gt;
    
    </summary>
    
      <category term="CS" scheme="http://djs66256.github.io/categories/CS/"/>
    
    
      <category term="CPU" scheme="http://djs66256.github.io/tags/CPU/"/>
    
      <category term="Branch Prediction" scheme="http://djs66256.github.io/tags/Branch-Prediction/"/>
    
  </entry>
  
  <entry>
    <title>Objc类型推断</title>
    <link href="http://djs66256.github.io/2018/11/24/2018-11-24-Objc%E7%B1%BB%E5%9E%8B%E6%8E%A8%E6%96%AD/"/>
    <id>http://djs66256.github.io/2018/11/24/2018-11-24-Objc类型推断/</id>
    <published>2018-11-24T15:42:04.000Z</published>
    <updated>2018-11-24T15:56:54.000Z</updated>
    
    <content type="html"><![CDATA[<p>Objc是一门非常啰嗦的语言，而且历史悠久，背靠C语言，发展缓慢。自从苹果有了新宠swift，更是对他不管不顾了，所以有很多现代语言拥有的优秀特性，Objc都无法享受到。其中一项就是类型推断。</p><a id="more"></a><p>每次我们在定义变量的时候，都需要先写类型，而Objc中类型名字又往往非常的长，所以很多时候一行初始化代码就让我们感到非常冗长。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">MYDoSomethingViewController *viewController = [[MYDoSomethingViewController alloc] init];</span><br></pre></td></tr></table></figure><p>而大部分时间，这些变量的含义相当明确，我们也并不需要去知道具体的类型，而我们却要每次去声明一次，显得非常多余。</p><p>得益于C语言的发展，在2015年，GCC和LLVM都先后的支持了类型推断，这样我们可以用于日常开发来增加代码可读性和简洁性。</p><p>首先我们定义两个类型推断关键字：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> var __auto_type</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> let __auto_type const</span></span><br></pre></td></tr></table></figure><p>以下是两者的对比：</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)t &#123;</span><br><span class="line">    let view = [<span class="built_in">UIView</span> new];</span><br><span class="line">    view.backgroundColor = <span class="built_in">UIColor</span>.redColor;</span><br><span class="line">    </span><br><span class="line">    let dict = @&#123; <span class="string">@&quot;view&quot;</span>: view &#125;;</span><br><span class="line">    </span><br><span class="line">    __<span class="keyword">weak</span> let wself = <span class="keyword">self</span>;</span><br><span class="line">    __block var num = <span class="number">0</span>;</span><br><span class="line">    let block = ^(<span class="built_in">UIView</span> *view, <span class="built_in">NSDictionary</span> *dict) &#123;</span><br><span class="line">        __<span class="keyword">strong</span> let <span class="keyword">self</span> = wself;</span><br><span class="line">        num = <span class="number">1</span>;</span><br><span class="line">        [<span class="keyword">self</span> func];</span><br><span class="line">    &#125;;</span><br><span class="line">    block(view, dict);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (var i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)t &#123;</span><br><span class="line">    <span class="built_in">UIView</span> *view = [[<span class="built_in">UIView</span> alloc] initWithFrame:<span class="built_in">CGRectZero</span>];;</span><br><span class="line">    view.backgroundColor = <span class="built_in">UIColor</span>.redColor;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">NSDictionary</span> *dict = @&#123; <span class="string">@&quot;view&quot;</span>: view &#125;;</span><br><span class="line">    </span><br><span class="line">    __<span class="keyword">weak</span> <span class="keyword">typeof</span>(<span class="keyword">self</span>) wself = <span class="keyword">self</span>;</span><br><span class="line">    __block <span class="built_in">NSInteger</span> num = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">void</span>(^block)(<span class="built_in">UIView</span> *view, <span class="built_in">NSDictionary</span> *dict) = ^(<span class="built_in">UIView</span> *view, <span class="built_in">NSDictionary</span> *dict) &#123;</span><br><span class="line">        __<span class="keyword">strong</span> <span class="keyword">typeof</span>(wself) <span class="keyword">self</span> = wself;</span><br><span class="line">        num = <span class="number">1</span>;</span><br><span class="line">        [<span class="keyword">self</span> func];</span><br><span class="line">    &#125;;</span><br><span class="line">    block(view, dict);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>遗憾的是，由于这个特性是C语言的特性，所以目前无法识别oc中的一些特定语法，比如forin和容器模板类型。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Objc是一门非常啰嗦的语言，而且历史悠久，背靠C语言，发展缓慢。自从苹果有了新宠swift，更是对他不管不顾了，所以有很多现代语言拥有的优秀特性，Objc都无法享受到。其中一项就是类型推断。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="iOS" scheme="http://djs66256.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>roadmap之objc</title>
    <link href="http://djs66256.github.io/2018/09/26/2018-09-26-iOS-roadmap%E4%B9%8Bobjc/"/>
    <id>http://djs66256.github.io/2018/09/26/2018-09-26-iOS-roadmap之objc/</id>
    <published>2018-09-25T17:33:55.000Z</published>
    <updated>2018-10-05T13:10:39.000Z</updated>
    
    <content type="html"><![CDATA[<p>objc是iOS的首要开发语言，在很多厂还是以objc为主，所以我们需要对objc更深入的了解，这里主要分为3个难点进行总结，runtime，runloop和block。</p><a id="more"></a><h1 id="Runtime"><a href="#Runtime" class="headerlink" title="Runtime"></a>Runtime</h1><p>Objc runtime是Objc运行的核心，我们都知道Objc是一门动态语言，为了实现其特性，必须需要一些动态的初始化。这里从几个方面进行介绍。</p><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>首先我们在了解runtime之前，必须要知道几个objc中重要的概念，才能更好的理解接下来的一些内容。</p><h5 id="Class-Meta-Class-isa"><a href="#Class-Meta-Class-isa" class="headerlink" title="Class / Meta Class / isa"></a>Class / Meta Class / isa</h5><p>在面向对象中，非常重要的一个概念就是类。在objc中，我们需要除了需要知道类的概念，还要知道元类的概念，以及他们之间的关系。</p><p>Class代表着一个对象的原型，所有对象都是在此模板上“复制”出来的。</p><p>而Meta Class可以认为是Class的原型，而Meta Class的原型则是NSObject。为什么需要这么设计呢？主要为了区分实例方法和类方法，也就是<code>-</code>和<code>+</code>方法。所有的实例方法均取自Class中，而类方法均取自元类中。</p><p>一个对象如何确定所属的类呢？是通过isa指针来实现的。在对象的第一个内存地址中就是isa指针，会指向其对应的类。而类中的isa指针则指向其元类。下图就能表示相互间的关系。</p><p><img src="/images/2018/runtime/isa.png" alt=""></p><h5 id="property"><a href="#property" class="headerlink" title="property"></a>property</h5><p>property一般代表的是一个类的成员，也有一些特殊情况，我们会重写他，但是其含义都是一样的，都表示了这是一个成员对象，并且拥有getter或者setter方法。同时property会拥有部分特殊含义的标识，比如assign/strong/weak, readonly, class, non/atomic, setter/getter等，编译器会根据其标识自动生成不同的代码。在老版本的objc中还需要自已去绑定其成员对象<code>@synchronize</code>，现在则会默认生成一个<code>_</code>开头的同名对象，或者可以通过该方法重新命名一个名字。</p><p>在运行时，property也有一个对应的数组进行保存其名字与属性。</p><h5 id="ivar"><a href="#ivar" class="headerlink" title="ivar"></a>ivar</h5><p>ivar在这里是真正的代表了一个成员对象的信息，包括其类型，大小，以及对应基址的偏移量。</p><p>一个实例对象，可以认为就是一个struct，我们需要访问成员就必须知道其偏移量才行。ivar的作用，就是记录其偏移量。</p><p>在我们访问成员变量的时候，并不是通过简单的<code>self+offset</code>来实现的，而是通过<code>self+ivar-&gt;offset</code>来实现的。这么做的原因是在objc中会有一个ivar rebase的过程，就是会通过父类的信息去重新推导出子类的偏移量。这样的好处是我们在编译上就不需要强依赖了，同时在升级过程中，也不会出现修改父类导致子类偏移量不对的问题了。</p><h5 id="selector"><a href="#selector" class="headerlink" title="selector"></a>selector</h5><p>这是我们熟悉的方法对象，在实现上可以认为<code>SEL</code>就是常量字符串<code>const char *</code>，但是是由一个全局表所管理的字符串，其内容都是唯一的。所以我们再比较SEL的时候，是可以使用<code>==</code>来判断是否相等的，但是我们在传递SEL的时候不能简单的使用C字符串来表示。同时不同类上的同名方法也是指向了同一个地址的。</p><h5 id="IMP"><a href="#IMP" class="headerlink" title="IMP"></a>IMP</h5><p>selector代表的是这个方法的名字，而IMP则是这个方法的函数指针（关于函数指针可以去了解下C语言）。</p><p>在objc中方法名SEL和其具体实现IMP是分离的，也就是说我们是可以改变这两者的映射关系的。</p><p>IMP我们也是可以直接调用的，但是由于其隐去了参数等信息，所以相对来说是比较危险的，不太符合我们强类型的一些要求。</p><h5 id="tagged-point"><a href="#tagged-point" class="headerlink" title="tagged point"></a>tagged point</h5><p>在64位系统中，有一种优化的指针类型，叫做tagged point，这种指针不会申请新的内存，而是把数据保存在指针中。由于64位系统的指针范围非常大，而我们的设备物理内存远远达不到这个量级，所以会有很多位是永远空余出来的，为了充分利用这一部分内存，就会把小数据写入这部分空闲区域，最常见的就是短字符串了。这类指针的最高位为1，所以可以通过判断<code>&lt;0</code>来确定是否是tagged point。</p><h5 id="objc-msgSend"><a href="#objc-msgSend" class="headerlink" title="objc_msgSend"></a>objc_msgSend</h5><p>这是objc种最核心的一个方法，是由汇编写成，为的是参数和返回值的透传，也可以去掉<code>objc_msgSend</code>的调用栈。</p><p>这个方法还有一些神奇的内容，这里就不细究了，建议自己学习汇编和C abi来研读这段简短的代码，这里做一个简单的流程介绍。</p><ol><li>如果是nil，返回0</li><li>通过SEL指针，在cache中查找。这里的cache是hash table，并且保留了一定的容量，hash key是指针值，所以查找速度还是很快的。</li><li>如果没有找到，则到class的method_list中查找，这是一个数组的顺序遍历过程，所以会比较慢。这里可能会触发class的initialize。</li><li>找到对应方法，会更新到缓存中，然后再返回，以方便下次缓存中就能命中。另外这个缓存是没有过期一说的，也就是说会越加越多。</li><li>通过找到的这个函数指针，直接进行直接调用。</li></ol><h2 id="动态特性"><a href="#动态特性" class="headerlink" title="动态特性"></a>动态特性</h2><p>我们都说objc是一门动态语言，那么动态体现在什么地方呢？个人认为主要体现在2个地方，分别是类的动态性和方法的动态性。</p><h5 id="类的动态性"><a href="#类的动态性" class="headerlink" title="类的动态性"></a>类的动态性</h5><p>一般来说，类的创建只能是在编译期就已经确定好的，但是由于objc的特性，类的创建其实是在动态库加载的时候，这个特性很像一些脚本语言。</p><p>在全局有一个类的表保存着所有的类，如果我们导入一个动态库，那么libobjc会在其加载之后去读取其中的类的信息，并且创建一个类加入这个全局表中，这个加载过程我们之后再讨论，这里我们要知道objc的类并不是我们认知的那样，在编译的时候就固定了的。</p><p>当然，我们也可以手动通过代码来动态的创建一个类，并且加入全局表中，比如KVO就是这样实现的。</p><p>当然我们无法对已经存在的类进行修改，可以想象，一旦修改了会发生什么严重的后果。</p><h5 id="方法的动态性"><a href="#方法的动态性" class="headerlink" title="方法的动态性"></a>方法的动态性</h5><p>方法的动态特性应该都很熟悉了，为什么会有这样特性，归功于objc_msgSend的机制，类并不会和函数指针绑定，而是通过SEL间接绑定的。</p><h5 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h5><p>我们知道了objc的动态特性，也要知道他被应用于哪些地方。</p><ul><li>category</li></ul><p>category是一些面向对象语言的特例，很少有强类型语言是支持这种写法的。那么为什么objc可以这么写呢？这还是要归功于objc类的创建是在运行期的。在加载完类后，会去遍历该macho中的category，并找到所对应的类，将其方法拷贝到类的method_list头部，这也是为什么category方法会覆盖原始类中的方法的原因了。而不同category之间，由于其加载顺序的不确定，所以不能确保谁的方法优先级会更高。如果想要通过这种方法来重写原本的方法，是强烈不推荐的，至于原因也不用多说了。</p><ul><li>weak</li></ul><p>weak属性也是我们平时使用非常多的，weak属性虽然不在动态特性里，但是由于和associate非常相似，所以也拿出来说说。</p><p>weak的实现方式是将该对象和weak属性的<code>指针</code>的<code>指针</code>加入一个全局weak表中（源码中是SlideTable），如果该对象释放了，则会去weak表中清除所有指针所指向的内容，也就是把该指针置为nil了。</p><ul><li>associate</li></ul><p>我们都奇怪，为什么category能够增加方法，却不能增加成员呢？这也是由objc的特性所导致的，一旦category可以增加成员，那么会导致一个类的大小和成员偏移量都是不固定的，一旦载入了某个动态库，导致这些信息发生改变，对已经存在的对象是无法处理的，所以无法对已经存在的类进行ivar相关的修改了。</p><p>associate的实现和weak非常类似，也是在一个全局的表中，加入该对象，在释放的时候会去遍历该表，并将其内容进行释放清理。</p><ul><li>method swizzle</li></ul><p>这个应该是我们最先意识到其动态能力的功能了，很多库都用到了该实现。其原理也很简单，改变了SEL和IMP的映射关系而已。</p><p>网上有很多人讨论是否需要使用该特性。个人的观点是能不用则不用。如果你是一个小团队开发，大家天天都能面对面讨论，那么这个问题不会特别大，但是如果这个项目庞大到必须进行模块拆分，很有可能就会给项目带来不可控因素。由于objc runtime是一个全局的东西，你无法把风险只控制在一个模块内，一旦出了问题就会非常严重。</p><p>比如很多项目可能都会去改写<code>objectAtIndex:</code>，去除其越界的可能和NSNull对象，但是很有可能就因为表面上不会产生crash了，但是问题依旧存在，逻辑上的问题也依旧存在，如果这是一个支付功能，可能就会变成错误的金额导致严重的线上问题。</p><ul><li>KVO</li></ul><p>这是苹果后来新加入的一个特性，他会动态的为被观察对象创建一个子类，并且重新其观察属性的setter方法，并将该对象的isa指向新的子类。</p><p>这个子类在使用过程中并没有被察觉是因为他还重写了他的<code>- class</code>方法，如果你使用objc runtime方法获取，则会得到其真正的类型。</p><p>所以在setter方法中，我们就不需要再去写<code>willChangeValueForKey</code>了，写了反而会导致触发两次。</p><ul><li>JSPatch， RN</li></ul><p>这一类都是通过其方法的动态特性实现的，通过方法名来反射其真正的方法，这里就不在详细赘述了。</p><h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p>为了更加了解objc，就需要了解其整个加载与初始化的过程。以及<code>+load</code>和<code>+initialize</code>的调用时机。（<code>+load</code>方法苹果官方已经不推荐使用，个人认为主要有2个原因，1是影响启动时间，2是加载的顺序太早，也早于C/C++的constructor方法，可能会导致一些初始化顺序上的错误。我们也尽量避免使用吧。）</p><h5 id="load-initialize"><a href="#load-initialize" class="headerlink" title="load / initialize"></a>load / initialize</h5><p>我们要知道这两个方法被调用的条件是什么。</p><p>load方法是类被初始化的时候调用，也就是在内存构建出全局类表之后，再统一调用其<code>+load</code>方法，当然，在加载依赖动态库的时候会先调用动态库的<code>+load</code>方法。</p><p>initialize是类被实例化的时候会被调用，他和<code>+load</code>没有直接连接，也没有先后顺序关系。</p><h5 id="objc-init"><a href="#objc-init" class="headerlink" title="objc_init"></a>objc_init</h5><p>这个是libSystem初始化objc runtime的入口，在这里比较重要的就是他会注册一个回调<code>_dyld_objc_notify_register(&amp;map_images, load_images, unmap_image);</code>。在动态库加载的生命周期中会进行objc类的初始化和<code>+load</code>方法的触发。值得注意的是这个回调的调用时机非常早。</p><p>然后会遍历macho文件中的objc信息，读取Class信息，并且将创建好的Class加入全局类表中，读取注册所有的SEL，读取并注册所有的protocol。</p><p>然后开始<code>realizeClass</code>，主要就是准备各种状态，基类元类信息和protocol，property等信息。</p><p>然后开始读取category信息，并且将其方法列表拷贝到class的method_list头部。</p><p>此后，开始调用<code>+load</code>方法</p><h2 id="message-forward"><a href="#message-forward" class="headerlink" title="message forward"></a>message forward</h2><p>当objc接收到不存在的SEL的时候，这时候会触发message forward，也就是系统给予我们一次机会，重新定向消息，比如转发给第三者。有些实现就是利用这个特性实现其proxy的功能。</p><p>除了发生不能响应消息会触发forward外，其实我们也能够直接手动通过<code>_objc_msgForward</code>来实现。</p><p>forward转发需要生成invocation来实现，这会导致性能的大幅下降，所以尽量不要使用这种特性。也有些地方介绍将其导向到一个空方法来避免这类crash，但这也会带来一些不可预期的错误，比如返回值问题，可能会产生一些随机值，带来更大的影响，所以个人不太建议这种方式去处理奔溃率的问题，这就是一种偷懒的自欺欺人方式。</p><h1 id="Runloop"><a href="#Runloop" class="headerlink" title="Runloop"></a>Runloop</h1><p>接下来我们来看看另一个非常重要的概念，runloop。runloop对于我们来说非常重要，可以说在我们刚入门的时候就已经在使用它了。</p><p>runloop其实就是一个死循环，在新的任务进入前一直保持等待状态，同时保证加入的任务按照顺序执行，这个在其他平台也有对应的实现。其目的有3个：</p><ul><li>保证线程不会直接退出</li><li>保证线程内的子任务按照顺序执行</li><li>用于线程间通信，也就是异步调用</li></ul><h5 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h5><p>要了解runloop和线程之间的关系，两者并不是等同的，runloop并不属于多线程的技术，只是在一个线程内保持一个事件队列来保证线程等待。</p><h5 id="runloopMode"><a href="#runloopMode" class="headerlink" title="runloopMode"></a>runloopMode</h5><p>需要理解不同mode对应的触发时机，什么时候需要使用不同的mode。</p><h5 id="timer"><a href="#timer" class="headerlink" title="timer"></a>timer</h5><p>timer需要结合mode来理解和使用。</p><h5 id="autorelease-pool"><a href="#autorelease-pool" class="headerlink" title="autorelease pool"></a>autorelease pool</h5><p>为什么要把autorelease pool放到这里说呢，主要是因为他触发的时机和runloop相关。他会注册两个observer在runloop中，分别为BeforeWaiting和Exit的时候，这能增加我们对于程序运行的理解，虽然现在arc很少碰到autorelease对象，但也要明白什么时候进行内存回收的。</p><h1 id="Block"><a href="#Block" class="headerlink" title="Block"></a>Block</h1><p>Block内容比较简单，首先我们要知道我们平时所使用的block分为3种，也是从其内存所处位置来区分的：</p><ul><li>malloc block</li><li>stack block</li><li>global block</li></ul><p>了解上述几种类型的区分主要是为了能够更清楚的处理内存循环引用的问题，知道哪些情况下会出现循环引用。在arc中，一般很少存在stack block，因为一旦被赋值则会自动进行拷贝操作。</p><p>strong和copy属性对于block来说其实都是一样的，最终转化为<code>_Block_copy</code>，但习惯和约定上我们都使用copy属性。</p><p>block按照生命周期来看，可以分为逃逸型(ESCAPE)闭包和非逃逸(NONESCAPE)型闭包，逃逸型闭包往往会引发内存泄露问题。</p><p>内存泄露特别需要注意隐式self的循环引用。</p><p>需要了解<strong>block, </strong>weak, __strong之间的区别和作用。</p><h5 id="进阶"><a href="#进阶" class="headerlink" title="进阶"></a>进阶</h5><p>block其实就是一个struct对象，其结构和objc对象布局类似，不过会有一个固定的<code>void (*invoke)(void *, ...);</code>成员，可以了解下这个方法的参数分别是什么，和objc_msgSend有什么类同和区别。</p><p>然后会把所有捕获的参数拼在结构最后，也可以看看<strong>block对象是如何实现其功能的，这里简单说一下，</strong>block对象是直接在堆上生成的，所以不受栈的限制。</p><p>既然是按照objc对象布局的，所以也就可以接收objc消息，和像objc对象一样使用了。</p><p>具体实现可以自行研读libclosure源码。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>要了解这些基础知识非常建议自己去读读源码，而他需要很多的基础编程知识，才能够读通其源码。</p><p>这里介绍了一些关键点，但并没有很详细的对每个点进行讨论，我仅仅作为抛砖引玉，个人还是建议亲自研读源码或者官方文档。</p><p>可以前往<a href="https://github.com/djs66256/iOS-developer-roadmap">github</a>查看其思维导图。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;objc是iOS的首要开发语言，在很多厂还是以objc为主，所以我们需要对objc更深入的了解，这里主要分为3个难点进行总结，runtime，runloop和block。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
      <category term="roadmap" scheme="http://djs66256.github.io/categories/iOS/roadmap/"/>
    
    
      <category term="runtime" scheme="http://djs66256.github.io/tags/runtime/"/>
    
      <category term="block" scheme="http://djs66256.github.io/tags/block/"/>
    
      <category term="objc" scheme="http://djs66256.github.io/tags/objc/"/>
    
      <category term="runloop" scheme="http://djs66256.github.io/tags/runloop/"/>
    
  </entry>
  
  <entry>
    <title>iOS-roadmap之前言</title>
    <link href="http://djs66256.github.io/2018/09/25/2018-09-25-iOS-roadmap%E4%B9%8B%E5%89%8D%E8%A8%80/"/>
    <id>http://djs66256.github.io/2018/09/25/2018-09-25-iOS-roadmap之前言/</id>
    <published>2018-09-24T16:25:37.000Z</published>
    <updated>2018-09-27T16:41:30.000Z</updated>
    
    <content type="html"><![CDATA[<p>在经历了这么多年的iOS开发，当初一个什么也不懂的大学生，也通过自身的努力，慢慢的有了一些心得和体会，所以这里准备将自己这么多年在iOS方面的经验进行一个总结。</p><a id="more"></a><p>做这件事的主要目的有两个：</p><ul><li>这么多年零碎的学习，却没有一个比较完整的总结，形成不了一个体系，我也感到比较遗憾，所以这里进行一些总结和归纳，让自己能更系统更全面的审视iOS开发。</li><li>自己在iOS开发的道路上几乎都是自己摸索，也走了不少弯路，所以想能够给新的开发者一些建议和引导。</li></ul><p>这里会涉及的东西并没有那些控件的使用，类的使用，我认为这些东西官方文档才是最清楚和正确的，所以我不会去讨论如何编码这么细节的内容。</p><p>同时这里可能会涉及一些比较底层的内容，也有一些比较生僻的东西，可能需要读者拥有一定的基础。</p><p>并不想单纯的罗列这些知识点，可能会详细讨论某些问题，或者按照一定的顺序去讲解内容，尽量保证我们对于知识的理解是有条理的，全面的。</p><p>内容的会持续更新，我会在<a href="https://djs66256.github.io">博客</a>和<a href="https://github.com/djs66256/iOS-developer-roadmap">github</a>同时更新。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在经历了这么多年的iOS开发，当初一个什么也不懂的大学生，也通过自身的努力，慢慢的有了一些心得和体会，所以这里准备将自己这么多年在iOS方面的经验进行一个总结。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
      <category term="roadmap" scheme="http://djs66256.github.io/categories/iOS/roadmap/"/>
    
    
  </entry>
  
  <entry>
    <title>程序在执行main方法之前都在做什么</title>
    <link href="http://djs66256.github.io/2018/09/19/2018-09-19-%E7%A8%8B%E5%BA%8F%E5%9C%A8%E6%89%A7%E8%A1%8Cmain%E6%96%B9%E6%B3%95%E4%B9%8B%E5%89%8D%E9%83%BD%E5%9C%A8%E5%81%9A%E4%BB%80%E4%B9%88/"/>
    <id>http://djs66256.github.io/2018/09/19/2018-09-19-程序在执行main方法之前都在做什么/</id>
    <published>2018-09-19T14:40:34.000Z</published>
    <updated>2018-09-22T08:36:31.000Z</updated>
    
    <content type="html"><![CDATA[<p>我们很少关注应用启动前，系统会给我们做些什么事情，可能知道<code>+ load</code>和<code>constructor</code>会在main方法之前执行。那么这次我们来看看main方法之前都做了哪些事情。</p><a id="more"></a><p>以下代码均经过摘取简化。</p><h2 id="1-dyld-start"><a href="#1-dyld-start" class="headerlink" title="1. _dyld_start"></a>1. _dyld_start</h2><p>系统启动应用的入口是<code>_dyld_start</code>，是用汇编写的。</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// call dyldbootstrap::start(app_mh, argc, argv, slide, dyld_mh, &amp;startGlue)</span></span><br><span class="line"><span class="keyword">bl</span>  __ZN13dyldbootstrap5startEPK12macho_headeriPPKclS2_Pm</span><br><span class="line"></span><br><span class="line"><span class="comment">// LC_MAIN case, set up stack for call to main()</span></span><br><span class="line"><span class="keyword">br</span>  x16</span><br></pre></td></tr></table></figure><p>首先dyld会调用<code>dyldbootstrap::start</code>，该方法会返回<code>main</code>函数的函数指针，并将其保存到x16中，然后才会继续调用<code>main</code>方法。但是调用这两个方法的方式是不一样的，<code>bl</code>是真正意义上的方法跳转，是会产生堆栈信息的，而<code>br</code>则相当于long jump，是不会产生新的栈帧信息的，所以我们在断点的时候，只能看到<code>main</code>作为程序入口的栈信息了。</p><p>那么接下来我们来详细看看<code>dyldbootstrap::start</code>里面做了些什么。</p><h2 id="2-dyldbootstrap-start"><a href="#2-dyldbootstrap-start" class="headerlink" title="2. dyldbootstrap::start"></a>2. dyldbootstrap::start</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// if kernel had to slide dyld, we need to fix up load sensitive locations</span></span><br><span class="line"><span class="comment">// we have to do this before using any global variables</span></span><br><span class="line">rebaseDyld(dyldsMachHeader, slide);</span><br><span class="line"></span><br><span class="line"><span class="comment">// allow dyld to use mach messaging</span></span><br><span class="line">mach_init();</span><br><span class="line"></span><br><span class="line"><span class="comment">// now that we are done bootstrapping dyld, call dyld&#x27;s main</span></span><br><span class="line"><span class="keyword">uintptr_t</span> appsSlide = slideOfMainExecutable(appsMachHeader);</span><br><span class="line"><span class="keyword">return</span> dyld::_main(appsMachHeader, appsSlide, argc, argv, envp, apple, startGlue);</span><br></pre></td></tr></table></figure><p>首先，我们都知道系统为了安全性，其实每个程序都会有一个随机的偏移值的，那么这里首先要对应的去除这个偏移量，以及初始化mach内核。</p><p>然后调用<code>dyld::_main</code>，这个最终会返回<code>main</code>函数地址。</p><h2 id="3-dyld-main"><a href="#3-dyld-main" class="headerlink" title="3. dyld::_main"></a>3. dyld::_main</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F; Entry point for dyld.  The kernel loads dyld and jumps to __dyld_start which</span><br><span class="line">&#x2F;&#x2F; sets up some registers and call this function.</span><br><span class="line">&#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F; Returns address of main() in target program which __dyld_start jumps to</span><br><span class="line">&#x2F;&#x2F;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// add dyld itself to UUID list</span></span><br><span class="line">addDyldImageToUUIDList();</span><br><span class="line"></span><br><span class="line"><span class="comment">// instantiate ImageLoader for main executable</span></span><br><span class="line">sMainExecutable = instantiateFromLoadedImage(mainExecutableMH, mainExecutableSlide, sExecPath);</span><br><span class="line"></span><br><span class="line"><span class="comment">// load any inserted libraries</span></span><br><span class="line">loadInsertedDylib(*lib);</span><br><span class="line"></span><br><span class="line">link(sMainExecutable, sEnv.DYLD_BIND_AT_LAUNCH, <span class="literal">true</span>, ImageLoader::RPathChain(<span class="literal">NULL</span>, <span class="literal">NULL</span>), <span class="number">-1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// link any inserted libraries</span></span><br><span class="line"><span class="comment">// do this after linking main executable so that any dylibs pulled in by inserted</span></span><br><span class="line"><span class="comment">// dylibs (e.g. libSystem) will not be in front of dylibs the program uses</span></span><br><span class="line">link(image, sEnv.DYLD_BIND_AT_LAUNCH, <span class="literal">true</span>, ImageLoader::RPathChain(<span class="literal">NULL</span>, <span class="literal">NULL</span>), <span class="number">-1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// only INSERTED libraries can interpose</span></span><br><span class="line"><span class="comment">// register interposing info after all inserted libraries are bound so chaining works</span></span><br><span class="line">image-&gt;registerInterposing();</span><br><span class="line"></span><br><span class="line"><span class="comment">// apply interposing to initial set of images</span></span><br><span class="line">sImageRoots[i]-&gt;applyInterposing(gLinkContext);</span><br><span class="line"></span><br><span class="line"><span class="comment">// run all initializers</span></span><br><span class="line">initializeMainExecutable();</span><br><span class="line"></span><br><span class="line"><span class="comment">// notify any montoring proccesses that this process is about to enter main()</span></span><br><span class="line">notifyMonitoringDyldMain();</span><br><span class="line"></span><br><span class="line"><span class="comment">// find entry point for main executable</span></span><br><span class="line">result = (<span class="keyword">uintptr_t</span>)sMainExecutable-&gt;getThreadPC();</span><br></pre></td></tr></table></figure><p>我们按照注释所说的，首先会加载inserted libraries，这个是通过运行参数中的配置，加载其中的lib，我们一般用不到。</p><p>然后是链接，也就是macho文件的初始化，绑定一些符号表等，这个在下面进行详细说明。</p><p><code>interpose</code>在iOS中是被禁用的，其功能相当于<code>swizzle</code>，这里我们也不去详细说明了。</p><p>然后是执行初始化工作，包括oc的运行时初始化，c++的静态对象初始化，c的constructor方法。</p><p>最后返回main方法的地址。</p><h3 id="3-1-dyld-link"><a href="#3-1-dyld-link" class="headerlink" title="3.1 dyld::link"></a>3.1 dyld::link</h3><p>这里我们来看看link都做了些什么。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// add to list of known images.  This did not happen at creation time for bundles</span></span><br><span class="line"><span class="keyword">if</span> (image-&gt;isBundle() &amp;&amp; !image-&gt;isLinked())</span><br><span class="line">    addImage(image);</span><br><span class="line"></span><br><span class="line"><span class="comment">// we detect root images as those not linked in yet</span></span><br><span class="line"><span class="keyword">if</span> (!image-&gt;isLinked())</span><br><span class="line">    addRootImage(image);</span><br><span class="line"></span><br><span class="line"><span class="comment">// process images</span></span><br><span class="line">image-&gt;link(gLinkContext, forceLazysBound, <span class="literal">false</span>, neverUnload, loaderRPaths, path);</span><br></pre></td></tr></table></figure><h3 id="3-2-ImageLoader-link"><a href="#3-2-ImageLoader-link" class="headerlink" title="3.2 ImageLoader::link"></a>3.2 ImageLoader::link</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">this</span>-&gt;recursiveRebase(context);</span><br><span class="line"><span class="keyword">this</span>-&gt;recursiveBind(context, forceLazysBound, neverUnload);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( !context.linkingMainExecutable )</span><br><span class="line">  <span class="keyword">this</span>-&gt;weakBind(context);</span><br><span class="line"></span><br><span class="line"><span class="comment">// interpose any dynamically loaded images</span></span><br><span class="line"><span class="keyword">this</span>-&gt;recursiveApplyInterposing(context);</span><br></pre></td></tr></table></figure><h3 id="3-3-ImageLoader-recursiveBind"><a href="#3-3-ImageLoader-recursiveBind" class="headerlink" title="3.3 ImageLoader::recursiveBind"></a>3.3 ImageLoader::recursiveBind</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Normally just non-lazy pointers are bound immediately.</span></span><br><span class="line"><span class="comment">// The exceptions are:</span></span><br><span class="line"><span class="comment">//   1) DYLD_BIND_AT_LAUNCH will cause lazy pointers to be bound immediately</span></span><br><span class="line"><span class="comment">//   2) some API&#x27;s (e.g. RTLD_NOW) can cause lazy pointers to be bound immediately</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// bind lower level libraries first</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; libraryCount(); ++i) &#123;</span><br><span class="line">  ImageLoader* dependentImage = libImage(i);</span><br><span class="line">  <span class="keyword">if</span> ( dependentImage != <span class="literal">NULL</span> )</span><br><span class="line">    dependentImage-&gt;recursiveBind(context, forceLazysBound, neverUnload);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// bind this image</span></span><br><span class="line"><span class="keyword">this</span>-&gt;doBind(context, forceLazysBound);</span><br></pre></td></tr></table></figure><p>在绑定的时候会先递归绑定其依赖的动态库，然后再来绑定自身。</p><h3 id="3-4-ImageLoaderMachOCompressed-doBind"><a href="#3-4-ImageLoaderMachOCompressed-doBind" class="headerlink" title="3.4 ImageLoaderMachOCompressed::doBind"></a>3.4 ImageLoaderMachOCompressed::doBind</h3><p>没啥好说的，看注释吧。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// run through all binding opcodes</span></span><br><span class="line">eachBind(context, &amp;ImageLoaderMachOCompressed::bindAt);</span><br><span class="line"></span><br><span class="line"><span class="comment">// if this image is in the shared cache, but depends on something no longer in the shared cache,</span></span><br><span class="line"><span class="comment">// there is no way to reset the lazy pointers, so force bind them now</span></span><br><span class="line"><span class="keyword">if</span> ( forceLazysBound || fInSharedCache ) </span><br><span class="line">  <span class="keyword">this</span>-&gt;doBindJustLazies(context);</span><br><span class="line">        </span><br><span class="line"><span class="comment">// this image is in cache, but something below it is not.  If</span></span><br><span class="line"><span class="comment">// this image has lazy pointer to a resolver function, then</span></span><br><span class="line"><span class="comment">// the stub may have been altered to point to a shared lazy pointer.</span></span><br><span class="line"><span class="keyword">if</span> ( fInSharedCache ) </span><br><span class="line">  <span class="keyword">this</span>-&gt;updateOptimizedLazyPointers(context);</span><br><span class="line"></span><br><span class="line"><span class="comment">// set up dyld entry points in image</span></span><br><span class="line"><span class="comment">// do last so flat main executables will have __dyld or __program_vars set up</span></span><br><span class="line"><span class="keyword">this</span>-&gt;setupLazyPointerHandler(context);</span><br></pre></td></tr></table></figure><h3 id="3-5-eachBind"><a href="#3-5-eachBind" class="headerlink" title="3.5 eachBind"></a>3.5 eachBind</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// resolve symbol</span></span><br><span class="line">symbolAddress = <span class="keyword">this</span>-&gt;resolve(context, symbolName, symbolFlags, libraryOrdinal, &amp;targetImage, last, runResolver);</span><br><span class="line"></span><br><span class="line"><span class="comment">// do actual update</span></span><br><span class="line"><span class="keyword">return</span> <span class="keyword">this</span>-&gt;bindLocation(context, addr, symbolAddress, type, symbolName, addend, <span class="keyword">this</span>-&gt;getPath(), targetImage ? targetImage-&gt;getPath() : <span class="literal">NULL</span>, msg);</span><br></pre></td></tr></table></figure><h2 id="4-dyld-initializeMainExecutable"><a href="#4-dyld-initializeMainExecutable" class="headerlink" title="4. dyld::initializeMainExecutable"></a>4. dyld::initializeMainExecutable</h2><p>我们回到初始化这里来。经过上面的macho绑定工作以后，虽然已经是一个完整的程序结构了，但是仍需要完成一些运行时的初始化。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// run initialzers for any inserted dylibs</span></span><br><span class="line">sImageRoots[i]-&gt;runInitializers(gLinkContext, initializerTimes[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">// run initializers for main executable and everything it brings up</span></span><br><span class="line">sMainExecutable-&gt;runInitializers(gLinkContext, initializerTimes[<span class="number">0</span>]);</span><br></pre></td></tr></table></figure><h2 id="5-ImageLoader-runInitializers"><a href="#5-ImageLoader-runInitializers" class="headerlink" title="5. ImageLoader::runInitializers"></a>5. ImageLoader::runInitializers</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Calling recursive init on all images in images list, building a new list of</span></span><br><span class="line"><span class="comment">// uninitialized upward dependencies.</span></span><br><span class="line"><span class="keyword">this</span>-&gt;recursiveInitialization(context, thisThread, images.images[i]-&gt;getPath(), timingInfo, ups);</span><br><span class="line"></span><br><span class="line"><span class="comment">// If any upward dependencies remain, init them.</span></span><br><span class="line"><span class="keyword">if</span> ( ups.count &gt; <span class="number">0</span> )</span><br><span class="line">  processInitializers(context, thisThread, timingInfo, ups);</span><br></pre></td></tr></table></figure><p>此时依赖的动态库会递归的调用初始化方法。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// initialize lower level libraries first</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; libraryCount(); ++i) &#123;</span><br><span class="line">  ImageLoader* dependentImage = libImage(i);</span><br><span class="line">  <span class="keyword">if</span> ( dependentImage != <span class="literal">NULL</span> ) &#123;</span><br><span class="line">    <span class="comment">// don&#x27;t try to initialize stuff &quot;above&quot; me yet</span></span><br><span class="line">    <span class="keyword">if</span> ( libIsUpward(i) ) &#123;</span><br><span class="line">      uninitUps.images[uninitUps.count] = dependentImage;</span><br><span class="line">      uninitUps.count++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> ( dependentImage-&gt;fDepth &gt;= fDepth ) &#123;</span><br><span class="line">      dependentImage-&gt;recursiveInitialization(context, this_thread, libPath(i), timingInfo, uninitUps);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// initialize this image</span></span><br><span class="line"><span class="keyword">bool</span> hasInitializers = <span class="keyword">this</span>-&gt;doInitialization(context);</span><br></pre></td></tr></table></figure><p>这里可以看出来，动态库的初始化方法是早于自身被执行的。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">ImageLoaderMachO::doInitialization</span><span class="params">(<span class="keyword">const</span> LinkContext&amp; context)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="comment">// mach-o has -init and static initializers</span></span><br><span class="line">doImageInit(context);</span><br><span class="line">doModInitFunctions(context);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> (fHasDashInit || fHasInitializers);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而初始化方法主要就是macho中被标识为S_MOD_INIT_FUNC_POINTERS的section中的方法，详细可以去了解下macho相关知识。</p><h2 id="6-libSystem-initializer"><a href="#6-libSystem-initializer" class="headerlink" title="6. libSystem_initializer"></a>6. libSystem_initializer</h2><p>以上其实就已经是整个初始化过程了，这里主要讲下一个非常重要的初始化方法。位于<code>libSystem</code>动态库中的<code>libSystem_initializer</code>。</p><p>大家都知道，在iOS中所有的系统基础库均出自<code>libSystem</code>，所以这个库一般都是第一个被初始化的。接下来我们来看看他具体做了什么。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">__libkernel_init(&amp;libkernel_funcs, envp, apple, vars);</span><br><span class="line"></span><br><span class="line">__libplatform_init(<span class="literal">NULL</span>, envp, apple, vars);</span><br><span class="line"></span><br><span class="line">__pthread_init(&amp;libpthread_funcs, envp, apple, vars);</span><br><span class="line"></span><br><span class="line">_libc_initializer(&amp;libc_funcs, envp, apple, vars);</span><br><span class="line"></span><br><span class="line"><span class="comment">// <span class="doctag">TODO:</span> Move __malloc_init before __libc_init after breaking malloc&#x27;s upward link to Libc</span></span><br><span class="line">__malloc_init(apple);</span><br><span class="line"></span><br><span class="line">_dyld_initializer();</span><br><span class="line"></span><br><span class="line">libdispatch_init();</span><br><span class="line">_libxpc_initializer();</span><br><span class="line"></span><br><span class="line">_container_init(apple);</span><br><span class="line"></span><br><span class="line">__libdarwin_init();</span><br></pre></td></tr></table></figure><p>这里我们可以根据名字看到其初始化都做了些什么，有个关键的<code>libdispatch_init</code>，我们再来看看。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">libdispatch_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">_dispatch_hw_config_init();</span><br><span class="line">_dispatch_time_init();</span><br><span class="line">_dispatch_vtable_init();</span><br><span class="line">_os_object_init();</span><br><span class="line">_voucher_init();</span><br><span class="line">_dispatch_introspection_init();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> _os_object_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">_objc_init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最终他会去调用objc的运行时初始化。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> _objc_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> initialized = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (initialized) <span class="keyword">return</span>;</span><br><span class="line">    initialized = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// fixme defer initialization until an objc-using image is found?</span></span><br><span class="line">    environ_init();</span><br><span class="line">    tls_init();</span><br><span class="line">    static_init();</span><br><span class="line">    lock_init();</span><br><span class="line">    exception_init();</span><br><span class="line">        </span><br><span class="line">    <span class="comment">// Register for unmap first, in case some +load unmaps something</span></span><br><span class="line">    _dyld_register_func_for_remove_image(&amp;unmap_image);</span><br><span class="line">    dyld_register_image_state_change_handler(dyld_image_state_bound,</span><br><span class="line">                                             <span class="number">1</span><span class="comment">/*batch*/</span>, &amp;map_2_images);</span><br><span class="line">    dyld_register_image_state_change_handler(dyld_image_state_dependents_initialized, <span class="number">0</span><span class="comment">/*not batch*/</span>, &amp;load_images);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个初始化中，会注册一个动态库初始化完成的回调。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/***********************************************************************</span></span><br><span class="line"><span class="comment">* load_images</span></span><br><span class="line"><span class="comment">* Process +load in the given images which are being mapped in by dyld.</span></span><br><span class="line"><span class="comment">* Calls ABI-agnostic code after taking ABI-specific locks.</span></span><br><span class="line"><span class="comment">*</span></span><br><span class="line"><span class="comment">* Locking: write-locks runtimeLock and loadMethodLock</span></span><br><span class="line"><span class="comment">**********************************************************************/</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *</span><br><span class="line">load_images(<span class="keyword">enum</span> dyld_image_states state, <span class="keyword">uint32_t</span> infoCount,</span><br><span class="line">            <span class="keyword">const</span> struct dyld_image_info infoList[])</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Discover load methods</span></span><br><span class="line">    load_images_nolock(state, infoCount, infoList);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Call +load methods (without runtimeLock - re-entrant)</span></span><br><span class="line">    call_load_methods();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> nil;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个回调中，会去初始化objc的运行时，并且与当前objc运行时合并，这里的合并可能会导致一些方法被覆盖等问题，category会覆盖原本的方法，主应用会覆盖动态库的方法，当然你也可以利用这个特性，做一些黑科技（个人不建议这样去覆盖方法，尽可能使用runtime来做，或者不要去做）。这些都是题外话了。</p><p>然后才是调用load方法。load方法和其他constructor方法一样都是被依赖的动态库中的方法是早于依赖方调用的。</p><p>注意，<code>dyld_image_state_dependents_initialized</code>这个事件是在自己<code>doInitialization</code>之前被调用的，所以一个动态库中<code>load</code>方法会早于自己的其他<code>constructor</code>类型的方法，在做某些黑科技的时候不要搞错了。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>这里我们主要需要注意的就是初始化方法调用的顺序问题，在做一些初始化的时候不要出现违反顺序的情况。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;我们很少关注应用启动前，系统会给我们做些什么事情，可能知道&lt;code&gt;+ load&lt;/code&gt;和&lt;code&gt;constructor&lt;/code&gt;会在main方法之前执行。那么这次我们来看看main方法之前都做了哪些事情。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="dyld" scheme="http://djs66256.github.io/tags/dyld/"/>
    
  </entry>
  
  <entry>
    <title>函数式编程思想简介</title>
    <link href="http://djs66256.github.io/2018/09/16/2018-09-16-%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B%E6%80%9D%E6%83%B3%E7%AE%80%E4%BB%8B/"/>
    <id>http://djs66256.github.io/2018/09/16/2018-09-16-函数式编程思想简介/</id>
    <published>2018-09-16T08:15:38.000Z</published>
    <updated>2018-09-16T17:28:05.000Z</updated>
    
    <content type="html"><![CDATA[<p>在平时的iOS开发中，我们大部分还是依靠着最早的MVC的思想开发着，最多也是进行了部分变种，比如MVP，MVVM，这些都是完全的面向对象的思想，在处理一些简单的场景的时候还是非常有效的，但是在某些场合，我们总会觉得难以解耦，难以进行模块化拆分，这是由于目前大部分架构中，在业务逻辑部分依然是过程性的，整个过程中还是需要依赖大量的其他模块，这次我们来看看函数式编程的思想，以另一种角度来看问题是否能够去解决部分问题。</p><a id="more"></a><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>函数式编程对于我们这种熟悉了面向对象的人来说，是非常别扭和难以理解的。首先我们需要了解几个概念，这里不讨论严格的概念，可以说这是我结合iOS开发的部分特性所做的一些理解。</p><h4 id="函数是一等公民"><a href="#函数是一等公民" class="headerlink" title="函数是一等公民"></a>函数是一等公民</h4><p>这是函数式编程中最重要的思想基础。万物都是对象，包括函数。这个思想的引入，让我们可以将函数作为参数、返回值这一类原本对象才可以做的事情。在objc中的<code>block</code>，swift中的<code>closure</code>都可以认为是函数式编程的基础。</p><h4 id="高阶函数"><a href="#高阶函数" class="headerlink" title="高阶函数"></a>高阶函数</h4><p>参数或者返回值中存在函数对象的，都认为是高阶函数。可以说高阶函数是函数式编程中的连接器，他同样也可以实现比如<code>函数修饰器</code>这样的功能。</p><h4 id="纯函数"><a href="#纯函数" class="headerlink" title="纯函数"></a>纯函数</h4><p>没有副作用的函数，简单的说就是给以相同的输入，必定有相同的输出。这也是函数式编程思想中非常重要的一个点，因为没有副作用可以让我们的设计变得更加简单。</p><h4 id="monad"><a href="#monad" class="headerlink" title="monad"></a>monad</h4><p>关于monad这个概念，很多人都有不同的理解，但基本上都是把纯函数连接起来的一种设计模式，同时这也是纯的。这个概念在业务开发中基本上不太可能实现，但是在业务的部分场景还是可以做到的。</p><h2 id="优势"><a href="#优势" class="headerlink" title="优势"></a>优势</h2><p>由于函数式编程中要求大量的纯函数，那么我们就可以很容易的进行单元测试。</p><p>同时在多线程编程中，由于其独立性和无副作用，我们也不需要思考数据竞争，死锁等这类问题。也能更灵活的进行功能拆分和执行分配。</p><p>函数式编程是一套比较全面的编程思想，对于我们面向对象比较熟悉的人来说，有些地方确实会比较奇怪。虽然说我们也可以用面向对象来实现类似的功能，但是函数式编程会更加简单和直接。</p><h2 id="框架"><a href="#框架" class="headerlink" title="框架"></a>框架</h2><p>函数式编程是专门为了计算这类场景而生的，目前由于脚本语言的兴盛，这类思想也同时融入了很多面向对象语言中。</p><h4 id="promise"><a href="#promise" class="headerlink" title="promise"></a>promise</h4><p>promise可以说是我最早接触到的函数式编程相关框架。promise的初衷是为了解决异步callback的多次调用而开发的。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">step1(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (err) print(err)</span><br><span class="line">    <span class="keyword">else</span> step2(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (err) print(err)</span><br><span class="line">        <span class="keyword">else</span> step3(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">            <span class="comment">// go on ...</span></span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>那么通过promise，我们可以改写为：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">step1()</span><br><span class="line">.then(step2)</span><br><span class="line">.then(step3)</span><br><span class="line">.then(<span class="comment">/* go on ... */</span>)</span><br><span class="line">.catch(print)</span><br></pre></td></tr></table></figure><h4 id="ReactiveX"><a href="#ReactiveX" class="headerlink" title="ReactiveX"></a>ReactiveX</h4><p>同样类似的<code>ReactiveX</code>也具有相同的效果，不过RX把注意点更加放在了对数据的处理上面，这也就正式的把我们从逻辑的思维中脱离开来，从而关注数据的流动上。后面会来讨论这种思维的变化给我们带来了什么。</p><p>RX中<code>signal</code>, <code>observer</code>, <code>observable</code>, <code>subject</code>主要负责数据的流动，<code>scheduler</code>负责执行线程相关操作。</p><h4 id="Redux"><a href="#Redux" class="headerlink" title="Redux"></a>Redux</h4><p>参考ReSwift</p><p><img src="/images/2018/functional/redux.png" alt=""></p><p>可以说这是我接触到的第一个以函数式编程为基础的，专注于数据流动的框架。</p><p>关于这个框架的详细介绍，我相信有很多人都比我更加了解，也比我讲的好，所以这里就不做介绍了。</p><h2 id="数据流动"><a href="#数据流动" class="headerlink" title="数据流动"></a>数据流动</h2><h4 id="一个请求的例子"><a href="#一个请求的例子" class="headerlink" title="一个请求的例子"></a>一个请求的例子</h4><p>在我们平时的开发过程中，基本上关注的是业务的流程实现，按照逻辑的顺序进行思考和设计，如果我们按照标准的面向对象来设计一个请求，那么很有可能是这样的。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)request &#123;</span><br><span class="line">  Request *req = [Request new]</span><br><span class="line">  req.delegate = <span class="keyword">self</span>;</span><br><span class="line">  [req start];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)requestComplete:(Request *)req &#123;</span><br><span class="line">  Request *req = [Request new];</span><br><span class="line">  [req startWithComplete:^(Request *req) &#123;</span><br><span class="line">    <span class="keyword">if</span> (req.error == <span class="literal">nil</span>) &#123;</span><br><span class="line">      <span class="built_in">NSDictionary</span> *json = [JSON parse:req.data];</span><br><span class="line">      ClassA *a = [ClassA from:json];</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这是一个非常典型的设计方式，当我们只有一个请求的时候这样做也非常简单，但是一旦拥有多个请求的时候，事情就变得很复杂。那么如果我们使用函数式来改写，就比如AF所做的那样。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)request &#123;</span><br><span class="line">  Request *req = [Request new];</span><br><span class="line">  [req startWithComplete:^(Request *req) &#123;</span><br><span class="line">    <span class="keyword">if</span> (req.error == <span class="literal">nil</span>) &#123;</span><br><span class="line">      <span class="built_in">NSDictionary</span> *json = [JSON parse:req.data];</span><br><span class="line">      ClassA *a = [ClassA from:json];</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样在出现多个请求的时候，回调也能保证其唯一性。但是这个依然还是有一些问题。</p><ol><li>数据解析和model化等操作依然需要在回调时自己去调用，如果将这些放入Request中，又难以保持低耦合性。</li><li>自动化测试困难，因为请求返回后必然是接着视图的更新或者业务逻辑的执行，自动化测试一旦涉及这些会显得很臃肿。</li><li>当多个请求在需要并发，而回调需要保持同步的时候。</li><li>流量控制只能依赖于网络层的来控制，同时线程控制依然需要手动控制，可能GCD会让这类操作稍微简单一点。</li></ol><p>那么如果我们只关注数据来改写呢，可能会是这样的，为了更适合函数式编程，这里采用js来改写：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Request().then(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> json = JOSN.parse(data)</span><br><span class="line">  <span class="keyword">let</span> a = ClassA.from(json)</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;).catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>那么我们来看看如何解决上述的几个问题。</p><p>为了保证低耦合性，我们不希望将解析等操作放入网络层中，但也不希望在业务层过多的出现这类代码。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Request().then(<span class="built_in">JSON</span>.parse).then(ClassA.from).then(<span class="function"><span class="params">a</span> =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">&#125;).catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>而其中分离出来的中间过程，由于是纯函数，是可以完全的单元测试的，这也就增强了单元测试的粒度。如果你说客户端业务代码的单元测试是否有这个必要，那么我觉得很多时候没有这个时间和必要，但是当我们做一些基础库，或者SDK时，涉及的面非常广时，我们不得不去考虑单元测试的必要性，而且覆盖率越高越好。</p><p>如果要实现并发这种呢？按照数据的角度去思考其实就非常的简单。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> req1 = Request().then(<span class="built_in">JSON</span>.parse).then(ClassA.from)</span><br><span class="line"><span class="keyword">let</span> req2 = Reqeust().then(<span class="built_in">JSON</span>.parse).then(ClassB.from)</span><br><span class="line"><span class="built_in">Promise</span>.all([req1, req2]).then(<span class="function"><span class="params">ab</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// reload data</span></span><br><span class="line">&#125;).catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>由于中间过程是纯函数，那么我们要将他们放入子线程也就非常的简单，甚至RX自己就提供了这样的接口。</p><h4 id="函数修饰器"><a href="#函数修饰器" class="headerlink" title="函数修饰器"></a>函数修饰器</h4><p>这是某些语言的特性，所以拿出来简单说说。</p><p>一个函数修饰器的参数中必然至少有一个是函数，返回的也依然是函数。这个比较难以理解，我们来举一个例子：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">needLogin</span>(<span class="params">f</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (login)</span><br><span class="line">    <span class="keyword">return</span> f</span><br><span class="line">  <span class="keyword">else</span> </span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; print(<span class="string">&#x27;error&#x27;</span>) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么我们需要为某个函数增加这个检查的时候，只需要增加一层修饰即可：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> NewRequest = needLogin(Request)</span><br><span class="line">NewRequest().then(...)</span><br></pre></td></tr></table></figure><p>python:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@needLogin</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">Request</span>()</span></span><br><span class="line"><span class="function">  # ...</span></span><br><span class="line"><span class="function">  <span class="title">pass</span></span></span><br></pre></td></tr></table></figure><p>这种方法的优势在于，我们不需要在请求里去增加这种业务代码，同时也不需要在业务层里进行复杂的校验，同时这样表示也会更加接近自然语言。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>这里简单介绍了函数式编程思想，和我们平时开发所使用的方法都不一致，也没有哪一种方法更好的定论。但是这一种以数据作为主体的方法却是非常值得我们思考的，因为可以认为数据是不会存在高耦合这样的问题，这也给我们提供了一种新的解决问题的思路。</p><p>这是一种新的思考方式，可以说这里的一些内容是无法让你真正改变的，很多还是需要靠自身的理解和感悟，才能将其应用于平时的开发之中。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在平时的iOS开发中，我们大部分还是依靠着最早的MVC的思想开发着，最多也是进行了部分变种，比如MVP，MVVM，这些都是完全的面向对象的思想，在处理一些简单的场景的时候还是非常有效的，但是在某些场合，我们总会觉得难以解耦，难以进行模块化拆分，这是由于目前大部分架构中，在业务逻辑部分依然是过程性的，整个过程中还是需要依赖大量的其他模块，这次我们来看看函数式编程的思想，以另一种角度来看问题是否能够去解决部分问题。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://djs66256.github.io/categories/iOS/"/>
    
    
      <category term="functional" scheme="http://djs66256.github.io/tags/functional/"/>
    
  </entry>
  
</feed>
